// Must match the CHECK constraint on prices.mainland_chain
// (mainland_price_migration.sql). Keep in sync if that constraint changes.
export const MAINLAND_CHAINS = ['Carrefour', 'E.Leclerc', 'Système U', 'Auchan', 'Autre'];
