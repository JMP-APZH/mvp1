# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) for the Prix Martinique project.

## Project Overview

**Prix Martinique ("Vie chère en Martinique")** is a community-driven PWA for price tracking and comparison across supermarkets in Martinique.

- **Live URL:** https://mvp1-prixmartinique.vercel.app
- **Stack:** React 19 (Vite), Tailwind CSS v3, Supabase (Postgres, Auth, Storage, Real-time).
- **Core Mission:** Empowering residents to combat the high cost of living through transparency and collective data.

## Current State (Audit: May 2026)

### Active Version: `App10.jsx`
- **Status:** Launched. Production.
- **Key Imports in `main.jsx`**: `App10.jsx` is the primary entry point.
- **Includes:** Advanced price charts (`recharts`), BQP (Bouclier Qualité Prix) verification system, Diaspora tracking, Personal stats, Shopping list (Supabase-synced), Toast notifications, Password reset flow.

### File Inventory (App Versions)
| File | Status | Description | Recommendation |
| :--- | :--- | :--- | :--- |
| `src/_archive/App.jsx` | Archived | Blue theme, original Price Entry prototype. | Do not touch |
| `src/_archive/App2.jsx` | Archived | Intermediate version. | Do not touch |
| `src/_archive/App3.jsx` | Archived | Orange/red gradient theme introduction. | Do not touch |
| `src/_archive/App4.jsx` | Archived | QuaggaJS integration for iOS. | Do not touch |
| `src/_archive/App5.jsx` | Archived | Basic Auth + Gamification integration. | Do not touch |
| `src/_archive/App6.jsx` | Archived | HybridBarcodeScanner implementation (v1). | Do not touch |
| `src/_archive/App7.jsx` | Archived | UI Refinements and French accent fixes. | Do not touch |
| `src/_archive/App8.jsx` | Archived | Feature-complete version prior to Antigravity sessions. | Do not touch |
| `src/_archive/App9.jsx` | Archived | Iteration on Z-index and Store Selection. | Do not touch |
| `src/App10.jsx` | **ACTIVE** | Current version. | **PRESERVE** |

### Features Completed (Fully Working)
- **Authentication**: Google OAuth & Email/Password via `AuthContext`. Password reset flow (implicit flow, cross-browser).
- **Gamification**: Points (+10 per scan), levels, and dynamic leaderboard.
- **Barcode Scanning**:
  - `BarcodeDetector` (Android/Chrome) → High performance.
  - `ZXing` / `Quagga` fallback (iOS/Safari). Validated on physical iOS.
- **Store Selection**: Multi-step wizard (`StoreSelectionWizard.jsx`) with favorites and geocoding support. 78 store locations loaded, aligned with latest BQP documentation.
- **BQP Verifier**: Association of products with BQP categories and community voting on accuracy/quality.
- **Diaspora Tracking**: Profile-level region/city tracking (e.g., Hexagone scans) with privacy-protected backend logging.
- **Real-time Updates**: Live price feed via Supabase real-time channels.
- **Personal Stats**: User-specific contribution analytics (`PersoStats.jsx`).
- **Shopping List (Cloud Sync)**: `src/hooks/useShoppingList.js`. Supabase-backed for authenticated users, localStorage fallback for anonymous users. Auto-migrates localStorage items on login. Product photos pulled from `prices` table.
- **Toast Notifications**: `src/components/Toast.jsx` + `src/hooks/useToast.js`.
- **Auth Loading Spinner**: Prevents mobile white screen during auth init (`AuthContext.jsx`, `index.css`).
- **Supabase Keep-Alive**: GitHub Actions cron (`.github/workflows/keep-supabase-alive.yml`) pings Supabase **twice a week** (Monday + Thursday at 08:00 UTC) to prevent free-tier auto-pause (threshold: 7 days inactivity). If the project gets paused despite this, restore it at supabase.com/dashboard — you have 90 days before data is lost. Long-term fix: upgrade to Supabase Pro ($25/month) for guaranteed uptime.

### Feb 28, 2026 — Security Audit & Shopping List Milestone
Four Supabase database migrations applied:
1. **`enable_rls_on_public_tables`** — RLS enabled on `stores`, `prices`, `products` with correct public-read / authenticated-write policies.
2. **`fix_function_search_paths_and_view`** — Fixed mutable `search_path` on 6 DB functions; `increment_store_popularity` promoted to `SECURITY DEFINER`; `feature_request_stats` view recreated without `SECURITY DEFINER`.
3. **`fix_rls_policy_duplicates_and_auth_uid`** — Removed 7 duplicate RLS policies; fixed `auth.uid()` per-row evaluation → `(select auth.uid())` across 30+ policies on 12 tables.
4. **`add_missing_fk_indexes_and_drop_redundant`** — Added 9 missing FK indexes (`bqp_quality_votes`, `bqp_votes`, `feature_requests`, `feature_votes`, `price_likes`, `shopping_lists`, `user_badges`, `user_favorite_stores`, `user_favorites`); dropped `idx_bqp_code` (duplicate of existing unique constraint).

### Post-Launch Fixes & Polish (Mar–May 2026)
- **Password reset**: Full reset flow + in-app password change (`bd766a4`). Multiple iterations to resolve cross-browser failures; landed on implicit flow (`4c8a3ba`).
- **Bundle splitting**: Legacy `App*.jsx` files moved to `src/_archive/`. Vite `manualChunks` configured (`5b9e596`). Bundle reduced from ~890 KB.
- **Shopping list photos**: `fetchItems` in `useShoppingList.js` joins `prices` to retrieve `product_photo_url` per product (`5b9e596`).
- **UX polish**: Toast system, welcome screen, a11y improvements, BQP performance (`7528bae`).
- **Mobile white screen**: Loading spinner added during auth initialization (`3c3e105`).

### Jul 21, 2026 — Gamification (`award_points`) Regression Fixed
Discovered while auditing a 10-item test scan session ahead of the RPPRAC presentation: the test user completed 10 price submissions but earned **0 points**.

- **Root cause**: `awardPoints` in `AuthContext.jsx` was refactored on 2026-02-05 (`73f6c22`) from `(activityType, points, description)` down to `(points, description)`, dropping the `p_activity_type` arg sent to the `award_points` Postgres RPC (which requires it, no default). Call sites in `App10.jsx` (price submission `+10`, BQP verification `+5`) were never updated and kept calling it with 3 positional args, so `points`/`description` were silently misassigned and the RPC call failed every time. The error was caught and only `console.error`'d — the price/photo submission itself always succeeded, so the bug was invisible in normal use.
- **Impact**: Every point-earning action for every user since 2026-02-05 (~5.5 months) silently failed. Verified via direct Supabase query: all user_profiles created after that date sit at 0 points regardless of activity; only the one profile predating the regression has nonzero points.
- **Fix**: Restored the 3-arg signature and `p_activity_type` passthrough in `awardPoints` (`AuthContext.jsx`). No call-site changes needed — they were already correct.
- **Backfill**: The Jul 20 test session (10 price submissions by user "Tony") was manually backfilled via 10 direct `award_points` RPC calls → 100 pts, Level 2. All other historical activity since 2026-02-05 was **not** backfilled by design — only submissions from this fix onward award correctly.
- **Also observed, not fixed**: `user_profiles.city` is not trimmed before insert, producing duplicate-looking entries (e.g. `"Zurich"` vs `"Zurich "`) in city-based stats/leaderboards. Low priority, cosmetic.

### Jul 21, 2026 — Community Leaderboard Querying Nonexistent Table
Found while investigating why the Communauté → Classement tab showed no ranking at all for any user, not just Tony.

- **Root cause**: `Leaderboard.jsx` has queried `.from('profiles')` since 2026-02-10 (`68fda8f`) — a table that **does not exist** in the schema (confirmed: `PGRST205`). The correct table, used everywhere else in the app, is `user_profiles`. The failed query was caught silently, leaving the component permanently stuck on its "Pas encore de classement" empty state for ~5.5 months, independent of the `award_points` bug above.
- **Fix**: `Leaderboard.jsx` now queries `user_profiles`, matching `AuthContext.jsx` / `AdminDashboard.jsx` / the schema.
- **`total_contributions` — permanent DB fix (Jul 21, 2026, applied manually via Supabase SQL Editor)**: this column was never written to by `award_points` or any migration (permanently 0 for everyone). Initially patched around client-side in `Leaderboard.jsx` (deriving the count from `COUNT(prices)` per leaderboard load); that workaround has since been **replaced** by `total_contributions_migration.sql`, which adds `sync_total_contributions()` — a trigger on `prices` (insert/update/delete) that recomputes the affected user's `total_contributions` from `COUNT(*)` server-side — plus a one-time backfill. `Leaderboard.jsx` now reads the column directly again. Verified after applying: backfilled counts matched actual `prices` rows exactly (Tony: 10, Jean-Marie Philocles: 8, others: 0). The migration file lives at `prix-martinique/total_contributions_migration.sql`; run manually in the Supabase Dashboard → SQL Editor (no `supabase/` CLI migrations folder in this project — all schema changes are applied this way, see the many `*_schema.sql` / `*_migration.sql` files in the repo root).

### Jul 21, 2026 — Component-Wide Bug Sweep (pre-RPPRAC audit)
Went looking for more of the same failure class (wrong table/column names, RPC signature mismatches) across every active component (everything reachable from `App10.jsx`; `src/_archive/**` and the unused `BQPSearchPrototype.jsx` excluded). Method: cross-referenced every `.from()`/`.rpc()`/embedded-select call against the real schema, then live-tested each one with the anon key.

**Confirmed and fixed:**
- **`Community.jsx` "Score de Souveraineté"**: filtered `products.is_bqp` (column doesn't exist — real column is `is_declared_bqp`). Introduced in the same commit as the `profiles`-table bug (`68fda8f`, 2026-02-10) — this stat has read a hardcoded **0%** since the feature was created. Fixed: now filters `is_declared_bqp`.
- **`ShoppingList.jsx` basket price comparator**: embedded select `stores(id, name, type)` — `stores` has no `type` column (closest real column is `category`, e.g. `"Hypermarché"`). Every comparison silently failed the moment a shopping list had ≥1 item (`storeBaskets` never populated — this is the "compare your basket total across nearby stores" feature). `type` wasn't used downstream, so it was simply dropped from the select.
- **`get_bqp_vote_stats` RPC**: called from `App10.jsx`'s `fetchBqpVotes` (fires whenever a scanned barcode matches an existing BQP association) — errored every time with `column "vote_type" does not exist`, because `bqp_quality_votes` uses a column named `vote` (see `milestone2_schema.sql`), not `vote_type` like `bqp_votes`. Confirmed live: both vote tables currently have 0 rows, consistent with community voting never having round-tripped successfully. Fix written as `bqp_vote_stats_fix_migration.sql` (DB-side — same manual-apply-via-SQL-Editor flow as `total_contributions_migration.sql`; **not yet applied as of this writing**, pending you running it).

**Flagged, not changed (uncertain or out of scope):**
- **Admin/journalist role gate** (`UserMenu.jsx` `userRoles.includes('admin')`, sourced from the `user_roles` table via `AuthContext.jsx`'s `fetchUserRoles`): an anon-key probe showed 0 rows, but RLS may simply be hiding other users' rows from an unauthenticated read — this could **not** be confirmed as broken vs. working-as-intended. Worth a manual check: log in as your own account and confirm the "Console Admin" entry still appears in the user menu.
- **`AdminDashboard.jsx` KPI trend badges**: the `+12%` / `+5%` arrows next to Total Scans / Utilisateurs Actifs are hardcoded, not computed from any query. Low risk technically, but worth knowing before presenting real-looking-but-fake growth numbers to RPPRAC.
- **`PersoStats.jsx` "Mes Économies"**: computed as `points * 0.15` (explicitly commented `// Mock logic`), but the adjacent "Comment c'est calculé ?" explainer describes a completely different methodology (savings vs. highest observed price for the same product). The number itself isn't wrong per se, but the stated methodology doesn't match the code — worth rewording the explainer or implementing the described logic before anyone asks how it's calculated.
- **Badges system** (`user_badges`, `badges` tables): both empty in production; no code path was found that ever inserts into them. The "Mes Badges" UI in `PersoStats.jsx` is fully wired but has nothing to display — appears to be a stubbed-out feature rather than a bug.

### Jul 21, 2026 — Product Detail Card, Swipeable Photos, Admin Barcode Audit
Three features requested ahead of the RPPRAC presentation, built against real production data:

- **`ProductDetailModal.jsx`** (new): tapping a card in the recent-prices feed opens a "wow" detail view — total scan count, distinct-shop count, best price, a price-trend chart (reusing the extracted `PriceHistoryChart.jsx`), and an intra-Martinique store-by-store price comparison sorted cheapest-first. Excludes legacy `prices` rows with a null `store_id` (pre-dates mandatory store selection) from the shop count and comparison table — confirmed live that at least one such row exists (`881db4d5-...`, submitted before store selection was enforced) and would otherwise show as a misleading "Magasin inconnu — cheapest!" entry.
- **`PriceHistoryChart.jsx`** (extracted from `App10.jsx`, where it was previously inline and only used by the BQP-scan flow): now shared between that flow and `ProductDetailModal`. Same props/behavior, just relocated.
- **Swipeable photo viewer**: the image zoom modal (`App10.jsx`) previously showed one image at a time — closing and reopening was required to compare the product photo against the price-tag photo. Now a single modal with left/right arrows, dot indicators, and touch-swipe (50px threshold) cycles between all photos for that scan.
- **`BarcodeAudit.jsx`** (new, admin-only, Console Admin → "Intégrité Codes-barres" sub-tab): lists recent scans with a product photo next to the currently-stored `products.barcode`, so an admin can visually compare the captured value against the barcode printed on the packaging. Two actions per entry: **"J'ai corrigé"** (admin enters the correct barcode, updates `products.barcode` directly, logged with `resolution_type = 'admin_modification'`) or **"Demander re-capture"** (flags the item for the original user to rescan, `resolution_type = 'user_recapture'`, no value changed). Every action is an insert into `barcode_flags` — append-only, publicly readable (RLS `for select using (true)`) so the audit trail survives corrections and is inspectable by RPPRAC or any other external party, not just admins. Migration: `barcode_audit_migration.sql`, applied 2026-07-21 (verified: table exists, both migrations from this date confirmed live — `get_bqp_vote_stats` no longer errors, `barcode_flags` is queryable).
- **Photo zoom + barcode overlay** (`BarcodeAudit.jsx`, added same day after initial admin testing): the product photo thumbnail is now clickable, opening a full-screen zoom with the stored barcode overlaid bottom-left (dark chip, monospace) so the admin can compare the captured value against the packaging without leaving the zoomed view.

### Jul 21, 2026 — Closed Out: Fake Trend Badges, Savings Methodology Mismatch
- **`AdminDashboard.jsx` KPI badges**: replaced the hardcoded `+12%`/`+5%` with real numbers — count of scans and count of distinct active users in the trailing 7 days (`created_at >= now() - 7 days`). Percentage-of-previous-period was considered and rejected: with volumes this low, a previous-period count of 0 makes any % swing either undefined or absurd (e.g. "+∞%"); an absolute weekly count stays honest and meaningful regardless of baseline. Badge is hidden entirely when the weekly count is 0, rather than showing a claim with nothing behind it. Verified live: 10 scans / 1 active user this week (Tony's Jul 20 session).
- **`PersoStats.jsx` "Mes Économies"**: replaced the mock `points * 0.15` with the methodology the UI already claimed — for each product the user has priced, compare their price to the highest price observed for that same product across all stores, sum the positive differences. Verified live: Tony (all 10 products unique, no other pricer to compare against) → €0.00; Jean-Marie Philocles (has repeat-priced products) → €0.35. Both are real, non-hardcoded, and match the on-screen explainer (also tightened for precision — "highest price observed," not "average of highest prices observed").

### Jul 21, 2026 — Leaderboard City Filter, Category Filtering, Store Filter
- **Leaderboard city filter silently excluded Tony**: root cause traced to `AuthModal.jsx`'s signup form, which uses a free-text "Ville" input for *every* region including Martinique (972) — unlike `UserMenu.jsx`'s profile editor, which uses a clean dropdown for 972 residents. Tony's stored `city` was `"Fort-de-France "` (confirmed via exact codepoint dump: trailing space, charcode 32). The Leaderboard's `.eq('city', city)` filter compares against the clean hardcoded Martinique-cities list, so it never matched — Tony only ever showed up under "Toute la Martinique" (no filter applied). Also found affecting a second profile (`"Zurich "` vs a separate clean `"Zurich"` profile). **Fix**: `AuthContext.jsx` now trims `city` in `signUp()`, and `updateProfile()` trims every string value generically (defense in depth for any future free-text profile field, not just city). **Data cleanup**: `trim_city_cleanup_migration.sql` — one-time `UPDATE ... SET city = trim(city)` for existing corrupted rows. Manual Supabase SQL Editor run, same as prior migrations.
- **`products.category` vs `category_id`**: confirmed **not a bug**. `category` (legacy text column) is intentionally always null — hardcoded in `submitPrice` (`App10.jsx`) with a `// Legacy field` comment. The real relationship is `category_id → categories.id`, verified live via a working embedded join (`products.select('*, categories(name, icon)')`). Data-completeness gap, not a technical one: only 5 of 23 products currently have `category_id` set, since it's an optional field during capture — most scans don't get categorized. Worth revisiting capture-flow UX later if category coverage matters for the Friday demo, but out of scope for this pass.
- **`CategoryManager.jsx`** (admin-only, Console Admin → "Catégories" sub-tab): lets an admin add a new category (icon + name, `display_order` auto-computed as current max + 10). Required a new RLS policy — `categories_schema.sql` only ever defined a public **read** policy; insert was completely blocked (confirmed live: `42501` RLS violation) until `categories_admin_insert_migration.sql` added an admin-gated insert policy (same `user_roles` check pattern as `barcode_flags`). **Superseded same day** — see the next entry below; the component was removed after a scope clarification, but the RLS migration/policy is harmless to leave in place.
- **Category icon picker + store filter** (`App10.jsx`, Comparer/search tab): the `categoryFilter` state and its filtering logic already existed but had **no UI to actually set it** — only a chip to clear it. Added: a "Catégorie" button under the search bar opens an icon-grid overlay (from the `categories` table); a new "Magasin" button opens a store-picker overlay, populated only with stores that have ≥1 scanned product (derived from the loaded `prices` feed, not a separate query). Selecting a store narrows the category picker to only categories with a product actually scanned at that store (`pickerCategories`), and narrows the product list (`filteredPrices`) to that store. Both filters are independent and combinable; each shows a dismissible chip when active. Required adding `stores(id, ...)` (previously just `name, full_address`) to the `prices` load query so store-level filtering has an ID to match on.

### Jul 21, 2026 — Product Completion Tool, Hunter Detail Card, Comments + Sharing
Scope clarification from Jean-Marie: the standalone "Catégories" admin tool (add brand-new category types) wasn't what was wanted — the actual need was the same *per-product* correction pattern already built for barcodes, extended to category. Three changes:

- **`ProductCompletion.jsx`** (renamed from `BarcodeAudit.jsx`; `CategoryManager.jsx` deleted, its sub-tab removed from `AdminDashboard.jsx`): Console Admin's sub-tab is now "Compléter produit" (was "Intégrité Codes-barres" + a separate "Catégories" tab). Same photo-zoom + barcode-flag workflow as before, plus a category `<select>` directly on each card (current category or "Non catégorisé", any category from the `categories` table) that updates `products.category_id` immediately on change — no flag/audit workflow for this one, since (unlike a barcode) there's no "ask the user to recapture" equivalent for a category; it's just a direct correction.
- **`HunterDetailModal.jsx`** (new): clicking any row in `Leaderboard.jsx` (not just the podium) opens a contributor card — prices collected and distinct shops visited at the top, then the full list of their collected products (name, price, store, date). Mirrors `ProductDetailModal`'s pattern but scoped by user instead of product.
- **Product comments + sharing** (`ProductDetailModal.jsx`): logged-in users can post a comment; anyone (including anonymous visitors) can read them, per spec. Comments are sorted most-liked-first (like count desc, then newest first as tiebreak). Each comment shows the author's `display_name` and a "Top Chasseur" badge if they're currently in the leaderboard's global top 3 (computed live each time the card loads, not a stored badge — the actual `user_badges` system is still unused/empty, see above). Non-logged-in users attempting to comment or like are routed to the existing auth modal via a new `onRequireAuth` callback prop (mirrors the `!user → setShowAuthModal(true)` pattern already used for price likes in `App10.jsx`). Sharing: a WhatsApp button (`wa.me` deep link) and a "copy link" button, both pointing at `<origin><path>?product=<id>`; `App10.jsx` now reads that query param on mount and auto-opens the corresponding `ProductDetailModal`, so shared links actually land on the right product. New tables: `product_comments`, `product_comment_likes` (`product_comments_migration.sql`) — public read, authenticated-own-row write, following the same RLS shape as every other user-generated-content table in this schema.

### Jul 21, 2026 — France Hexagonale Price Comparison (flagship diaspora feature)
The core "combat the cost of living" comparison — Martinique price vs. its France Hexagonale equivalent — already had a display mechanism (`PriceDuel.jsx`, wired into `App10.jsx`'s barcode-scan flow via a `mainlandPriceData` fetch on `origin_region_code = 'Hexagone'`), but nothing ever fed it data. Two feed mechanisms were discussed:
1. **French community contributors** submitting their own scans — the existing mechanism already supports this in principle, but `submitPrice`'s store selection (`StoreSelectionWizard.jsx`) is hardcoded to the 78-row Martinique-only `stores` table, so a Hexagone-based contributor has no store to pick from today. **Not built this pass** — flagged as real future work, not something to fake.
2. **Admin-entered reference prices found online** — built this pass, since it's immediately testable and doesn't require reworking the store-selection flow.

- **`mainland_price_migration.sql`**: adds `mainland_chain` (checked against `Carrefour`, `E.Leclerc`, `Système U`, `Auchan`, `Autre`), `source_type` (`'scan'` default vs `'admin_reference'`, so provenance is always honestly labeled wherever a price is shown), and `source_url` (optional, for the admin's online source) to `prices`. No new RLS policy — admin-only reach is enforced at the UI layer, consistent with `ProductCompletion.jsx`'s existing precedent (the `prices`/`products` authenticated-write policies already allow any logged-in user to write these columns via direct API; only the admin tool's own visibility gates who normally would).
- **`MainlandPriceAdmin.jsx`** (new, Console Admin → "Prix France Hexagonale" sub-tab): search a Martinique product, see its current best local price and the live % gap, enter a mainland reference price (amount, chain, optional source link), with existing entries listed (and deletable) below. Inserts directly into `prices` with `store_id: null`, `origin_region_code: 'Hexagone'`, `source_type: 'admin_reference'` — the exact shape `PriceDuel`'s existing fetch already expects, so a newly-added reference price shows up in the barcode-scan "Duel des Prix" with zero changes needed there.
- **`ProductDetailModal.jsx`** also now surfaces this: a `PriceDuel` card (Martinique's best price vs. the cheapest mainland reference) plus a full list of all mainland entries by chain, both isolated into their own query (`loadMainlandPrices`) separate from the core stats query — the new `mainland_chain`/`source_type`/`source_url` columns don't exist until the migration runs, and a shared query would have broken the *entire* already-deployed detail card (stats, chart, store comparison, comments) for every product until then, not just the new section. Verified this isolation live before shipping: the core query succeeds and the mainland query fails cleanly on its own pre-migration.
- **Bug caught before shipping**: `MainlandPriceAdmin.jsx`'s "best local price" calc originally used `.neq('origin_region_code', 'Hexagone')` server-side — Postgres's `<>` excludes `NULL` rows entirely (three-valued logic), and `origin_region_code` is `NULL` for a large share of existing prices. Confirmed live against a real product (1 total price row, `.neq()` returned 0). Fixed to match `ProductDetailModal`'s existing approach: fetch all rows, filter client-side where `!== 'Hexagone'` (which correctly treats `null` as "local").

### Jul 22, 2026 — Mainland Price Admin Redesigned as a Browsable List
Feedback after first hands-on test: requiring a manual search before seeing anything wasn't practical. Redesigned to match `ProductCompletion.jsx`'s pattern instead — a scrollable list of products (deduped from the `prices` feed, one card per product with a photo), each showing its photo, current best Martinique price, and any existing mainland entries as small chips; tapping "+" expands an inline add-price form. Verified live: 17 real products with photos populate the list correctly.

- **Evidence photo upload added**: rather than (or alongside) a plain source-link field, the admin can now attach a screenshot of the French chain's website as proof of the price — same base64→Blob→upload pattern already used for product/price-tag photos in `App10.jsx`'s `submitPrice`. New `evidence_photo_url` column (`mainland_evidence_photo_migration.sql`). Reuses the existing public `price-tag-photos` storage bucket rather than creating a new one, to avoid depending on storage-level permissions that can't be verified from the SQL editor alone.
- **`ProductDetailModal.jsx`** updated to match: each mainland price entry now shows its evidence photo as a clickable thumbnail (own lightweight zoom overlay, `zoomedEvidence` state) alongside the existing source-link.
- Mainland-column loading in the new list is isolated in its own try/catch (same reasoning as `loadMainlandPrices` in `ProductDetailModal.jsx`) — the browsable product list itself doesn't depend on `mainland_price_migration.sql`/`mainland_evidence_photo_migration.sql` having run; only the "existing entries" chips do, and they degrade to empty rather than breaking the list.

### Jul 22, 2026 — Product Card: Explicit 4-Source Price Comparison
Replaced the ad-hoc `PriceDuel` + generic "Prix en France Hexagonale" list in `ProductDetailModal.jsx` with a structured comparison always showing exactly 4 labeled sources, each either a real price + diff badge or an honest "Information manquante" placeholder (never silently hidden):

1. **Martinique (dernier scan)** — the reference every other source is compared against. Computed as the most recent non-Hexagone `prices` row for the product (`latestLocal`), not the cheapest — "dernier scan" (last scan), matching what a shopper would have most recently seen, as opposed to the pre-existing "Meilleur prix" headline stat (which stays, unchanged, as a separate vanity stat).
2. **France Hexagonale — communauté**: most recent entry with `source_type = 'scan'`. Was always empty as of Jul 21 (Option 1 wasn't buildable yet) — **built Jul 22, see below**.
3. **France Hexagonale — capture en ligne**: most recent entry with `source_type = 'admin_reference'`, including its evidence photo as a clickable thumbnail if one was attached.
4. **Autres magasins en Martinique**: every other Martinique store price for this product (`storeComparison`, minus whichever store is source 1), each with its own diff badge.

Diff badges (`abs €` and `%`) are always computed against source 1, colored green when the source is cheaper and red when it's more expensive — verified live against a real 2-store product (Pli Bel Price François 2.10€ as reference vs. Leclerc 1.75€ → correctly shows `-0.35€ (-17%)` in green).

### Jul 22, 2026 — Option 1 Built: France Hexagonale Community Scans
Following the design discussion above, extended the existing scan/manual-entry flow (`App10.jsx`) rather than building a parallel one:

- **Region toggle** added to the "Où êtes-vous ?" gate that starts every scan session: 🇲🇶 Martinique (unchanged — `StoreSelectionWizard`) vs. 🌍 France Hexagonale (new — a plain chain `<select>` using the same `MAINLAND_CHAINS` list as the admin tool, now extracted to `src/constants/mainlandChains.js` so both places can never drift from the DB's `mainland_chain` CHECK constraint). The rest of the form (barcode scan, manual entry, photos) is unchanged either way — only what happens at submission differs.
- **`submitPrice`**: when the Hexagone toggle is active, writes `store_id: null`, `origin_region_code: 'Hexagone'`, `mainland_chain`, `source_type: 'scan'` — deliberately `'scan'`, not `'admin_reference'`, so it's honestly distinguished from an admin-entered reference price everywhere provenance is shown. This is the exact shape source #2 on the product card and the scan-flow "Duel des Prix" were already built to read, so no display-side changes were needed — they just started working once real data existed to feed them.
- **Bug found and fixed while in this code**, not something introduced today: `handleBarcodeDetected`'s fetch of the "local" price for the scan-result `PriceDuel` (`latestPriceData`) had no filter excluding `origin_region_code = 'Hexagone'` at all — it just took the single most recent price row for the product, mainland or not. Confirmed this was **already live and wrong** against the admin's own test data: the Carrefour reference price added for "350G EMMENTAL RAPE" (2.85€, Jul 22) was the most recent row overall, so scanning that barcode would have shown 2.85€ mislabeled as the Martinique price. Fixed using the same verified-correct pattern as the earlier `ProductDetailModal`/`MainlandPriceAdmin` fixes (fetch a few recent rows, filter client-side for `origin_region_code !== 'Hexagone'` — not `.neq()`, which drops `NULL` rows). Also updated `PriceDuel`'s mainland label to show the real chain name (`mainland_chain`) instead of the hardcoded "France Continentale" placeholder.
- No new migration — reuses `mainland_price_migration.sql`'s columns entirely.

### Jul 22, 2026 — Invisible Input Text: `color-scheme` Alone Wasn't Enough
Reported after testing `MainlandPriceAdmin.jsx`: typed text in the price/chain fields was invisible while typing. First pass fixed it globally via `html { color-scheme: light; }` in `src/index.css` (browsers style native form controls per OS dark-mode preference by default, regardless of the page's own light background, when no `color-scheme` is declared). **Reported still broken after retest.** Rather than keep theorizing about which specific browser/OS combination doesn't fully respect `color-scheme` for native controls, applied the definitive fix: added explicit `text-gray-900` directly to the price input and chain `<select>` in `MainlandPriceAdmin.jsx`, and preemptively to the equivalent inputs in `ProductCompletion.jsx` (identical missing-color pattern, not yet reported but equally exposed). Explicit color can't depend on inherited theme behavior the way `color-scheme` does — kept the global CSS fix in place too, as a defensive baseline for every other input in the app that hasn't been individually patched yet.
- Ruled out during investigation: no Tailwind `dark:` variant classes exist anywhere in the codebase (`darkMode` isn't even configured in `tailwind.config.js`), and the admin header's `text-white` styling is a sibling of the tab-content area, not an ancestor — so this wasn't a CSS-inheritance leak from that header. The service worker (`public/sw.js`) also uses network-first for JS/CSS, ruling out stale-cache as the cause.

### Jul 22, 2026 — BQP Section Polish
- Renamed "BQP Watchdog" → "Surveillance BQP" (`BQPVerifier.jsx` header), 🛡️ emoji kept.
- Removed the green "Local 🌴" badge shown per item — will be replaced with something more useful later (not yet specified).

### Jul 22, 2026 — Mainland Entries Were Leaking Into the Comparer Feed; Added At-a-Glance Diff Badge
Reported after adding the first two mainland reference prices: the "Comparer" feed card for the affected product (Emmental) appeared to lose its product photo. Root cause: `loadRecentPrices` (`App10.jsx`) had no filter on `origin_region_code` at all — France Hexagonale entries (`store_id: null`, usually no `product_photo_url` since they're admin-entered from an evidence screenshot, not a real scan) were being pulled into the general feed and rendered as ordinary Martinique scan cards. Since the feed shows one card per `prices` row (not deduped by product), the photo-less mainland row became a new, separate, more-recent card for the same product — the original Martinique card and its photo were never actually touched, but the new card next to/above it made it look that way. Confirmed live: of the 50 most recent rows, 2 were Hexagone entries incorrectly present in what should be an all-Martinique feed.

- **Fixed**: `loadRecentPrices` now excludes `origin_region_code === 'Hexagone'` rows from the feed entirely — mainland/reference prices belong in the product detail card's comparison section (built Jul 21–22), not as regular scan cards here.
- **Added**: each remaining card now shows an at-a-glance diff badge (🌍 `+0.80€ (+28%)` in red, or green if cheaper) against the cheapest known France Hexagonale price for that product, computed in a second, isolated query (`mainlandByProduct`, keyed by `product_id`) so a failure there can't break the main feed. Clicking the card still opens the full detail breakdown (all 4 sources) — this is the "headline number," not a replacement for it. Verified live: Emmental's real data (Martinique 3.65€ vs. Carrefour reference 2.85€) computes to exactly `+0.80€ (+28%)`.

### Jul 22, 2026 — Hunter Detail Card Made Interactive; France Flag Everywhere; Feed Card Spacing
- **`HunterDetailModal.jsx`**: collected-product rows are now clickable, opening `ProductDetailModal` nested on top (same stacking-context pattern as the zoom overlays elsewhere — no z-index conflicts since it's a later-painted descendant of the hunter card's own stacking context). Each row also shows the same at-a-glance France Hexagonale diff badge as the Comparer feed (own isolated query, `mainlandByProduct` keyed by `product_id`). The "Prix collectés" and "Magasins visités" stat tiles are now buttons: the first opens a category-icon picker (only categories actually present among that hunter's items) that filters the list; the second opens a picker of the actual shops they've scanned at, same filtering behavior. Both filters combine and show dismissible chips, mirroring the Comparer tab's existing category/store picker pattern. `onRequireAuth` threaded through `App10.jsx` → `Community.jsx` → `Leaderboard.jsx` → `HunterDetailModal.jsx` → the nested `ProductDetailModal`, so a logged-out visitor trying to comment/like from within someone else's hunter card still reaches the sign-in modal instead of silently no-op'ing. Verified live: Tony's 10 items now include 7 with a real mainland comparison, span 1 shop and 4 categories — filters have real data to work with.
- **Globe icon → French flag**: replaced every `Globe2` (lucide "world") icon that specifically represented France Hexagonale with the 🇫🇷 emoji — the region toggle and "current chain" indicator in the scan flow (`App10.jsx`), the Comparer feed's diff badge, the "Prix France Hexagonale" admin tab button (`AdminDashboard.jsx`), and the intro banner in `MainlandPriceAdmin.jsx`. Left `ProductDetailModal.jsx`'s "Comparaison des prix" section-title icon as `Globe2` — that one labels the overall 4-source comparison (which includes purely-Martinique sources 1 and 4), not France specifically.
- **Comparer feed diff badge now shows the absolute mainland price too**, not just the delta — e.g. `🇫🇷 2.85€ · +0.80€ (+28%)` — so the actual France Hexagonale price is visible without a click, per request.
- **Feed card spacing tightened**: reduced the margin between the product/store-name header row and the photo thumbnails below it (`mb-2`→`mb-1` on the header, `mt-3`→`mt-1` on the photo row) to reclaim vertical space per card.

### Jul 22, 2026 — Feed Card Spacing Tightened Further; "Pas encore dispo" Placeholder
- Removed the remaining margins between the header row and the photo thumbnails entirely (`mb-1`→`mb-0` on the header row, `mt-1`→`mt-0.5` on the photo row; also dropped the now-redundant `mb-1` on the individual price badges, since the right column's own `gap-1` already spaces them) — per feedback, using the France Hexagonale badge's line as the visual reference point for how tight the layout should be.
- **`App10.jsx` Comparer feed + `HunterDetailModal.jsx`**: the France Hexagonale badge now always renders, even with no data — shows `🇫🇷 Pas encore dispo` in a muted gray badge instead of just omitting the badge, consistent with the "information manquante" honesty pattern already used in `ProductDetailModal.jsx`'s 4-source comparison.

### Jul 22, 2026 — Comparer Feed Card: Structural Fix, Not Just Spacing
User inspected the actual DOM and found the real cause of the persistent gap: the product-name/store-name text and the photo thumbnails were siblings in *different* divs — the header row (`flex justify-between`, name+store on the left, price+badges+action-buttons on the right) and the photo row below it. Since that header row's height is set by its *tallest* child, and the right-hand column (price + "Meilleur prix" + France badge + like/save buttons) is taller than the name+store text, the visible gap under "store name" was actually just empty space left by the row being as tall as its right column — no amount of margin-tuning on the photo row could close it, because the photo row wasn't actually adjacent to the store-name text in the DOM.

**Fix**: moved the photo-thumbnails block to live inside the same left-column div as the product name and store name (right after the store `<p>`), rather than as a sibling of the whole header row. The gap between store name and photos is now a direct, adjacent-sibling margin (`mt-0.5`) that can't be inflated by anything in the unrelated right-hand column. The right column (price/badges/buttons) is unaffected — the row's overall height is still whichever column is taller, but that no longer matters for the specific gap being tuned.

### Jul 22, 2026 — Panier Rebuilt: Budget Bar, Favorites Watchlist, Savings Detection, France Comparison
Brainstormed and then built the connection between three pieces of data that were each individually inert: `userFavorites` (write-only — the star toggled and persisted but was never read back anywhere), `userProfile.budget_min`/`budget_max` (captured in the profile form, never consumed), and `ShoppingList.jsx`'s existing cross-store basket comparator (already computed a real per-store total + completeness ranking, but wasn't connected to either of the above). All four pieces below live in `ShoppingList.jsx`, wired to real data and verified against it before shipping:

1. **Budget bar**: shows the recommended-basket total (the comparator's best-ranked store, `comparison[0].totalPrice`) against `userProfile.budget_max`, as a colored progress bar (green under budget, red over) with the exact € remaining or over. The budget itself is editable inline, right in the Panier screen (`updateProfile({ budget_max })`) — prefilled from the profile if already set there, but the point was to let the user set/adjust it as part of building the list, not force a trip to the profile menu first.
2. **Favorites watchlist**: a horizontal scroll of favorited products (photo, best known Martinique price, one-tap "+ Panier" button that calls the same `addToShoppingList` already used everywhere else in the app — passed down as a new `onAddItem` prop) sits above the panier list, so a list can be built directly from favorites as described. `user_favorites` is currently empty in production (nobody has favorited anything yet), so this renders its empty-state prompt for now — verified the query shape is correct, not yet verified with real favorited data.
3. **Savings opportunities**: for the recommended basket, flags any item where a *different* Martinique store has a strictly lower known price than the one being used — "moins cher chez X (Y€ au lieu de Z€)" with a running total. This is a nudge, not an automatic re-optimization across stores.
4. **France Hexagonale comparison**: sums the recommended basket's total against the cheapest known mainland price, *only* over items where both a Martinique and a mainland price are known (never inflates the comparison with unmatched items — states "based on N of M items" explicitly). Verified live against a real 2-item basket (Emmental + 2× Cassoulet): 15.25€ in Martinique vs. 9.55€ equivalent in France Hexagonale → **+5.70€ (+60%)**, a genuinely real number, not a demo placeholder.

**Bug fixed while rebuilding this**: the original comparator's `prices` query had no `origin_region_code` filter at all — the same contamination class already fixed in `App10.jsx`/`ProductDetailModal.jsx`/`HunterDetailModal.jsx`. A France Hexagonale entry for a panier item (`store_id: null`) would have created a phantom "undefined"-named store bucket in the cross-store comparison. Fixed by filtering `origin_region_code !== 'Hexagone'` before building `storeBaskets`, reusing the same fetched rows (split client-side) to also feed the new mainland-comparison feature — one query, two purposes.

## Known Issues & Limitations
None blocking. All migrations to date (`bqp_vote_stats_fix_migration.sql`, `barcode_audit_migration.sql`, `trim_city_cleanup_migration.sql`, `categories_admin_insert_migration.sql`, `product_comments_migration.sql`, `mainland_price_migration.sql`, `mainland_evidence_photo_migration.sql`, `recipes_schema.sql`, `recipes_seed_migration.sql`) — check each file's header comment for apply status as of the date you're reading this; the newest one or two may not yet be applied. `recipes_schema.sql` and `recipes_seed_migration.sql` were applied and verified live on 2026-07-27 (see the "Idées recettes" entry below).
- See "Jul 21, 2026 — Component-Wide Bug Sweep" above for items flagged but intentionally not changed (admin-role RLS uncertainty, unused badges system).
- Category coverage is currently low (6/23 products as of Aug 5, 2026 — see that date's entry below) since assigning a category during capture is optional — the category/store filters and the "Compléter produit" tool will look sparse until more products are categorized. The Panier's "Complétude du panier" tracker (Aug 5, 2026) is now a second surface nudging this up over time, alongside `ProductCompletion.jsx`.
- `product_bqp_associations` has only 1 row — `BQPVerifier.jsx`'s "Produits suivis" header stat will read "1" even though the actual BQP list renders 16 real categories fine (that list comes from `bqp_categories`, not from the association count).

### Jul 26, 2026 — Design Review Follow-Up: P0 Fixes
Read back the `bcd999eb` design-review artifact (2026-07-17) in full and cross-referenced it against everything shipped since — its "highest priority" finding (Leaderboard querying `profiles`) was already fixed on Jul 21, before this follow-up even started. Of its four remaining P0 items, two were code fixes, two are manual demo-day actions (rehearsing on a real phone, choosing what to lead with):

- **Install banner now persists its dismissal**, matching the pattern the welcome overlay already used (`localStorage`). Previously: on iOS, `showInstallPrompt` was set unconditionally on every load (`isIOS() && !isInstalledPWA()`) with no dismissal memory at all — reappeared on every reload regardless of "J'ai compris"/"Plus tard". Added a `dismissInstallPrompt()` helper (writes `install_prompt_dismissed` to `localStorage`) wired to all three dismiss paths (iOS "J'ai compris", Android "Plus tard", and a successful Android install) and checked before auto-showing on mount.
- **Verified Comparer/BQP demo data is in good shape** (the review's other P0 item) — no code changes needed. 20/26 of the most recent real Martinique entries have a product photo, 3 spot-checked photo URLs all return HTTP 200 (not broken links, unlike what the review found in its test environment). `bqp_categories` has 16 real rows driving the visible BQP list. One minor, non-blocking caveat noted above (`product_bqp_associations` header stat).

### Jul 26, 2026 — Design Review Follow-Up: P1 Items
- **Motion was completely dead app-wide — root cause, not a missing polish pass.** `tailwindcss-animate` was never installed (`plugins: []` in `tailwind.config.js`), so every `animate-in`/`fade-in`/`zoom-in-*`/`slide-in-from-*` class used throughout the entire codebase — in every modal, picker, and card built across this whole project, not just today's work — generated zero CSS and did nothing. Confirmed before and after in the built output: no `@keyframes enter`/`.animate-in{...}` rule existed pre-fix; both exist post-fix. Installed `tailwindcss-animate` as a dev dependency and registered it in `plugins`. This retroactively activates motion on every existing surface at once (all modals' entrance animations, the welcome overlay, every zoom/picker overlay) — far higher leverage than adding more of the same previously-inert classes would have been.
- **Store-selection wizard step labels ("Ville"/"Enseigne"/"Magasin") were already coded but permanently invisible** — same root-cause class as the motion bug: `hidden xs:block`, and Tailwind has no `xs` breakpoint configured (`theme.extend.screens` was never set). Confirmed no `.xs\:block` rule existed in the built CSS. Considered adding an `xs` breakpoint instead, but the review's own test viewport (390px) is narrower than any sensible `xs` threshold (e.g. 475px) would be — that fix wouldn't have shown the labels on the exact device the review used. Removed the guard entirely instead; the labels are short single words at `text-xs` and don't need hiding on any realistic phone width.
- **Price typography**: added `tabular-nums` (and bumped the Comparer feed's headline price to `text-3xl font-black`, from `text-2xl font-bold`) across every prominent € display — Comparer cards, `ProductDetailModal`'s stats/comparison rows, `HunterDetailModal`'s item prices, `ShoppingList`'s budget bar and France Hexagonale comparison totals.
- **Welcome overlay**: replaced the four emoji (📷💶📊🏆) with the same Lucide icon set used everywhere else in the app (`Camera`, `Euro`, `BarChart3`, `Trophy`, each in a small orange circle), so the app's best screen doesn't visually break from its own icon language depending on the OS's emoji font.
- **Not built this pass**: formalizing a second/third brand color (Martinique orange / France blue / BQP institutional blue) — this was my own addition when synthesizing the plan, not one of the review's own P1 punch-list items, and doing it properly risks a half-applied, inconsistent re-theme if rushed in alongside these bounded fixes. Worth a dedicated pass if wanted, not bundled here.

### Jul 26, 2026 — Design Review Follow-Up: P2 Items
The review's own P2 list was three items (second brand color, dark mode, micro-interactions); my synthesized plan added two more (Option 1, badges). Corrected one planning mistake and worked through the rest:

- **Correction: Option 1 (French community mainland scans) was already built on Jul 22** — I mistakenly re-listed it as an open P2 item when writing the plan. No work needed; see that date's entry above.
- **Badges system wired up** (was fully dead: built UI in `PersoStats.jsx`, 7 real badge definitions already seeded in `badges` with sensible `points_required` thresholds, but nothing anywhere ever inserted into `user_badges` — confirmed live, 0 rows). Rather than award client-side (would've needed a new RLS insert policy — `user_badges` only had a public SELECT policy), extended the existing `award_points()` RPC — already `SECURITY DEFINER`, already the sole place points get written — to award any newly-qualifying badges in the same atomic call (`badge_awarding_migration.sql`). Included an explicit `set search_path = public` to preserve the Feb 28, 2026 security-audit fix regardless of whether `CREATE OR REPLACE FUNCTION` would otherwise carry it forward — didn't want to risk silently reintroducing a fixed vulnerability while touching this function again. Migration also includes a one-time backfill so existing contributors aren't stuck at zero. Verified live before shipping: Tony (100 pts) → Débutant/Contributeur/Photographe; Jean-Marie (50 pts) → Débutant/Contributeur.
- **Brand color: gave BQP its own distinct hue instead of formalizing a full palette.** Investigating the review's "give BQP blue a deliberate role" note surfaced a concrete, unintended collision: `BQPVerifier.jsx`'s "institutional blue" and every France Hexagonale comparison surface (`ProductDetailModal`, the Comparer feed badge, `MainlandPriceAdmin`, the admin tab) were using the *exact same* `blue-*` Tailwind shade by accident — two unrelated meanings (an official government price-shield program vs. a France/Martinique cost comparison) sharing one color. Shifted BQP specifically to `indigo-*` (header, focus rings, filter chips, hover states) rather than touching the much more widely-used France-blue, which stays as the established convention everywhere else. Scoped to this one real problem rather than a full three-color system pass, which risks being half-applied if rushed.
- **Micro-interactions**: added `active:scale-*` press feedback (plus `transition-colors` → `transition-all` where needed so the scale transform actually animates) to the highest-traffic interactive surfaces — all 6 main nav tabs, the primary "Enregistrer le prix" and welcome-overlay "Commencer !" CTAs, and the clickable rows in the Comparer feed, `Leaderboard`, and `HunterDetailModal`. Not an exhaustive app-wide pass (the review itself scoped this as "not for this call — next milestone"); scoped to the paths most likely to be seen live.
- **Dark mode — explicitly not built, recommend staying rejected rather than revisiting.** This app was deliberately locked to `color-scheme: light` on Jul 22 after it caused a real, reported bug (invisible input text on a device with OS dark mode enabled). Building real dark mode later means carefully reintroducing `color-scheme: light dark` plus a full second color pass across every component *without* reopening that exact bug — a project of its own, not a P2 line item to fit in alongside these fixes.

### Jul 26, 2026 — PostHog Product Analytics Instrumented
Added `posthog-js` and a minimal `src/posthogClient.js` (inits from `VITE_POSTHOG_KEY`/`VITE_POSTHOG_HOST`, `person_profiles: 'always'` so an anonymous user's profile exists before `identify()` merges it on signup). Events wired in so far:

- **`App10.jsx`**: `barcode_detected`, `product_matched`, `scan_session_started`, `price_submitted` (both the manual-entry and quick-confirm paths, with `submission_method` distinguishing them), `first_contribution_completed` (fires once, gated on a `localStorage` flag).
- **`AuthContext.jsx`**: `posthog.identify()` on session restore and OAuth sign-in; `anon_to_signup_converted` for both email signup and Google OAuth (Google needs a heuristic — `isLikelyNewOAuthSignup` — since Supabase doesn't flag "this sign-in was a brand-new account" directly; approximated as `created_at`/`last_sign_in_at` within 15s of each other); `posthog.reset()` on sign-out.
- **`StoreSelectionWizard.jsx`**: `store_wizard_step_viewed` (per step), `store_wizard_completed`, `store_wizard_gps_detected`.

**Verified live before shipping** (this class of silent-failure bug — instrumentation that looks right but never actually sends — is exactly what's bitten this project before, see the Jul 21 bug-sweep entries above): ran the app against the real dev server, drove the store-selection wizard in a real browser, then confirmed via a direct PostHog SQL query against the raw `events` table (not just the client-side network log) that `$pageview` and `store_wizard_step_viewed` (steps "city" and "chain") landed within seconds, tied to a real `distinct_id`.

**`vercel.json` created** (didn't exist before): besides the PostHog vars, formalizes `framework`/`buildCommand`/`outputDirectory`, COEP/COOP headers, and separate production/preview env blocks for the Supabase vars (referenced via Vercel secret refs, `@vite_supabase_..._production` / `..._staging`). `VITE_POSTHOG_KEY`/`VITE_POSTHOG_HOST` are set directly as literal values instead of secret refs, deliberately — PostHog's project key is a public, write-only client token meant to ship in the browser bundle (unlike treating it as a secret, which would just add a manual Vercel-dashboard step for no security benefit).

**Broke the build** — see "Jul 27, 2026 — vercel.json Build Failure Fixed" below; the "separate production/preview env blocks" described above aren't a real Vercel construct.

### Jul 27, 2026 — vercel.json Build Failure Fixed
Deployment following the PostHog commit above (`bc63751`) failed schema validation: `should NOT have additional property 'preview'`. Root cause: `vercel.json`'s top-level `"preview": { "env": {...} }` block was invented when the file was first authored the day before — Vercel's `vercel.json` schema has no per-environment (`preview`/`production`) sub-block for scoping env vars; the top-level `env` key applies to every environment uniformly. Per-environment env var scoping is only configurable in the Vercel Dashboard (Project Settings → Environment Variables, each var individually scoped to Production/Preview/Development) or via `vercel env add`, not in JSON.

**Fix**: removed the `preview` block from `vercel.json` (`5de9f0f`). The staging Supabase secret refs (`@vite_supabase_url_staging`, `@vite_supabase_anon_key_staging`) it referenced are no longer wired to anything — if Preview deployments should still use the staging Supabase project, that split needs to be set up manually in the Vercel Dashboard.

**Second, deeper build blocker found the same day, after 5 more commits had piled up with zero deployment attempts**: opening the Vercel dashboard (Deployments tab) showed the *last deployment attempt of any kind* was still `bc63751` — Error, 16h stale — meaning **no push since the `preview`-key fix had triggered a build at all**, auto-deploy-on-push wasn't firing for reasons never root-caused (Git integration itself looked fine: repo connected, correct Production branch, no "Ignored Build Step" or "skip if no changes" misconfiguration). Manually invoking Vercel's "Create Deployment" for `main` surfaced the *actual* next blocker before even starting a build: `Environment Variable "VITE_SUPABASE_URL" references Secret "vite_supabase_url_production", which does not exist`. Root cause: `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` were already configured as ordinary Vercel Dashboard → Environment Variables (added Jan 11, scoped to All Environments) — but `vercel.json`'s `env` block (added Jul 26 alongside the PostHog work) referenced them via the **legacy** `@secret_name` syntax, which requires an old CLI-created (`vercel secrets add`) Secret object with that exact name. No such legacy secret was ever created, so the reference was dangling from the moment `vercel.json` was first authored — this was silently masking the *original* `preview`-key schema error the whole time, since schema validation happens before env-var resolution.

**Fix**: removed the `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` entries from `vercel.json`'s `env` block entirely — they're redundant with (and were actively conflicting with) the dashboard-configured Environment Variables, which Vercel injects into the build automatically with no `vercel.json` entry needed. Kept `VITE_POSTHOG_KEY`/`VITE_POSTHOG_HOST` as literals, unchanged. Manually triggered a Vercel "Create Deployment" for `main` afterward to actually get the 6-commit backlog live, since auto-deploy-on-push still hasn't been diagnosed — **worth checking after the next ordinary push whether it starts auto-deploying again on its own; if not, this needs a closer look at the GitHub App/webhook side, which isn't inspectable from the Vercel dashboard alone.**

### Jul 27, 2026 — "Idées recettes" Feature, Phase 1 (Admin-Curated Recipes → Shopping List)
First piece of a longer-term gamification arc discussed with Jean-Marie: let users browse a curated recipe, see its ingredient list priced against real Martinique data, and one-tap the whole thing into their Panier — aimed at turning the app from a one-off price-check tool into something used for recurring weekly meal planning. Phase 1 is admin-curated only; phase 2 (open community recipe submission) and phase 3 (nutriscore-style healthiness rating, explicitly flagged as "just a thought" — not designed) are intentionally out of scope, but the schema leaves room for both without a future migration.

- **New tables** (`recipes_schema.sql`): `recipes` (name, description, photo_url, servings, prep_time_minutes, category as free text — deliberately not FK'd to the existing `categories` table, which models product categories, a different taxonomy — difficulty, is_active for hide-without-delete, plus `created_by`/`is_community_submitted` sitting unused for phase 2), `recipe_ingredients` (ingredient_name always present regardless of a product match, nullable `product_id`, quantity/unit as free text, display_order, notes), `recipe_cooked_log` (gamification hook, `unique(user_id, recipe_id, cooked_on)` caps points to once per calendar day per recipe while still letting a recurring weekly cook re-earn points — not a once-ever cap). RLS follows the established two patterns in this codebase: public read + admin-gated write (same `exists (select 1 from user_roles where user_id = (select auth.uid()) and role = 'admin')` form as `categories_admin_insert_migration.sql`) on `recipes`/`recipe_ingredients`, and public read + own-row insert (same shape as `product_comments`) on `recipe_cooked_log`.
- **Seed data** (`recipes_seed_migration.sql`): 10 iconic Martinique dishes (Colombo de poulet, Court-bouillon de poisson, Accras de morue, Féroce d'avocat, Matoutou de crabe, Blaff de poisson, Ragoût de porc, Riz et pois rouges, Gratin de christophine, Fricassée de chatrou). Ingredient-to-product matching was checked against the *live* `products` table (queried via the Supabase REST API with the anon key, since RLS allows public read) rather than invented — of the 22 real products in the catalog as of this writing (mostly packaged/test items, see the existing category-coverage note below), only 2 honest matches existed: `350G EMMENTAL RAPE` → "Emmental râpé" (Gratin de christophine), and `Huile tournesol - marque distributeur` → "Huile"/"Huile de friture" (Féroce d'avocat, Accras de morue). Every other ingredient (fresh produce, meat, fish, spices) is left `product_id = null` with a `-- TODO` comment rather than forced into a wrong match — expected given the current data-completeness gap, not a bug to fix here.
- **`RecipeDetailModal.jsx`** (new): mirrors `ProductDetailModal.jsx`'s conventions (same `z-[300]` overlay chrome, WhatsApp/copy-link share via `?recipe=<id>`, `onRequireAuth` guard pattern). Computes an estimated recipe cost by batch-fetching prices for all matched `product_id`s and taking the cheapest per product — reusing the exact "never `.neq('origin_region_code', 'Hexagone')` server-side, filter client-side instead" pattern already fixed three times elsewhere in this codebase (NULL is the normal case for Martinique rows, and Postgres's `<>` silently drops NULL rows). Shows an honest "N sur M ingrédients avec prix connu" line even when N is 0. "Ajouter tous les ingrédients" adds each matched ingredient at quantity 1 (not a unit-conversion of the recipe's real quantity — there's no `sold_by`/`unit_size` field on `products` to convert "500 g" into a shopping-list unit count, and inventing a rounding heuristic would silently misrepresent most ingredients; the Panier's existing `+`/`-` stepper covers the gap, and this is called out as a caption in the UI).
- **"J'ai cuisiné cette recette ✅" gamification hook**: inserts into `recipe_cooked_log`, then calls the existing `awardPoints('recipe_cooked', 15, ...)` → `award_points` RPC unchanged. Deliberately a new, distinct activity-type string rather than reusing `'price_submission'` (which the Jul 21 bug-sweep already found gets reused for both price submissions and BQP verification, undermining any future per-activity analysis) — `recipe_cooked` stays honestly distinguishable in the data. **Known limitation, not solved here**: `award_points()`'s badge-awarding is purely cumulative-points-threshold based (see `badge_awarding_migration.sql`), with no per-activity-type branching — a hypothetical "5 recettes cuisinées" badge isn't achievable through this RPC alone; would need a new RPC/trigger keyed off `recipe_cooked_log` counts.
- **`RecipeAdmin.jsx`** (new, Console Admin → "Recettes" sub-tab, wired into `AdminDashboard.jsx` alongside `ProductCompletion.jsx`/`MainlandPriceAdmin.jsx`): full recipe CRUD — name/description/category/difficulty/servings/prep-time/photo-URL form, plus a per-ingredient product-search-and-associate flow (`products.name ilike`) so future recipes don't require a developer to hand-write SQL. Ingredient saves are delete-all-then-reinsert per recipe rather than a real diff (no multi-statement transaction primitive available via supabase-js without a custom RPC — matches the general lack of transactions elsewhere in this codebase).
- **`ShoppingList.jsx`**: new "Idées recettes" horizontal-scroll section, inserted directly after the existing "Mes Favoris" block and copying its exact card chrome/dedup-check conventions (`items.some(i => i.productId === ...)`), loaded independently of `items` (unlike the price comparator further down, which only makes sense once the panier has something in it) so recipes are always browsable even from an empty cart.
- **`App10.jsx`**: `?recipe=<id>` deep link mirrors the existing `?product=<id>` `useEffect` exactly; `RecipeDetailModal` is rendered at the top `App10.jsx` level (like `ProductDetailModal`, not threaded through `Community → Leaderboard → HunterDetailModal`) specifically so the deep link works from any tab, not just Panier — this also means `onRequireAuth` reaches it directly without needing to be threaded into `ShoppingList.jsx` at all (the only auth-gated action, "cooked," lives inside the modal; adding ingredients to the cart works for anonymous users too, same as the rest of the shopping list).
- **Verified before shipping**: `npm run build` and `npx eslint` clean (the only lint findings are 14 pre-existing issues in `App10.jsx`/`AdminDashboard.jsx`, confirmed identical via `git stash` against the pre-change baseline — nothing newly introduced). Drove the real dev server in a browser: the new "Idées recettes" section renders in the correct position, and the pre-existing `ShoppingList.jsx` sections (budget bar, favorites, comparator) are unaffected — confirming the new recipes-loading `useEffect` is properly isolated (own try/catch, same convention as `loadFavorites`).
- **Migrations applied, 2026-07-27**: `recipes_schema.sql` then `recipes_seed_migration.sql` were run manually via the Supabase Dashboard → SQL Editor (Jean-Marie ran them; DDL requires privileges the anon key doesn't have, so this couldn't be scripted). Verified directly against the live REST API afterward: all 10 recipes present with correct categories, 60 `recipe_ingredients` rows total including all 3 designed product matches, `recipe_cooked_log` reachable and empty, and RLS correctly rejects an anonymous insert into `recipes` (`401`). The "Idées recettes" section in the Panier tab now shows real data instead of its empty state.
- **Recipe photos, 2026-07-27**: deliberately did *not* hotlink stock-photo URLs found on the web for the "no photo" chef-hat placeholder — no way to verify licensing per image, and a third-party link can vanish or get blocked anytime. Instead: `public/recipes-pictures/` (see its `README.md` for the exact filename per recipe) holds real photos Jean-Marie supplies directly, git-committed and deployed with the app like any other static asset (no Supabase Storage upload step needed, unlike product/price-tag photos). `recipes_photo_urls_migration.sql` points each recipe's `photo_url` at `/recipes-pictures/<file>`.
  - **Filenames adjusted same day** once the real files landed: the actual images use underscores rather than the originally-templated hyphens, and `colombo_de_poulet`/`court-bouillon_de_poisson` are `.webp` (everything else `.jpg`) — updated both the migration and the README to match what's actually in the folder rather than asking for a rename. Still **not yet applied** as of this writing — run `recipes_photo_urls_migration.sql` in the Supabase SQL Editor to go live. Aside: `ragout_de_porc.jpg` is ~2MB, notably larger than the other images (30–150KB) — not blocking, but worth compressing at some point for load time.

### Jul 27, 2026 — Scan Confirmation: Celebration Overlay + Next-Step Choice
The plain bottom toast after a successful price submission ("Prix enregistré avec succès!") gave no sense of accomplishment and no obvious next action, so a rapid-scan session had no natural pause point. Added a centered celebration overlay (`App10.jsx`, `scanCelebration` state) that appears in addition to the existing toast (toast kept as-is for its `aria-live` accessibility wiring) on both submission paths — the manual-entry form (`submitPrice`) and the barcode-scan "quick confirm" (`C'est le même`) — showing a `PartyPopper` icon, the product name, and the points earned (or a thank-you line for anonymous contributions), plus two choices: **"Scanner un autre article"** (dismisses the overlay and scrolls to top — no form-reset logic needed since `submitPrice`'s existing reset already keeps `storeId`/`isMainland`/`mainlandChain`/`userName` persistent, exactly the "same shop" behavior requested) and **"Voir mes articles scannés"** (dismisses and switches to the Comparer tab, `setActiveTab('search')`). Auto-dismisses after 9s if ignored, equivalent to "scan another" since the form's already reset either way. Reuses the exact `fixed inset-0 ... z-[300] ... animate-in zoom-in-95` overlay chrome already proven correct in `ProductDetailModal.jsx`/`RecipeDetailModal.jsx`. Both choice buttons capture `posthog.capture('scan_celebration_choice', { choice })`, per the PostHog instrumentation convention above.
- **Verified**: `npm run build` and lint clean (same 13 pre-existing findings, nothing new). Could **not** complete a full live click-through test: submitting a price as an anonymous (not-logged-in) user hit a pre-existing, unrelated `401` — `products`/`prices` RLS requires the `authenticated` role (per the Feb 28, 2026 audit), yet `submitPrice`'s own code still branches on a "Merci pour votre contribution!" message for the anonymous case, implying anonymous submission was assumed to work. **Flagged, not fixed here** — worth a look, since it means every "anonymous contributor" path in the UI (a real, advertised feature) may currently be dead on arrival. Deliberately didn't sign in to complete the test either, to avoid writing throwaway test data into a real account's points/leaderboard history on the live-pointed dev environment — this overlay's correctness rests on code review + build/lint + the proven overlay pattern reuse, not an end-to-end click-through. Worth a quick manual check logged in as a real account before considering this fully shipped.
- **Redirect copy clarified, same day, after feedback**: renamed the two choice buttons so it's unambiguous what each does — "Scanner un autre article" → **"Continuer à scanner"**, "Voir mes articles scannés" → **"Voir les prix et détails enregistrés"** (explicitly names both prices and item details, matching what the Comparer tab actually shows). Added a "Que souhaitez-vous faire ?" lead-in line above the two buttons.

### Jul 27, 2026 — Submission Flow Review: BQP Stale-Product Bug, Field Reorder
Asked to review the whole submission flow end-to-end after a reported "glitch" selecting BQP, plus whether the field order made sense. Traced the actual bug rather than guessing:

- **Confirmed bug — BQP category picker can silently link the wrong product, or silently no-op.** `scannedProduct` (the state `handleBqpSelect` uses to know which product to attach a BQP category to) is only ever set in two places: when a barcode scan matches an *existing* product, or right after `submitPrice` creates/finds a *new* one — and it is **never cleared** in between. Two broken paths both stemmed from this:
  1. **Manual-entry form's own "Produit BQP ?" toggle** → "Choisir la catégorie BQP..." sub-button, shown *before* the price is even submitted. For a genuinely new product (nothing scanned yet this session), `scannedProduct` is `null`, so `handleBqpSelect`'s `if (!scannedProduct) return;` guard bails silently — the picker either does nothing or (if the user had scanned something *else* earlier in the session) links the newly-chosen BQP category to that **stale, unrelated product** instead. This same button's label also read `manualEntry.categoryId` (the *product* category grid) instead of the actual BQP category, so even a successful link would never reflect in the button text.
  2. **Scanning a brand-new/unrecognized barcode**: `handleBarcodeDetected` sets `bqpCheckResult = { status: 'new_product', barcode: code }` — no `.product`, since nothing exists in the DB yet — but the UI showed the same "Lier à une catégorie BQP" button as the working `not_found` case (an *existing*, unlinked product), inviting the same stale-`scannedProduct` mislink.
  - **Fix**: removed the broken pre-submit sub-button entirely (the toggle itself still works fine — `is_declared_bqp` saves correctly on submit — it just now shows an explanatory caption: "Enregistrez d'abord le prix — vous pourrez choisir la catégorie BQP juste après"). The post-scan "Lier à une catégorie BQP" button is now gated on `bqpCheckResult.product` actually existing; a genuinely new/unrecognized barcode instead shows a "Nouveau produit !" message pointing at the form below. The **only** BQP-linking entry point left is the one that was already correct: the automatic prompt that fires right after `submitPrice` confirms the product row exists.
- **Field order changed**: moved "Prix (EUR)" to right after the photo-upload section (was previously after Catégorie/Local/MDD, near the bottom). Reasoning from feedback: the price is the one thing already in front of the user — on the shelf, and in the price-tag photo they just took — so capturing it immediately after the photos matches how the scan actually happens in-store, rather than making it the 7th field. Pure reorder, no logic changes; `submitPrice`'s existing shop-persistence-on-reset behavior (`storeId` etc. kept across submissions) was already correct and needed no change.
- **Verified live** (dev server, fresh browser tab — a stale tab left over from before a dev-server restart threw an unrelated `Cannot read properties of null (reading 'useState')` React error, purely an HMR/module-cache artifact, not a real bug; resolved by using a new tab): confirmed Prix now renders immediately after the photo section, and toggling "Produit BQP ?" shows the new caption instead of the removed button.
- **Full authenticated round-trip verified live, 2026-07-27, logged in as jm.philocles@gmail.com (real admin account)**: submitted several real test prices (`TEST QA Celebration Overlay 1-6`) end to end. Confirmed: points incremented correctly per submission (+10 each, visible in the header badge); the post-submit "Produit non classé BQP → Lier à une catégorie BQP" prompt appeared and, on selecting a category, correctly showed "Produit BQP Vérifié !" — verified via a direct REST query against `product_bqp_associations` that the newest row links the *just-submitted* test product (not a stale one from an earlier scan), confirming the stale-`scannedProduct` bug is actually fixed, not just gated differently. Also confirmed the celebration overlay renders correctly with real data (product name, points, both choice buttons) and that "Voir les prix et détails enregistrés" correctly switches to the Comparer tab (`comparerActive: true` via direct DOM check) — an earlier attempt at this same check via manual coordinate-clicking appeared to fail, but that turned out to be a testing-tool artifact (CDP screenshot latency in this environment outrunning the 9s auto-dismiss / coordinate mistiming), not a real bug — confirmed by re-testing with a script that clicks and checks state atomically in-page.
- **Test data left behind**: the 6 `TEST QA Celebration Overlay N` products/prices (and one `product_bqp_associations` row) created during this live test are real rows in production. `qa_cleanup_2026-07-27.sql` removes them. It does not reverse the +60 points awarded to jm.philocles@gmail.com during testing (see that file's header comment for the manual follow-up if wanted).
- **Applied and verified live, Jul 28, 2026**: `qa_cleanup_2026-07-27.sql` run in the Supabase SQL Editor. Confirmed via direct query: zero rows remain across `products`/`prices`/`product_bqp_associations` matching `TEST QA Celebration Overlay%`, and the shopping-cart item that had referenced one of these products cascaded away cleanly (no orphaned `shopping_list_items` rows) — cart correctly down to its 10 real items.

### Jul 27, 2026 — Store Wizard: City List Duplicates Fixed, "(Code Postal)" Format
Reported after testing store selection: cities appeared in inconsistent formats, splitting the same real city's stores across separate list entries. Root cause traced, not guessed: `getCityList()` (`src/utils/geocoding.js`) dedupes `stores.city` values with a plain `Set` — correct logic — but **7 of the 78 stores (ids 1-7, the original seed data) have `city` values combining a postal code + shortened name** ("97200 Fort-de-France", "97231 Robert", "97232 Lamentin") while every other store uses the clean canonical name ("Fort-de-France", "Le Robert", "Le Lamentin") already used everywhere else in the app (matching the pre-existing `MARTINIQUE_CITIES` GPS-detection list in the same file). Same city, two different strings → never deduped, and the naming itself differed too ("Robert" vs the correct "Le Robert").

- **Data fix**: `stores_city_normalization_migration.sql` — 5 `UPDATE` statements correcting those 7 rows to the canonical name already used by the other 71. Includes a one-line sanity-check query (should return zero rows once applied) — see the "Applied and verified" entry below.
- **Postal codes added**: extended the existing `MARTINIQUE_CITIES` array (rather than a parallel lookup table) with a `postalCode` field per city, and added `getPostalCode(cityName)`. **These postal codes are from general knowledge of Martinique's commune postal system, not sourced from an authoritative dataset — worth a spot-check before fully trusting them.** Tartane and Morne-des-Esses are localities (not communes in their own right), so they carry their parent commune's code (La Trinité / Sainte-Marie respectively) — that's how French addressing already treats them, not an approximation.
- **Display**: `StoreSelectionWizard.jsx`'s Step 1 city list now renders `Ville (code postal)`, sorted alphabetically (already was, via `getCityList()`'s existing `localeCompare(..., 'fr')` — no sort-logic change needed, only the fix to what's being sorted). Falls back to just the city name if `getPostalCode` doesn't recognize it (defensive, e.g. a future store added with a city outside the known list), rather than showing a broken "(null)".
- **Verified live** (dev server): confirmed the new format renders correctly for already-clean cities ("La Trinité (97220)", "Le Carbet (97221)", etc.), all alphabetically sorted.
- **Applied and verified, Jul 28, 2026**: `stores_city_normalization_migration.sql` run in the Supabase SQL Editor. Confirmed live via the REST API: zero rows now match the old `^[0-9]{5} ` format, and the 5 previously-split cities merged to the expected combined counts (Fort-de-France 12, Le Lamentin 6, Ducos 3, Schoelcher 3, Le Robert 2). Total distinct `stores.city` values is now exactly **28** — matching `MARTINIQUE_CITIES`'s length exactly, confirming no duplicates or stray formats remain anywhere in the table, not just the 5 known cases.

### Jul 27, 2026 — Auth Emails Translated to French (Supabase Dashboard, not code)
Reported: signup-confirmation and password-reset emails were arriving in English, Supabase Auth's default language, even though the app is 100% French-facing. Not a code fix — `AuthContext.jsx`'s `signUp()`/`resetPasswordForEmail()` just call Supabase's built-in auth flows; the email *content* is entirely configured in Supabase Dashboard → Authentication → Email Templates, with nothing representing it in this repo (no `supabase/` CLI config here, same as every other Supabase-dashboard-only setting in this project).

- **Cleared up a misread along the way**: the dashboard shows an amber "Set up custom SMTP" banner ("built-in email service... not meant to be used for production apps") sitting directly above the template list, which reads like it could be gating customization. Checked directly — it doesn't. That banner is purely about *deliverability* (the free built-in relay has strict rate limits, a handful of emails/hour), a separate concern from *template content*, which was fully editable (Subject field + HTML body editor with Source/Preview, Save/Reset) regardless of SMTP setup.
- **Translated and saved, logged in as jm.philocles@gmail.com (admin)**: the two templates this app actually triggers — **"Confirm sign up"** (subject "Confirmez votre inscription — Vie chère en Martinique") and **"Reset password"** (subject "Réinitialisation de votre mot de passe — Vie chère en Martinique") — both with French HTML bodies, `{{ .ConfirmationURL }}` left intact. Confirmed via the dashboard's "Successfully updated email template" toast after each save. **"Invite user", "Magic link or OTP", "Change email address", and "Reauthentication" were left in English** — the app doesn't trigger any of those flows today, so there was nothing real to verify against; translate them if/when one of those flows gets used.
- **Not addressed, flagged only**: the built-in relay's production rate-limit ceiling (the SMTP banner's actual concern) — separate from the language ask, not urgent unless signup volume grows enough to hit it. Would need a provider like Resend/SendGrid/Postmark wired in via "Set up SMTP" if that becomes a real problem.

### Jul 28, 2026 — Feature Request Comments + Admin Reply (Communauté "Améliorations")
Asked to review how the admin is notified of new "Développement Participatif" submissions and whether an admin-dashboard messaging feature made sense. Investigation (background Explore agent) found: **no notification mechanism existed at all** — a submission was a bare `INSERT` into `feature_requests`, no email/webhook/trigger/Edge Function anywhere in the repo — and **no admin review UI existed either**; `AdminDashboard.jsx` had zero references to feature requests. Recommended a lightweight middle ground over a full two-way chat system: fix the broken admin update path and add a minimal admin reply tool, since `admin_comment` already existed as a schema column with nowhere to write it from. User agreed, and asked to additionally open up public comments on each suggestion (on top of the existing up/down vote), with the admin's reply from the dashboard carrying visible "official" weight.

- **Root cause found while wiring the admin tool**: the existing `"Admins can update all feature requests"` RLS policy checked a `profiles` table that doesn't exist in this project (same bug class as the `Leaderboard.jsx` fix from the Feb 28, 2026 audit) — it never matched any real admin, so every admin status/reply update would have silently failed RLS (42501) even before this feature existed. Also found `feature_request_stats` (the view `Community.jsx` reads) never actually exposed `description`/`category`/`created_at`/`admin_comment` — those fields were rendering `undefined`/`Invalid Date` even though the component code was already trying to read them.
- **`feature_request_comments_migration.sql`** (new, **applied and verified live 2026-07-28**): adds `feature_request_comments` (public read, any logged-in user can insert their own — same append-only pattern as `product_comments`), recreates the broken admin policy using the standard `user_roles` + `(select auth.uid())` check used everywhere else in this codebase, and recreates `feature_request_stats` to expose the missing columns plus a `comment_count` subquery. No `SECURITY DEFINER` on the recreated view, per the earlier lint-fix convention.
- **`FeatureRequestDetailModal.jsx`** (new): tapping a suggestion card (previously inert except for the vote buttons) now opens a detail view — full description, vote buttons, a distinct blue "Réponse officielle de l'équipe" box when `admin_comment` is set, and a chronological comment thread with its own composer. Author names resolved via `user_profiles.display_name`, same lookup pattern as `ProductDetailModal.jsx`'s comments. **Bonus fix bundled in**: `feature.userVote` (the "already voted" highlight) was dead code before — `feature_request_stats` has no per-user context and nothing populated it — now fixed by merging a separate `feature_votes` query keyed to the current user, in both the card list (`Community.jsx`) and this modal.
- **`FeatureRequestAdmin.jsx`** (new) + **`AdminDashboard.jsx`**: new "Suggestions" sub-tab, sorted newest-first with vote/comment counts visible per row (since there's still no push notification — an admin has to actually open this tab to see new ones, which is the accepted tradeoff of the lightweight approach over a full notification system). Expand a row to set `status` and write `admin_comment`; saving updates `feature_requests` directly, which is exactly what the RLS fix above unblocks.
- **Verified live** (dev server, logged in as jm.philocles@gmail.com admin — note: had to unregister a stale PWA service worker caching an old JS bundle from a previous session before the dev server's fresh code would actually load, not a code bug): created a real `"TEST QA Feature Comments"` suggestion, posted a comment (rendered with correct author name + count), upvoted it (highlight lit up correctly, confirming the userVote fix), then went to Console Admin → Suggestions, wrote an official reply, and saved. **Confirmed via a direct `feature_requests` query (not just "no error shown")** that `admin_comment` and `updated_at` actually changed — proving the RLS fix genuinely works, not just that the UI stopped complaining. Reopened the suggestion on the public side and confirmed the "Réponse officielle de l'équipe" box renders the reply correctly.
- **Test data left behind**: the `TEST QA Feature Comments` row (+ its one vote, one comment) is real data in production. `qa_cleanup_2026-07-28.sql` (new file, **not yet applied**) removes it — none of `feature_requests`/`feature_votes`/`feature_request_comments` have a DELETE policy (by design, append-only), so this can only be cleaned up via the SQL Editor, not from the app.
- **Not built**: true two-way threaded messaging (admin reply visible only as a single most-recent `admin_comment`, not a reply-per-comment chat) — deliberate scope choice per the agreed lightweight approach; revisit if one official reply per suggestion turns out to be too limiting.
- **Follow-up fix, same day**: reported that on mobile, part of the Console Admin sub-tab menu was unreachable — adding the "Suggestions" tab made it the 5th item in a plain `flex gap-2` row with no overflow handling, so on a narrow viewport the tabs past what fit were clipped by the fixed-width header with no way to reach them (pre-existing latent bug, just never had enough tabs to surface it before). Fixed in `AdminDashboard.jsx` by making the sub-tabs row horizontally scrollable (`overflow-x-auto no-scrollbar`, `flex-shrink-0 whitespace-nowrap` per button, `-mx-6 px-6` to bleed edge-to-edge under the header's padding) — same pattern already used for the horizontal-scroll rows in `ShoppingList.jsx`. Verified by constraining the nav to 350px width in a live DOM test: confirmed `scrollWidth` (740px) exceeds `clientWidth`, and scrolling the container reveals and makes clickable the previously-unreachable tabs (Recettes, Suggestions).

### Jul 28, 2026 — Communauté "Impact" Tab Reviewed: Fake Trend Badge Removed, "Locaux" Undercount Fixed
Asked to explain how the Score de Souveraineté works and suggest improvements. Traced `loadCommunityStats()` (`Community.jsx`) rather than guessing: the score is `(products where is_declared_bqp = true) ÷ (total products) × 100`, identical for every user (no personalization, no time window despite a comment claiming "last 30 days" — the actual query has no date filter at all). Flagged several gaps; user asked to build two of them:

- **Fake trend badge removed**: the "+2% ce mois" text next to the score was hardcoded, not computed from anything — same class of bug already fixed elsewhere in `AdminDashboard.jsx`/`PersoStats.jsx` on Jul 21, 2026, just missed here since this tab wasn't in scope then. Considered computing a real month-over-month delta (`products.created_at` does exist, so a "score as of 30 days ago" comparison is technically possible) but rejected it for the same reason the Jul 21 fix rejected %-of-previous-period trends: with only ~30 products total, a small/zero prior-period baseline makes the swing either undefined or absurd. Removed rather than faked, matching the established "hide the claim entirely rather than show one with nothing behind it" convention.
- **"Produits BQP/Locaux" undercount fixed**: the query only checked `is_declared_bqp`, silently ignoring the separate `is_local_production` column — so a product marked locally-produced but not BQP-declared wasn't counted, despite the card's own label promising both. Fixed to `.or('is_declared_bqp.eq.true,is_local_production.eq.true')` (a single OR query, so a product matching both isn't double-counted). **Verified live**: today's real data has 0 products where `is_local_production = true` and no `is_declared_bqp` overlap, so the visible number is unchanged for now (2 products, 7%) — but the gap is closed going forward as soon as a local-only product exists.
- **Not built, flagged for later**: scoring actual submission/scan behavior instead of static catalog composition (a product currently counts as "sovereignty progress" the instant it's tagged BQP, regardless of whether anyone's actually buying it), a personal-contribution line next to the community-wide score, and turning "Mission Collective" into a real progress bar instead of plain text. User's call on priority; none of these are quick fixes like the two above.

### Jul 28, 2026 — Panier Emptying on Refresh: Duplicate Primary Shopping Lists
Reported: selecting items into the Panier, then refreshing the page, showed an empty cart. Traced rather than guessed: `getOrCreatePrimaryList()` (`src/hooks/useShoppingList.js`) used `.maybeSingle()` to find the user's one `is_primary = true` `shopping_lists` row — but `.maybeSingle()` errors (returns null data) whenever *more than one* row matches, and only `data` was destructured so the error was silently swallowed. On that null, the code fell through to *creating a new empty primary list* — which is itself another duplicate, making the next load fail the same way. Self-reinforcing: one real account (the admin/dev-testing account) had accumulated **152 duplicate primary lists**, 9 of them holding real items scattered across different list_ids — so each refresh pointed the UI at a fresh, different, empty list. `shopping_list_schema.sql` had even flagged this exact gap already, commented out: "Ensure only one primary list per user (optional, but good practice)" — it was load-bearing, not optional. Checked all users, not just the reporting one: confirmed via a live query that only this one account was affected (152/152 duplicate rows belonged to it).

- **Code fix**: `getOrCreatePrimaryList()` now uses `.order('created_at', { ascending: true }).limit(1)` instead of `.maybeSingle()` — tolerates existing duplicates deterministically (oldest wins) instead of erroring and spawning another one. Stops new duplicates from forming for anyone, not just the affected account.
- **`shopping_list_dedup_migration.sql`** (new, **applied and verified live 2026-07-28**): generic, idempotent — merges every item from duplicate primary lists into the oldest one per user (summing quantities on conflict via the existing `unique(list_id, product_id)` constraint), deletes the now-redundant duplicate lists, then adds `create unique index ... on shopping_lists (user_id) where (is_primary = true)` so this is structurally impossible at the DB level going forward, not just app-level defensive coding.
- **A first attempt at live data recovery via an authenticated in-browser script was blocked by the Claude Code auto-mode safety classifier** (bulk delete/update of real user rows) — correctly, since that's exactly the class of action that should go through a reviewable SQL migration instead of an ad hoc script. Handed the migration file to the user to run instead, consistent with every other schema change this session.
- **Verified live**: post-migration, the affected account has exactly 1 primary list containing all 20 previously-scattered items (quantities correctly summed) — confirmed via direct query, then confirmed the Panier badge and full list render correctly in the UI, then did a second real page reload and confirmed the count held and no new duplicate list was created (`primaryListCount` stayed at 1 both times).
- **One leftover item surfaced by the recovery**: a `TEST QA Celebration Overlay 6` product (from earlier live-testing this session) was sitting in the merged cart — harmless, but worth removing along with the other `qa_cleanup_*.sql` test data if a cleanup pass happens.

### Jul 28, 2026 — Favoris "+ Panier" / "Ajouté ✓" Button Could Appear to Un-toggle
Reported: in Panier → Mes Favoris, a card's button correctly switches "+ Panier" → "Ajouté ✓" when added, but the user could get it back to "+ Panier" and re-click it — looking like an unwanted toggle-off. The button itself already had `disabled={inPanier}` (confirmed live: a real `disabled=""` DOM attribute, which browsers genuinely refuse to dispatch click events on) — so a literal click-to-undo wasn't possible. Investigated the actual mechanism instead of trusting that guard was sufficient:

- **Real bug found in `addToShoppingList()`** (`useShoppingList.js`): the insert-a-new-item path had no error handling. `shopping_list_items` has a `unique(list_id, product_id)` DB constraint, and if local React state and the DB ever briefly disagree about whether a product is already in the cart (slow round-trip, a second tab/device, or — as found live in this same investigation — the residual effects of the now-fixed duplicate-primary-list bug), the insert hits that constraint, fails with `23505`, and the code's `if (!error) { setShoppingList(...) }` silently did nothing on the error branch — leaving the button stuck showing "+ Panier" forever with zero feedback, functionally indistinguishable from "it reversed."
- **Fix**: on a `23505` conflict, the hook now fetches the real DB row and resyncs local state to it (updating the existing entry or adding it, whichever local state was missing) instead of silently dropping the action. Other errors are now at least logged instead of swallowed.
- **Verified live** by deliberately reproducing the race: inserted a product directly into the DB (simulating "another tab/device already added it" while local state didn't know), then clicked that same product's "+ Panier" button in the UI — confirmed it now correctly resyncs to "Ajouté ✓" (disabled) with the real DB quantity, instead of staying stuck.
- **Noted, not fully root-caused**: while live-debugging this, the test account's cart quantity total was observed to have dropped between two points in the same session (20 → 12) without an intentional removal action being taken. Most likely explanation is cross-talk from an earlier browser tab in this same debugging session that had become unresponsive and was later closed, not a new production code path — the item timestamps recovered are consistent with earlier test data, not a fresh loss. Flagged here for visibility rather than silently ignored, since it couldn't be fully forensically confirmed.

### Jul 28, 2026 — Profile Dropdown "Mes Contributions" Fixed + New "Mes Scans" List
Asked to review "Mes contributions à l'effort collectif" (profile dropdown, next to "Mes Économies") and brainstorm how to make it more valuable. Traced `UserMenu.jsx` rather than guessing:

- **Confirmed broken**: the tile showed `🔥 {userProfile?.streak_count || 1}` — `streak_count` is never written anywhere in the codebase (grepped repo-wide), so every user, permanently, saw "🔥 1" regardless of activity. It also wasn't clickable at all.
- **A second, unrelated bug found right next to it**: "Mes Économies" in this same dropdown computed `(total_contributions || 0) * 0.85`€ — a fabricated multiplier applied to the user's *scan count*, not an actual savings figure. This was a separate, still-broken copy of a calculation that had already been fixed correctly in `PersoStats.jsx` on Jul 21, 2026 (compare each priced product to the highest price observed for it, sum the positive differences) — the fix never propagated to this second copy, which is exactly the kind of drift duplicated logic invites.
- **`src/utils/userStats.js`** (new): extracted `calculateSavings(supabase, userId)` as the single shared implementation. `PersoStats.jsx` now calls it instead of carrying its own inline copy; `UserMenu.jsx` calls it lazily (only when the dropdown is actually opened) instead of maintaining a second formula. One source of truth going forward.
- **"Mes contributions" now shows the real count**: `userProfile.total_contributions` — already a genuine, trigger-maintained column (`sync_total_contributions()`, kept in sync with the user's own `prices` rows) already used correctly elsewhere (Leaderboard's "X prix") but never wired into this tile. The tile is now a real button.
- **`src/components/MyScansModal.jsx`** (new) — clicking the tile opens "Mes Scans": every price the user has personally submitted, most recent first (photo, product name, store, relative date -- "Aujourd'hui"/"Hier"/"Il y a N jours"/"Il y a N mois"), each with its own favorite-star toggle and "+ Panier" quick-add (reusing the exact same `inPanier`-disabled pattern and conflict-resync fix from the Favoris cards earlier today). Closes the loop the user asked for: contribute a scan → find it again later → re-add it to the cart or favorite it, without re-searching.
- **Verified live**: both dropdown tiles show real numbers (savings recalculated correctly, contribution count matching the real row count); opened "Mes Scans" and confirmed real scans render with correct photos/stores/relative dates/favorite state; used the quick "+ Panier" button and confirmed it correctly flips to "Ajouté" using the same resync-safe path fixed earlier today.
- **Brainstormed enhancements, deliberately not built this pass** — logged instead as `status: 'planned'` rows in `feature_requests` (reusing the Communauté/admin-Suggestions system built earlier today rather than inventing a separate "pending features" panel), so they're visible both in Console Admin → Suggestions and publicly in Communauté → Améliorations marked "Prévu" with an official comment:
  - **"Grouper mes scans par magasin"** — once a user has many scans, group by store (mirrors how people actually shop) instead of one flat list.
  - **"Système de séries (streak) de contribution réel"** — replace the old dead streak placeholder with genuine consecutive-active-days tracking; needs new backend tracking, not yet built.

### Jul 28, 2026 — "Mes Économies" Methodology Overhauled
Follow-up review, same session: asked to critique "Mes Économies" itself, not just its (already-fixed) duplicate-logic bug. Flagged that comparing against the highest price *ever recorded* for a product is the most flattering baseline mathematically possible, not a representative one, and has no time bound (a stale outlier from months ago could inflate the number indefinitely). User asked to build all four proposed fixes:

- **`userStats.js` methodology changed**: now compares each submitted price against the **average** price for that product (not the max), restricted to a **12-month window** (`SAVINGS_WINDOW_DAYS = 365`) — a stale outlier no longer distorts the figure forever. `calculateSavings()` is now a thin wrapper around a new `calculateSavingsBreakdown()`, which also returns a **per-scan** (`byScanId`) breakdown, not just the aggregate total.
- **"Mes Économies" is now clickable** (was a static div), opening the same "Mes Scans" modal as "Mes contributions" — both tiles now lead to one place rather than needing two separate detail views.
- **Per-scan savings badges in `MyScansModal.jsx`**: each scan row that beat the 12-month average shows a small green "−X,XX€" badge, plus a summary banner at the top of the list ("X€ économisés...") that matches the dropdown tile's number exactly — turns a previously inert aggregate into something explorable.
- **Explainer added**: a small info icon + native tooltip next to "Mes Économies" in the dropdown states the methodology plainly ("Comparé au prix moyen observé... sur les 12 derniers mois"), and `PersoStats.jsx`'s existing "Comment c'est calculé ?" box was updated to match the new wording (was still describing the old max-based method).
- **Verified live**: the real number changed from 12.30€ (old max-based) to 6.15€ (new average-based) for the same test account and data — confirms the methodology change actually took effect, not just a re-run of the old formula. Confirmed the "Mes Scans" summary banner shows the identical 6.15€, and per-row badges only appear on scans that genuinely beat their product's average (verified several correctly showing badges, several correctly showing none).

### Jul 28, 2026 — "Prix Recherchés": Signal Community Demand to Users' Favorite Stores
Raised while looking at Panier: if a product interests someone but has only been priced at a store that isn't in *their* favorite stores, could the app nudge users who *do* frequent that store to go scan it? Discussed the design before building, since one real decision was buried in it: the natural "someone wants this" signal is `user_favorites`, but that table's RLS is intentionally `auth.uid() = user_id` (own rows only) — individual favorite lists are private. Rather than broadening that (which would expose who favorited what), chose the narrower option: a single-purpose aggregate.

- **`product_favorite_counts_migration.sql`** (new, **not yet applied**): adds `get_product_favorite_counts()`, a `SECURITY DEFINER` SQL function returning only `(product_id, favorite_count)` pairs — never which user favorited what. `user_favorites`' own RLS is untouched; this is the one narrow, purpose-built exception, matching the same reasoning already used for `award_points()`.
- **`src/utils/scanRequests.js`** (new): `getWantedScans(supabase, storeIds)` — for the user's own favorite stores, finds community-favorited products not yet priced at each one, joins in names, sorts by demand.
- **`src/components/WantedScansModal.jsx`** (new): "🎯 Prix recherchés", grouped by store, each item showing "N personnes le veulent". Three states handled explicitly and honestly: no favorite stores set (prompts to add some), favorite stores set but nothing missing ("Bravo, tout est à jour !"), and the populated list.
- **`UserMenu.jsx`**: new action row (mirrors the existing "Mon Impact" row) opening the modal, with a live count badge on the icon once the dropdown is opened.
- **Verified live, including the not-yet-applied-migration case**: confirmed the empty-favorite-stores state renders correctly; temporarily added a real favorite store and confirmed that with the RPC not yet applied, `getWantedScans` degrades gracefully to the positive empty state (no crash, no visible error) rather than breaking — the code already checks `countsError` defensively. Removed the temporary test store afterward.
- **Two follow-up ideas logged as `status: 'planned'` feature requests, not built**: a contextual "N produits recherchés ici" banner once a store is picked in the Scanner flow (the most actionable moment, deferred to keep this pass scoped), and a public Top-20-most-wanted-products ranking (by category, and a parallel one for BQP products specifically), which would reuse the same `get_product_favorite_counts()` function once it exists.

### Jul 28, 2026 — Test vs. Real Product Flag (Console Admin "Données test")
Asked for a way to flag test/demo products separately from real end-user scans in the admin dashboard, without deleting them (prototype phase -- test data still has demo value). Mirrors the Comparer feed's card style per the request, but scoped to one row per *product* (not per price/scan) since the flag lives on `products` and a product can have many scans.

- **`product_test_flag_migration.sql`** (new, **not yet applied**): adds `products.is_test_data boolean not null default false`, auto-backfills `true` for every product already named `TEST %` (the naming convention already used organically throughout this project's own QA history), and indexes it. No RLS policy needed or added -- confirmed `products` has no row level security enabled at all today (consistent with regular end users being able to create new products directly while scanning), so the toggle works via the existing open update path.
- **`src/components/TestDataAdmin.jsx`** (new): one row per product (photo, name, category, scan count, latest price/store), a "TEST" badge when flagged, filter chips (Tous/Réels/Test), search, and a one-tap toggle button per product. New "Données test" sub-tab in `AdminDashboard.jsx`.
- **Comparer feed now excludes test-flagged products** from what end users see (`App10.jsx`'s `loadRecentPrices()`) -- **deliberately implemented as an isolated, separately-fetched, try/catch-wrapped filter, not as a column added to the main feed query.** Reasoning: querying a nonexistent column throws a hard Postgrest error (`42703`) that would have taken down the *entire* Comparer feed for every real user until the migration ran -- unlike this session's other recent features, there was no way to make the naive version degrade gracefully, so the fetch was deliberately decoupled instead of just hoping the migration lands before the code does. **Verified live, deliberately without the migration applied yet**: confirmed the Comparer feed still renders normally (27 cards), the error is caught and logged as explicitly non-fatal rather than surfacing, and the new admin tab shows an empty list rather than crashing -- both before the migration exists. The moment the migration runs, both start working with no further deploy needed.
- **Applied and verified live, Jul 28, 2026**: `product_test_flag_migration.sql` run in the Supabase SQL Editor. Confirmed: 23 real / 1 auto-flagged ("Test product 1", pre-existing) in the new admin tab; manually toggled a real product to "test" and confirmed it correctly disappeared from the Comparer feed on next load (the SPA doesn't re-fetch the feed on every tab switch, so the first check while still on stale client state incorrectly still showed it -- a fresh page load confirmed the exclusion genuinely works); reverted the manual test toggle afterward to leave the account clean.

### Jul 28, 2026 — France Hexagonale Comparison Made Legible; Admin "Missing Matches" Views
Asked to explain why Panier's "budget consommé" (32.13€) didn't match "Ce panier en Martinique" (14.04€) inside the France Hexagonale comparison box. Traced and verified against real data before answering: not a bug -- two different-sized subsets of the same cart. The cart had 10 items; the recommended store (Carrefour C.C. Cluny) only had prices for 6 of them (first narrowing, previously invisible); of those 6, only 3 also had a known France Hexagonale price (second narrowing, the only one with any visible caption before this). User asked to make both narrowing stages visible, add a clickable "amount lost to unmatched items" breakdown, and add an admin-side view of the resulting gaps so they can be closed over time.

- **`ShoppingList.jsx`**: `mainlandComparison` state now also carries `bestFoundCount`/`bestStoreName` (stage 1) and `unmatchedAmount`/`unmatchedItems` (the items found at the recommended store but missing a mainland price, and their combined value). New "X sur Y disponibles chez [magasin]" line surfaces stage 1 (only shown when incomplete). Label renamed to "Ce panier en Martinique (articles comparables)". The "Basé sur X sur Y" caption's denominator changed from the full cart size to `bestFoundCount`, so it now describes stage 2 specifically instead of conflating both stages into one ambiguous fraction. New clickable "⚠ X€ non comparés -- N articles sans prix France Hexagonale" line.
- **`UnmatchedItemsModal.jsx`** (new): opened by that line, lists exactly which items are excluded and why, plus their combined total -- closes the loop the user's original question was really asking.
- **`MainlandPriceAdmin.jsx`**: new "Sans prix Martinique (N)" panel at the top -- products with a France Hexagonale reference price but zero Martinique scans, for the admin to prioritize getting scanned. New filter chips ("Tous" / "Sans prix 🇫🇷") on the existing browsable list, so the inverse gap (Martinique-priced products still missing a mainland reference) is just as easy to find and close from the same screen.
- **Verified live with real data end-to-end**: confirmed the Panier UI shows "6 sur 10... chez Carrefour C.C. Cluny", "Basé sur 3 sur 6...", and "18.09€ non comparés — 3 articles"; opened the modal and confirmed the 3 listed items (12.95€ + 1.85€ + 3.29€) sum to exactly 18.09€. On the admin side, confirmed the "Sans prix Martinique" panel is correctly empty today (verified via direct query: 0 of 7 mainland-priced products lack a local price) and, to prove the panel isn't just silently failing, temporarily created a Hexagone-only test product, confirmed it appeared correctly in the panel, then deleted it. Also confirmed the "Sans prix 🇫🇷" filter correctly narrows the existing list.

### Jul 28, 2026 — Profile Dropdown: Two Bugs Reported and Fixed
Two issues raised after checking the profile dropdown on mobile: (1) "Mes Économies" and "Mes contributions à l'effort collectif" both opened `onOpenMyScans()` with no arguments -- genuinely the exact same modal, no distinction at all, confirmed by reading the code rather than assuming; (2) the dropdown's lower section (budget, magasins habituels, géographie, etc.) scrolls but gave no visual cue that it did.

- **Distinct purposes restored**: `MyScansModal.jsx` now accepts an `initialFilter` prop (`'all' | 'savings'`). "Mes Économies" opens pre-filtered to only scans that beat the 12-month average (title becomes "Mes Économies", matching subtitle); "Mes contributions" opens the full list as before ("Mes Scans"). Added visible filter chips ("Tous mes scans (N)" / "Avec économies (N)") inside the modal itself so either entry point can switch to the other lens without re-opening from the dropdown -- reinforces that these are two views of the same data rather than two unrelated pages.
- **Scroll indicator added**: a small bouncing chevron-down over a white fade-gradient appears at the bottom of the dropdown's scrollable section whenever there's more content below, and disappears once scrolled to the bottom (tracked via `scrollHeight - scrollTop - clientHeight`, rechecked on open and on every scroll).
- **Verified live**: confirmed the gradient+chevron renders correctly on open; clicked "Mes Économies" and confirmed it opens with "Avec économies (6)" active and a filtered list; clicked "Mes contributions" separately and confirmed it opens with "Tous mes scans (8)" active and the full list -- genuinely distinct now, not the same page twice.

### Jul 28, 2026 — Favorites: Icon/Wording Consistency + Un-favorite from Every View
Reported: Panier's "Mes Favoris" empty-state text said "l'étoile ⭐" but Comparer's actual favorite control isn't a star; also believed favorites couldn't be removed once added. Investigated rather than assuming either claim was fully accurate:

- **Confirmed real**: three different icons for one concept across the app -- Comparer's feed cards and the BQP flow use a `Bookmark` icon (2 places, the actual majority convention), `MyScansModal.jsx` used a `Star` icon (an inconsistency introduced when that view was built), and Panier's empty-state copy referenced a star that doesn't exist anywhere in the UI.
- **Correction surfaced during investigation**: Comparer's own bookmark toggle already worked both ways -- verified live (click → favorited, click again → unfavorited, confirmed via the actual CSS/state change). The real gap was that Panier's "Mes Favoris" watchlist cards had **no remove control at all** -- only "+ Panier" -- so a user could add a favorite from Comparer but had no way to undo it from the Panier view itself.
- **Standardized on `Bookmark` everywhere**: swapped `MyScansModal.jsx`'s Star for Bookmark, fixed Panier's empty-state text ("le signet 🔖"), and made the previously-static "Ajouter aux favoris" tooltips in `App10.jsx` (Comparer feed + BQP flow) dynamic ("Retirer des favoris" when already favorited).
- **Added the missing remove control**: Panier's "Mes Favoris" cards now have a small bookmark button overlaid on the product photo, wired to the same `toggleFavorite()` already used everywhere else -- no new logic, just reusing the proven toggle. Un-favoriting is independent of the cart (separate tables, `user_favorites` vs `shopping_list_items`), confirmed live: removed a favorite that was also in the cart and the cart count didn't change.
- **Verified live end-to-end**: favorited a product from Comparer, confirmed it appeared in Panier's "Mes Favoris" with the new bookmark overlay, clicked it to remove, confirmed the item disappeared and the corrected empty-state text appeared, and confirmed the cart (13 items) was unaffected. Also confirmed `MyScansModal.jsx` now renders the outline Bookmark icon instead of Star.

### Jul 28, 2026 — Martinique/France Flag Emoji Replaced with Local SVGs (Desktop Rendering Fix)
Reported: on laptop, the Scanner tab's "Martinique" / "France Hexagonale" region buttons showed plain "MQ"/"FR" text instead of flags, though mobile rendered them correctly. Root cause: those buttons used the literal 🇲🇶/🇫🇷 emoji characters -- flag emoji are Unicode Regional Indicator Symbol pairs, and rendering them as an actual flag glyph depends entirely on the OS/browser's emoji font having that specific flag. Martinique's flag in particular is commonly missing even from otherwise-complete emoji fonts (Windows Chrome in this case), so the browser fell back to showing the two raw indicator letters as plain text -- not a code bug, a font-support gap with no fix possible by choosing a "better" emoji, since every flag emoji relies on the same mechanism.

- **`src/components/flags/FlagMartinique.jsx` + `FlagFrance.jsx`** (new): inline SVG components, not emoji characters -- render identically on every OS/browser since they're vector paths, no font dependency at all. Path data sourced from Twemoji (Copyright 2020 Twitter, Inc and contributors, CC-BY 4.0, https://github.com/jdecked/twemoji) specifically so they match what mobile already correctly shows, rather than a differently-styled flag from elsewhere. Verified the source before using it: an alternative popular flag-icon package's Martinique SVG turned out to be the unofficial 2023 flag redesign (black/green/red), not the traditional blue/white design Unicode's 🇲🇶 and mobile phones actually render -- would have "fixed" desktop while making it inconsistent with mobile, the opposite of the goal.
- **Every remaining 🇲🇶/🇫🇷 emoji usage across the app replaced** with these components for full consistency, not just the reported button: `App10.jsx` (Scanner region toggle, manual-entry mainland toggle, "Produit Local ?" label, Comparer feed's mainland diff/pending badges), `AdminDashboard.jsx` ("Prix France Hexagonale" tab), `AboutPage.jsx` ("Produits Locaux" heading), `HunterDetailModal.jsx` (mainland diff/pending badges), `MainlandPriceAdmin.jsx` (missing-matches panel; the "Sans prix 🇫🇷" filter chip label is a plain string in an array, not JSX, so simplified to "Sans prix France" instead of restructuring that render loop for one chip), `ShoppingList.jsx` (France Hexagonale comparison heading).
- **Verified live**: confirmed both flags render correctly and clearly even at small size (zoomed screenshot of the Scanner region toggle) and spot-checked the Comparer feed's diff/pending badges -- all show the real flag now, no remaining "MQ"/"FR" text fallback anywhere.
- **Follow-up, same day**: the Twemoji-matched design above was the traditional blue-field "fer-de-lance" flag -- deliberately chosen at the time specifically to match what the 🇲🇶 emoji already showed on mobile. Told directly that this design reads as a colonial-era symbol rather than a flag of Martinique, and asked to switch to the official 2023 redesign (black field, green band, red triangle) instead. Swapped `FlagMartinique.jsx`'s path data to the version from `flag-icons` (https://github.com/lipis/flag-icons, MIT) -- the exact source found and set aside during the original investigation specifically because it *didn't* match the emoji, which turned out to be the right call once it was clear "match the emoji" wasn't actually the goal. `FlagFrance.jsx` and every other call site untouched -- only the Martinique component's internals changed, verified live via a zoomed screenshot of the Scanner region toggle.

### Jul 30, 2026 — `dev` Branch + PR Workflow Introduced (Repo Config, Not App Code)
Set up a staging branch so changes can be tested before reaching production, rather than every change landing directly on `main`.

- **`dev` branch** created off `main` on GitHub (`JMP-APZH/mvp1`) and is now where all day-to-day changes land first. Vercel's Production Branch is still `main` (unchanged) — pushing to `dev` only triggers `dev`'s own separate preview deployment, never production. Default cadence: one discrete change → commit to `dev` → open a `dev → main` PR right away; a request that naturally splits into several sub-changes gets batched on `dev` and PR'd as one group once the whole piece is done.
- **`.github/workflows/pr-build-check.yml`** (lint + build on PRs) now triggers on PRs targeting `dev` as well as `main` — previously `main`-only.
- **Classic branch protection rule added on `main`**: requires a PR before merging, requires status checks to pass (no specific check selected yet — GitHub only allows requiring a check that has already run at least once via a real PR; add "Validate build" here once the first `dev → main` PR runs it), blocks force-push and branch deletion. No required-approval count set (solo maintainer) and admin bypass left on.
- **Caveat: not actually enforced yet.** `mvp1` is a private repo on GitHub's free plan, and branch protection rules on private repos are configurable but not enforced until upgrading to GitHub Team/Enterprise or making the repo public — confirmed via the "Not enforced" label GitHub shows next to the rule. Kept private for now; this is scaffolding for when that changes, not an active gate today. In the meantime the `dev`/`main` split and PR discipline are enforced by convention, not by GitHub.
- **Update, same day**: "Validate build" ran for the first time on PR #1 (the CLAUDE.md-only docs commit above) and passed, so it was added to `main`'s branch protection rule as the required status check.

### Jul 30, 2026 — Full ESLint Cleanup (29 Errors/Warnings) + Two Real Bugs Found
First real test of the new `pr-build-check.yml` gate: `npm run lint` had never actually been run in CI before, and immediately surfaced 89 problems across the repo. Fixed all of it in one pass rather than leaving the gate permanently red.

- **Config fixes**: excluded `src/_archive` (the explicitly frozen legacy `App.jsx`–`App9.jsx` files) from ESLint scanning entirely — most of the noise was there. Added a Node-globals override for `scripts/**` (false `no-undef` on `process`), trivial unused-param fix in `public/sw.js`.
- **29 real errors/warnings fixed** across `App10.jsx`, `AdminDashboard.jsx`, `PersoStats.jsx`, `Community.jsx`, `Leaderboard.jsx`, `ProductDetailModal.jsx`, `StoreSelectionWizard.jsx`, `ZXingBarcodeScanner.jsx`, `HybridBarcodeScanner.jsx`, `AuthContext.jsx`, `useShoppingList.js` — mostly unused vars and missing-hook-dependency warnings, resolved by wrapping data-loading functions in `useCallback` and adding them to their effects' dependency arrays. `AuthContext.jsx`'s bare context object was split into its own `src/contexts/authContextValue.js` (with `useAuth.js` re-exporting the hook) to fix a Fast Refresh violation from exporting both a component and a non-component value from one file.
- **Real bug #1 — `UserMenu.jsx`**: the free-text city input (profile dropdown, non-Martinique region) had its `value` bound to `userProfile?.city` instead of the local `cityInput` state that `onChange`/`onBlur` actually read — typed keystrokes never visibly appeared. The `useEffect` "syncing" `cityInput` was a band-aid for this exact mismatch. Fixed the binding directly and replaced the effect with React's recommended "adjust state during render" pattern (comparing against a `syncedProfileCity` state) instead of a `useEffect`, since a stray `throw`-free effect-based sync was the wrong tool here.
- **Real bug #2 — declaration-order crash in four files**: while wrapping loader functions in `useCallback` to satisfy `exhaustive-deps`, four files (`App10.jsx`, `Community.jsx`, `Leaderboard.jsx`, `StoreSelectionWizard.jsx`) ended up with a `useEffect` dependency array referencing a `useCallback` `const` declared *later* in the same file — a guaranteed `ReferenceError` on first render (plain JS temporal-dead-zone semantics, not a React quirk). ESLint did not reliably flag this pattern across all four files even though it's a real crash; caught by manually auditing every file's declaration order against its effects, not by tooling. Reordered declarations in all four so loaders are defined before the effects that call them.
- **One remaining item left as a documented false positive**: `useShoppingList.js`'s auth-state-sync effect still trips `react-hooks/set-state-in-effect`. Isolated-repro testing showed this very new (`eslint-plugin-react-hooks@7.0.1`) rule's trigger depends on unrelated surface details (e.g. whether an unrelated `throw` statement appears near the `await`), not on anything about the effect's actual correctness — suppressed with an explained `eslint-disable-line` rather than mutating working code to satisfy a demonstrably unreliable heuristic.
- **Verified**: `npm run lint` and `npm run build` both clean at the time of shipping.
- **Caught later, same day, and separately hotfixed** (see the entry below): the `useCallback` dependency arrays added here for `App10.jsx`'s `loadStores`/`loadRecentPrices` included `toast` (from `useToast()`), which turned out to be a new object every render — this shipped a real infinite-fetch-loop regression that wasn't caught by lint or build, only by manually testing the running app.

### Jul 30, 2026 — PostHog Dashboard Rebuilt Around Real Events; Admin "Analytics" Link Added
Asked why the PostHog project showed no recognizable dashboard. Root cause: the project (created 2026-07-26, so 4 days old) still had PostHog's auto-generated "Your starter dashboard" as its primary dashboard — built entirely on generic `$pageview`/`$autocapture`/`$referring_domain`, none of which reflect this app's actual instrumentation. Confirmed via direct HogQL query against the raw `events` table (not just the schema listing, which under-reports): real event volume is genuinely low (1,062 events / 2 unique people / 14 sessions in 90 days, matching internal testing only), but several custom events *do* have real data — `store_wizard_step_viewed` (504), `my_scans_opened` (18), `price_submitted` (6), `wanted_scans_opened` (6), `my_scans_item_added_to_panier` (5), `feature_request_opened` (4), `recipe_viewed` (2), `first_contribution_completed` (1), `feature_comment_posted` (1), `recipe_ingredients_added_to_list` (1), `feature_request_admin_replied` (1).

- **Instrumentation gap found, not yet root-caused**: `barcode_detected`, `product_matched`, `scan_session_started`, `anon_to_signup_converted`, `store_wizard_completed`, and `store_wizard_gps_detected` — all documented as instrumented in the Jul 26, 2026 PostHog entry above — have **zero** recorded events ever, confirmed via HogQL (not just an empty date range). Plausible benign explanation: no one has completed a real barcode-scan session since instrumentation was added (manual price entry may have been used for all real testing instead), but this hasn't been verified against the actual code paths — worth a dedicated check before trusting those event names in any future analysis.
- **New dashboard** (`Prix Martinique — Product Analytics`, project 232864, dashboard 862895 — https://eu.posthog.com/project/232864/dashboard/862895) replaces the starter as the project's `primary_dashboard`. Built entirely on the real events above: Price Submissions (daily trend), New Contributors (`first_contribution_completed`), Store Selection Funnel (`store_wizard_step_viewed` by `step_name`: city→chain→store — currently shows a near-total drop-off after "city", 500→4→0, itself worth investigating separately, possibly a re-render re-fire rather than 500 distinct visits), Repeat Contributors (weekly retention on `price_submitted`), Community Engagement (feature requests/comments/recipes, one multi-series chart), Personal Feature Usage (Mes Scans / Prix Recherchés adoption). Old starter dashboard (850101) left in place, just unpinned as primary, not deleted.
- **Admin Dashboard "Analytics" tab** (`AdminDashboard.jsx`): new sub-tab button that opens the PostHog dashboard in a new tab via `window.open`. Deliberately **not embedded** — PostHog iframe embedding requires a public share link (bypassing this app's own admin-role gate entirely) or a backend proxy to keep a read API key secret, and this app has no backend (pure client + Supabase, no Edge Functions). An external link was the only option that doesn't leak analytics data or require new infrastructure.

### Jul 30, 2026 — Infinite Fetch Loop from Unmemoized `useToast` Object (Hotfix)
Found while testing an unrelated PR's Vercel preview on a phone before merging — exactly the kind of check this session's new `dev`/PR workflow exists to enable. Confirmed via an instrumented `fetch` wrapper in a live browser session: loading the Scanner tab fired the same batch of Supabase requests (`stores`, `categories`, `prices`, mainland-prices, test-flag check) over 500 times in a few seconds, all succeeding (200 OK) — a genuine infinite loop, not a data problem.

- **Root cause**: `useToast.js`'s `toast` object (`{ success, error, info }`) was a plain object literal recreated on every render, never memoized. Earlier the same day, fixing ESLint's `exhaustive-deps` warnings added `toast` to the dependency arrays of `loadStores`/`loadRecentPrices` (`useCallback`s in `App10.jsx`) — correct per the lint rule, but since `toast`'s identity changed every render, those callbacks never stabilized: their `useEffect`s (deps `[loadStores]`/`[loadRecentPrices]`) re-fired every render, each setState triggering another render, ad infinitum.
- **Already live in production** at the time this was found — it shipped as part of the same-day ESLint cleanup PR, not something newly introduced by this hotfix.
- **Fix**: wrapped the `toast` object in `useMemo(..., [addToast])` in `useToast.js` — `addToast` itself was already stable (`useCallback` with `[]` deps), so this is the only change needed. `App10.jsx` untouched.
- **Verified live**: re-ran the same instrumented-fetch test against this fix's Vercel preview — confirmed the request flood is gone (each fetch fires once per real state change, not continuously).

### Jul 31, 2026 — Instrumentation Gap Investigated: One Real Bug, Five Benign Explanations
Followed up on the six events flagged in the Jul 30, 2026 PostHog entry above (`barcode_detected`, `product_matched`, `scan_session_started`, `anon_to_signup_converted`, `store_wizard_completed`, `store_wizard_gps_detected` — all documented as instrumented but with zero recorded events ever). Traced each one against the actual code rather than assuming the gap was uniform.

- **`store_wizard_completed` — confirmed real bug, fixed.** The capture lived in `StoreSelectionWizard.jsx`'s own `useEffect(() => { if (selectedStoreId) {...} }, [selectedStoreId])`, but `App10.jsx`'s parent ternary swaps `StoreSelectionWizard` out of the tree for a "store chosen" summary view in the *same render* that `manualEntry.storeId` becomes truthy — the component unmounts before that effect can ever observe the prop as truthy. This is unreachable through all four ways a store gets chosen (explicit pick, single-store auto-select on chain selection, favorites shortcut, nearest-store shortcut), not just one path. **Fix**: replaced the effect with a `selectStore(storeId)` wrapper that fires the capture at the point of selection (before calling the `onStoreSelect` prop that triggers the unmount), and routed all four selection paths through it instead of calling `onStoreSelect` directly.
- **`barcode_detected` / `product_matched` / `scan_session_started` — code correctly wired, no bug found.** Single, unconditional capture at each real entry point (`scan_session_started` on the "Scanner" button click; `barcode_detected`/`product_matched` inside `handleBarcodeDetected`, which both `ZXingBarcodeScanner` mount sites correctly wire as `onDetected`). Zero events most likely means every real price submission since Jul 26 used the manual-entry form rather than the camera-scan path — a usage-pattern gap, not an instrumentation bug.
- **`store_wizard_gps_detected` — legitimately zero, not a bug.** Fires inside `attemptGPSDetection`, gated on the browser's geolocation actually resolving. Directly observed via live console logs during this session's own preview testing: `GPS error: Timeout expired` and `GPS error: User denied Geolocation` — GPS has never successfully resolved in any tested session so far.
- **`anon_to_signup_converted` — legitimately zero, not a bug.** Fires unconditionally on every successful email signup (`AuthContext.jsx`'s `signUp()`) and on Google OAuth sign-ins that match the `isLikelyNewOAuthSignup` heuristic. The project has had exactly 2 unique people total since PostHog was added (Jul 26) — both existing accounts (the admin and the "Tony" test account) predate instrumentation, and no new signup has happened since.
- **Verification attempted, inconclusive due to a tooling limitation, not the fix.** Clicked through the store-selection flow (Basse-Pointe → E.Leclerc, single-store auto-select) on the `dev` preview via the automated Chrome browser tool, with `fetch`/`sendBeacon`/`XMLHttpRequest` all patched to log any PostHog-bound traffic. Nothing was captured — but as a control, `store_wizard_step_viewed` (a different event with 504 confirmed historical hits, fired repeatedly in the very same clicks) *also* failed to transmit under identical conditions, and a direct hand-written request to PostHog's endpoint from the same tab succeeded (HTTP 400, i.e. reachable, not network-blocked). That combination means this specific automated browser tool's environment suppresses PostHog's own SDK traffic specifically (a common privacy/tracking-protection behavior in automated browsers, keyed on the requesting script rather than the bare domain) — not that the fix is broken. Confidence in the fix rests on the traced root cause (the exact unmount-timing mechanism, confirmed structurally) and mirroring the already-proven `scan_session_started` pattern (fire synchronously on the user action, not from an effect watching a prop). A real-device click-through remains the way to fully close the loop if wanted.

### Aug 1, 2026 — Offline Mode Shipped (Not Yet Logged Here Until Now)
Noting for the record, since this file's "Last Updated" footer was stuck at Jul 30 despite two real commits landing after it: `8d74e3c` ("feat: offline mode for price submission and shopping list") added an IndexedDB-backed offline queue (`src/utils/offlineDb.js`, `priceSubmission.js`, `cartOps.js`, `syncQueue.js`, `useOnlineStatus.js`, `useOfflineSync.js`, `OfflineBanner.jsx`) so barcode scanning, price submission, and Panier add/remove/quantity edits work fully offline with automatic sync on reconnect, ahead of the Aug 15 RPPRAC follow-up. `04e0464` applied and verified `qa_cleanup_2026-08-01.sql`. No CLAUDE.md entry was written for either at the time — this note exists so the next person reading this file's history doesn't assume the feature doesn't exist just because it wasn't logged.

### Aug 5, 2026 — Panier: Basket Completeness Tracker + Category Budget Breakdown
Built on top of the existing cross-store comparator (`ShoppingList.jsx`'s `comparePrices` effect) rather than replacing it, per spec. Two new pieces, both purely inside `ShoppingList.jsx` (plus one small, necessary `App10.jsx` wiring change for the deep-link — see below):

- **"Complétude du panier"**: headline "X/Y articles à jour" + progress bar at the top of the Panier tab. An item counts as up to date when it has a `category_id` **and** its most recent Martinique price is within a freshness window. **Threshold picked from real data, not guessed**: queried production `prices.created_at` per product live before hardcoding anything — the most-recent price per product currently splits cleanly into two clusters (9–15 days old vs. 181–206 days old, nothing at all in between), so any threshold between ~16 and ~180 days gives an identical result today. Picked **30 days** (`FRESHNESS_WINDOW_DAYS` in `ShoppingList.jsx`) as a conventional, easy-to-explain value inside that gap, not a fitted number that happens to work today only.
- Incomplete items get inline quick-actions: **"Catégoriser"** opens an icon-grid popover (same visual/interaction pattern as the Comparer tab's category filter picker, reimplemented self-contained inside `ShoppingList.jsx` rather than extracted into a shared component, so `App10.jsx` didn't need touching for it) listing every category — not just `pickerCategories`' scanned-product subset, since a panier item may need a category nothing has used yet. **"Prix à confirmer"** deep-links into the existing manual-entry form: a new `prefillPriceSubmission(item)` in `App10.jsx`, passed down as `onRequestPriceUpdate`, sets `manualEntry.productName` and switches to the Scanner tab (`setActiveTab('scan')`) — `submitPrice`'s existing find-or-create step already matches on product name via `ilike`, so no new productId plumbing was needed. This is the one place this feature reaches outside `ShoppingList.jsx`; scoped to a single new handler + one prop, no tab/nav restructuring.
- **Category budget breakdown ("Répartition par catégorie")**: groups the panier by `category_id` with an explicit, never-hidden "Non catégorisé" bucket for nulls. Each item is valued at its cheapest known Martinique price across all stores (not tied to the comparator's single recommended store) — reuses the exact same `prices` rows already fetched for the comparator; the cheapest-price and most-recent-price-date lookups were merged into one pass over that array (`priceInfoByProduct`), replacing what used to be a second, separate loop building `cheapestByProduct` just for the savings-opportunities section. Purely descriptive, no cross-store savings claim per category (that needs more comparator data density than exists today).
- **RLS decision — resolved empirically, not guessed.** The spec flagged an open question: does writing `products.category_id` from a *user-facing* flow need its own RLS policy, or does the existing authenticated-write policy already cover it (as documented for `prices`/`products` in the Jul 21, 2026 France Hexagonale entry above)? Investigated in order: (1) confirmed via the anon key with no user JWT that `products` UPDATE is genuinely RLS-gated (a PATCH against a real row returns HTTP 200 but silently affects zero rows — contradicting `product_test_flag_migration.sql`'s Jul 28 comment claiming `products` "has no row level security enabled at all," which is stale/wrong and should not be trusted); (2) rather than fabricate a throwaway auth account to test with (no service-role key available to clean it up afterward, an unlike-this-project's-other-test-data permanent side effect), tested live in the already-open Chrome session, which turned out to be signed in as a real authenticated test account (`JMP2_972`) — clicked "Catégoriser" on a real panier item ("LAIT ECREME 0,3MG UHT BRICK") and selected "Produits Laitiers"; confirmed via a direct follow-up query against the live `products` table that `category_id` was genuinely persisted to `b47d1597-dfda-434e-990f-0fc91210328a`. **Conclusion: no new RLS policy needed** — any authenticated user can already write `products.category_id`, exactly like the existing admin tools (`ProductCompletion.jsx`) rely on, whose "admin-only" reach is a UI-level gate (which Console Admin sub-tab you can open), not a database one. The write path still has a try/catch with optimistic-update rollback and an inline error message, so if this conclusion is ever wrong for some other auth configuration, it fails visibly instead of silently.
- **PostHog**: `panier_completeness_viewed` (fires once per Panier visit with ≥1 item), `panier_categorize_completed`, `panier_stale_price_clicked` — snake_case, distinct per action, none reusing an existing event name.
- **Verified live against real production data**, not a fresh empty cart: opened the Panier tab as the real `JMP2_972` test account (14 real items) via the local dev server. Completeness read "5/14 articles à jour" before any change, correctly matching the 30-day freshness split found earlier. Categorized the milk item live (see RLS paragraph above) — completeness moved to 6/14, and "Répartition par catégorie" picked it up immediately (Produits Laitiers: 1.85€, "Non catégorisé" correctly still showing all remaining uncategorized items, 37%, never hidden). Clicked "Prix à confirmer" on "Evian 1L" and confirmed the Scanner tab opened with "Nom du produit" pre-filled to "Evian 1L". Confirmed all three PostHog events landed with correct properties via a direct HogQL query against the raw `events` table (`panier_completeness_viewed` with `item_count: 14`; `panier_categorize_completed` with the real `product_id`/`category_id`; `panier_stale_price_clicked` with `days_since_price: 182`, matching Evian 1L's real staleness). `npm run lint` and `npm run build` both clean. The one real side effect left behind by this live test — "LAIT ECREME 0,3MG UHT BRICK" now genuinely categorized as "Produits Laitiers" — is a correct, real improvement, not test data; left in place rather than reverted (reverting would have required the account's own session, since a plain anon-key PATCH is exactly the unauthenticated write this session proved gets silently blocked).
- **Not independently re-verified**: whether a non-test, ordinary authenticated user (vs. `JMP2_972`) also succeeds — the RLS conclusion above should hold generally since it's a project-wide policy, not an account-specific grant, but this pass only exercised one account.
- Real category coverage is now **6/23** non-test products (was 5/23 as of the Jul 21, 2026 bug-sweep entry above), from the live test above — see that entry's note for context on why coverage is low to begin with; this new tracker is now a second, ongoing avenue (alongside `ProductCompletion.jsx`) for closing that gap over time.

### Aug 7, 2026 — Comparer Tab: "Ajouter au panier" Now Gated Behind Sign-In
Requested: a logged-out user clicking the basket icon on a Comparer feed card should hit the sign-in modal, the same as the adjacent favorite (bookmark) icon already does — not silently start an anonymous localStorage cart from that entry point.

- **`App10.jsx`**: new `handleAddToShoppingListFromComparer(product)`, mirroring the existing `handleToggleFavorite` guard (`if (!user) { setShowAuthModal(true); return; }`) exactly. Wired only to the Comparer feed card's basket button (previously called `addToShoppingList` directly). Scoped deliberately narrow, per the request — anonymous cart-building elsewhere in the app (Panier's "Mes Favoris" watchlist, "Idées recettes" add-all, the post-scan BQP quick-add) is untouched and still works anonymously; that's a separate, intentional feature (`useShoppingList.js`'s documented localStorage fallback), not something this request asked to change.
- **Verified live, logged out**: clicked the basket icon on a real Comparer card — sign-in modal opened, matching the favorite icon's behavior exactly; closed the modal and confirmed the item was not added (no Panier badge, icon state unchanged).
- **Caught mid-test, not a code bug**: the first live-test attempt appeared to fail (item got added despite being logged out) — traced to a stale service worker still serving a pre-edit JS bundle in the test browser tab, not a problem with the guard itself. Unregistering the stale worker and reloading fixed it immediately. Same root-cause class as the Jul 28, 2026 "Feature Request Comments" entry's stale-SW note above — worth remembering as a recurring false-negative source when live-testing this app via an already-open dev-server tab.
- `npm run lint` / `npm run build` clean.

### Aug 7, 2026 — Panier Decluttered: Collapsible Basket Detail + "Idées recettes" Hub (Browse/Submit/Like/Favorite)
Two-part restructuring request, with a design-decision step first (`AskUserQuestion`) since several forks were genuinely product calls, not technical ones — basket-detail UI pattern (modal vs. accordion vs. separate page), recettes presentation (modal vs. new tab), community-submission scope (lightweight idea vs. full structured recipe), and whether "top-liked ideas join the official recipes" should be live automation or just messaging this pass. Decisions: **collapsible accordion** for the basket list, **full-screen modal** for recettes (reusing the established `z-[300]` overlay chrome, e.g. `UnmatchedItemsModal.jsx`), **lightweight idea submission** (title + description + meal-category dropdown, no ingredient list — same shape as `product_comments`), and **informational message only** for the promotion-to-official messaging (no auto-promotion logic).

- **Basket detail accordion**: the raw item list (photo, quantity steppers, remove) that used to render unconditionally right after "Idées recettes" is now collapsed by default behind a "🧾 Consulter le détail de mon panier (N)" toggle (`showBasketDetail` state, `ShoppingList.jsx`) — same list markup, just conditionally rendered. Fires `panier_basket_detail_expanded` on open only (not on collapse).
- **`RecipesHubModal.jsx`** (new): the old inline "Idées recettes" horizontal-scroll strip — and all its loading state/effects — was moved out of `ShoppingList.jsx` entirely into this new full-screen modal, opened from a single "Idées recettes →" link. `ShoppingList.jsx` no longer loads any recipe data itself, keeping it focused on favorites/basket per the request. Two sections:
  1. **"Recettes officielles"** — the admin-curated `recipes`/`recipe_ingredients` browsing + "+ Tout" quick-add, unchanged logic, just relocated.
  2. **"Idées de la communauté"** (new feature) — any logged-in user can submit a lightweight recipe idea (title, description, meal-time category via `<select>`: Petit-déj' / Snack du matin / Déjeuner / Goûter / Dîner — `src/constants/mealCategories.js`, matching the `mainlandChains.js` shared-constant precedent). Visitors can like (public count) and favorite (private, mirrors `user_favorites`) each idea. Sorted most-liked-first, newest tiebreak — same convention as `product_comments`. An info banner states that top-liked ideas may be manually promoted into the official recipes later (no automated pipeline built).
- **`community_recipe_ideas_migration.sql`** (new, **not yet applied**): three tables — `community_recipe_ideas`, `community_recipe_idea_likes`, `community_recipe_idea_favorites` — RLS modeled directly on `product_comments`/`product_comment_likes` (public read + own-row insert on ideas/likes, own-row delete on likes for un-like) and `user_favorites` (favorites are select/insert/delete restricted to the owning user, not publicly readable — a private bookmark list, not a public signal like likes). `RecipesHubModal.jsx` degrades gracefully without it: community section shows "Les idées de la communauté arrivent bientôt." instead of erroring, same isolated-fetch pattern already used for `product_test_flag_migration.sql`/`mainland_price_migration.sql`. **Verified this degradation live, deliberately without the migration applied** — official recipes still loaded, empty-state rendered correctly, no crash.
- **Real pre-existing bug found and fixed while wiring `onRequireAuth` into the new modal**: `AuthModal.jsx` was `z-[200]`, while every full-screen detail modal that calls `onRequireAuth?.()` (`ProductDetailModal`, `RecipeDetailModal`, `HunterDetailModal`, `FeatureRequestDetailModal`, and now `RecipesHubModal`) is `z-[300]` — so the sign-in modal has been rendering *behind* whichever detail modal triggered it, for every single one of these flows, since each was built. Never surfaced before because this is the first flow in this codebase to trigger `onRequireAuth` from within one of these full-screen modals during live testing. **Fixed**: bumped `AuthModal.jsx` to `z-[400]`, matching the "on top of everything" convention already used for photo-zoom overlays (`ProductCompletion.jsx`, `ProductDetailModal.jsx`'s own zoom, `MainlandPriceAdmin.jsx`) — this fixes the same latent bug for all five affected flows at once, not just the new one. Verified live: before the fix, triggering sign-in from inside `RecipesHubModal` showed only a sliver of the auth modal's header, unusable; after, it renders correctly on top.
- **PostHog**: `recettes_hub_opened`, `community_recipe_idea_submitted`, `community_recipe_idea_liked`, `community_recipe_idea_favorited`, `panier_basket_detail_expanded`. `recettes_hub_opened` was observed firing twice per open in the dev console query — confirmed as a React 19 `<StrictMode>` dev-only double-invoke (`main.jsx`), not a production double-fire; not something to fix.
- **Verified live** (local dev server, anonymous session — the previously-logged-in `JMP2_972` dev session had expired and re-authenticating wasn't available to this session; see the RLS caveat below): confirmed the decluttered Panier layout (Budget → Favoris → Idées recettes link → basket-detail toggle), the accordion expand/collapse with a real item added via a recipe's "+ Tout" (still works anonymously, unaffected by the unrelated Aug 5 Comparer sign-in gate), the recettes hub's official-recipes section and meal-category filter chips, the community section's graceful pre-migration empty state, `onSelectRecipe` still opening `RecipeDetailModal` correctly from inside the new hub, and the `onRequireAuth` fix end-to-end (sign-in modal now usable on top). `npm run lint` and `npm run build` both clean.
- **`community_recipe_ideas_migration.sql` applied and read-path verified live, same day**: confirmed via the anon key that all three tables exist and their RLS behaves as designed — `community_recipe_ideas`/`community_recipe_idea_likes` return real rows to an unauthenticated request (public read), `community_recipe_idea_favorites` returns `200 []` to the same request (private, correctly hidden rather than erroring). Reloaded the recettes hub in a fresh anonymous browser session afterward and confirmed the community section now queries the live tables successfully — "Aucune idée pour le moment. Soyez le premier à en proposer une !" (the real empty state) instead of the pre-migration "arrivent bientôt" fallback.
- **Write path verified live on production, Aug 7, 2026 (same day, after the user signed back into `JMP2_972`)**: submitted a real idea ("TEST QA Jus de fruits de la passion", Goûter category) through the deployed `RecipesHubModal.jsx` — appeared instantly with correct author/date. Liked it (heart count 0→1) and favorited it (bookmark filled), both confirmed via a direct query against `community_recipe_idea_likes` (real row, correct `user_id`/`idea_id`) — favorites couldn't be queried the same way (RLS correctly hides other sessions' rows from the anon key) but the UI toggle was confirmed both ways. Un-liked and un-favorited afterward through the same UI controls (both have delete policies, confirmed the like row was actually gone via a follow-up query). **Test data left behind**: the idea row itself has no delete policy (append-only, same as `product_comments`) — `qa_cleanup_2026-08-07.sql` (new, **not yet applied**) removes it via the SQL Editor.

### Aug 8, 2026 — Panier: Section Reorder, Complétude Made Collapsible, Independent Per-Store Comparateur Toggles
Four layout/interaction requests, all in `ShoppingList.jsx`:

- **Reorder**: "Consulter le détail de mon panier" moved from just above the (now-removed) inline item list to directly under the "Mon Panier (N) / Vider" header — the very first thing in the scrollable content. "Répartition par catégorie" moved to immediately follow it (3rd position counting the header as 1st). Both stay gated on `items.length > 0` as before (a fresh/empty cart shows neither, falling through to Budget → Favoris → Idées recettes → the empty-cart message, unchanged). "Complétude du panier" now follows Répartition, then Budget/Favoris/Idées recettes/Comparateur as before — no other section's relative order was touched.
- **Complétude collapsible**: the headline "X/Y articles à jour" + progress bar is now the always-visible click target (with a chevron), and the explanatory caption + per-item Catégoriser/Prix-à-confirmer list collapses underneath it — same accordion pattern as "Consulter le détail," collapsed by default. The per-item action buttons and the category picker inside now call `e.stopPropagation()` so clicking them doesn't also toggle the outer accordion closed.
- **Comparateur de Panier — independent per-store expand/collapse**: `expandedStore` (a single value — one store expanded at a time, closing on any other row's click) replaced with `expandedStores`, a `Set`, via a new `toggleStoreExpanded(storeId)` helper. Investigated the reported bug first rather than assuming: the old single-value ternary (`expandedStore === result.storeId ? null : result.storeId`) *did* already toggle the same store closed on a second click — confirmed this still works correctly with the new Set-based version too (verified live: click opens, click again closes). The real gap the request was pointing at is that opening a *different* store's row used to force-close whichever one was already open (shared single-value state) — the Set makes every store's expansion fully independent, so multiple can be open at once and none get closed as a side effect of expanding another.
- **Verified live** (local dev server, anonymous session, one real item added via a recipe's "+ Tout" — `useShoppingList.js`'s anonymous localStorage cart proved unreliable to seed directly via `localStorage.setItem` + full reload in this session specifically because of a `<StrictMode>` dev-only double-mount race with the "persist to localStorage" effect, clobbering the seed before the sync effect could use it; not a real bug, just a testing-tool limitation, worked around by adding the item through the real UI within one continuous SPA session instead of reloading): confirmed the new section order top-to-bottom exactly as requested, Complétude's collapse/expand both directions, and the Comparateur store row's same-button open→close toggle. **Not independently verified**: two-or-more-stores-open-simultaneously (the specific "opening B doesn't close A" case), since this session's cart only ever had items resolving to one store — the `Set`-based logic is straightforward enough to trust by inspection, but worth a spot-check with a richer multi-store cart (e.g. the `JMP2_972` production account) if it's ever in doubt.
- `npm run lint` and `npm run build` both clean.

### Aug 8, 2026 (2) — Répartition par Catégorie Made Collapsible + Drill-Down; "Prix à Confirmer" Landing Fixed
Follow-up to the same day's reorder pass, prompted by two things: (1) a clarifying question about what "Répartition par catégorie" actually aggregates, answered by reading the code rather than from memory (it's strictly the user's own Panier items, grouped by category — not all scans, not community-wide data — each valued at that product's cheapest known price across all stores); (2) a real UX bug found while testing "Prix à confirmer."

- **Répartition par catégorie — now collapsible + drill-down** (`ShoppingList.jsx`): matches the "Consulter le détail de mon panier" accordion pattern exactly — collapsed by default behind a "Répartition par catégorie (N)" toggle. Each category row inside is now itself a button (`toggleCategoryExpanded`, a `Set` state mirroring `expandedStores`' independent-per-row pattern from the same day's earlier Comparateur fix) that reveals the specific items priced into that category's subtotal. Required extending the `comparePrices` effect's `breakdownMap` to also collect each category's `items: [{productId, name, quantity, knownPrice}]` alongside the existing subtotal/count fields — no new query, just retaining data already being computed.
- **"Prix à confirmer" bug found and fixed — root cause, not guessed.** Reported: clicking it "navigates to the scan/home page" instead of doing something useful. Traced `App10.jsx`: `prefillPriceSubmission` correctly sets `manualEntry.productName`, but the Scanner tab's own render logic shows the *store-selection* step first whenever `!manualEntry.storeId && !manualEntry.mainlandChain` — true on every fresh page load, since store selection isn't persisted across reloads. The pre-fill was silently sitting in state the whole time, just invisible behind a mandatory step that gave no indication anything had happened. **Fix**: a blue banner ("🕐 Mise à jour du prix de **[produit]** — choisissez d'abord votre magasin pour continuer.") now renders above the store-selection step whenever `manualEntry.productName` is already set at that point — a condition that's only ever true via this deep-link, since the normal scan flow doesn't populate `productName` until *after* store selection. No behavior change to the actual submission flow; purely closes the "did anything happen?" gap.
- **Verified live** (same anonymous session, one real item "Huile de friture"): expanded "Répartition par catégorie" → clicked into "Non catégorisé" → confirmed the item + its price rendered underneath. Clicked "Prix à confirmer" → banner appeared immediately naming the product → persisted correctly through both store-selection steps → disappeared once a store was chosen → "Nom du produit" landed pre-filled with "Huile de friture" on the manual-entry form, confirming the full chain now reads as one continuous action instead of an unexplained jump.
- `npm run lint` and `npm run build` both clean.

## Accepted Risks & Frozen Dependencies

### Quagga CVEs — Do Not Auto-Fix
`quagga ≥ 0.7.0` carries transitive CVEs via its dependency chain:
`get-pixels` → `request` (abandoned) → `form-data`, `qs`, `tough-cookie`, `lodash`.

**These vulnerabilities are NOT exploitable in production.** All affected packages are Node.js server-side libraries. Vite excludes them from the browser bundle — they are never executed on the user's device. The app has no server process running Quagga.

**Do NOT run `npm audit fix --force`** to address these. Doing so installs `quagga@0.6.16` (a breaking API change) and risks breaking the iOS barcode scanner — a critical, hard-won feature that took multiple development iterations to stabilize.

**Planned**: Evaluate replacing `quagga` with `@ericblade/quagga2` (a maintained drop-in fork patching all CVEs) in a dedicated sprint with physical iOS device testing. This may also be superseded by a broader barcode scanner rework — keep open until direction is decided.

## Technical Details

### Tech Stack & Dependencies
- **React 19** / **Vite 7**
- **UI**: Tailwind CSS v3 (Utility classes only) + `tailwindcss-animate` (registered 2026-07-26 — see that date's entry; `animate-in`/`fade-in`/`zoom-in-*`/`slide-in-from-*` classes are used throughout the app and require this plugin to generate any CSS at all).
- **Icons**: `lucide-react`.
- **Charts**: `recharts` for price history trends.
- **Scanner**: `zxing/library` + `quagga`.
- **Analytics**: PostHog (`posthog-js`, `src/posthogClient.js`) — env vars `VITE_POSTHOG_KEY`, `VITE_POSTHOG_HOST`. See "Jul 26, 2026 — PostHog Product Analytics Instrumented" above for what's tracked.
- **Backend**: Supabase.
  - `supabaseClient.js` uses standard VITE_ env vars.

### PostHog Instrumentation Convention
Nothing is tracked automatically — PostHog only records what's explicitly wrapped in a `posthog.capture()` call. When building any new feature, identify its 2-4 meaningful user actions (not every click — the moments that answer "is this feature actually being used") and instrument them as part of the build itself, not as a follow-up pass. Follow the existing conventions: `snake_case` event names distinct per action (don't reuse one event string across unrelated actions — see the `'price_submission'` reuse wart flagged in the Jul 21, 2026 bug-sweep entry above, which made two different actions indistinguishable in the data), and verify events actually land with a direct PostHog SQL query against the raw `events` table before calling the feature shipped (same "verified live before shipping" convention used everywhere else in this file).

### Key Source Files
- `src/App10.jsx` — Main app entry point.
- `src/hooks/useShoppingList.js` — Shopping list state + Supabase sync logic.
- `src/hooks/useToast.js` — Toast notification hook.
- `src/contexts/AuthContext.jsx` — Auth state, points, profile, favorites.
- `src/components/ShoppingList.jsx` — Shopping list UI + basket price comparator.
- `src/components/StoreSelectionWizard.jsx` — Store selection flow.
- `src/components/BQPVerifier.jsx` — BQP product verification engine.
- `src/components/ZXingBarcodeScanner.jsx` — iOS/fallback barcode scanner.
- `src/components/Toast.jsx` — Toast notification UI.
- `src/components/ProductDetailModal.jsx` — Per-product "wow" card (scans, trend, cross-store comparison, comments, sharing).
- `src/components/PriceHistoryChart.jsx` — Shared recharts line chart (BQP scan flow + ProductDetailModal).
- `src/components/ProductCompletion.jsx` — Admin-only barcode + category correction (Console Admin "Compléter produit" sub-tab).
- `src/components/HunterDetailModal.jsx` — Per-contributor card from the Leaderboard (prices collected, shops visited, product list).
- `src/components/PriceDuel.jsx` — Martinique vs. France Hexagonale price comparison visual (`localPrice`/`mainlandPrice` props); used in both the barcode-scan flow (`App10.jsx`) and `ProductDetailModal.jsx`.
- `src/components/MainlandPriceAdmin.jsx` — Admin-only France Hexagonale reference price entry (Console Admin sub-tab).
- `src/components/RecipeDetailModal.jsx` — Recipe "wow" card: priced ingredient list, add-to-panier, "J'ai cuisiné" gamification hook, sharing.
- `src/components/RecipeAdmin.jsx` — Admin-only recipe + ingredient CRUD, incl. product search/associate (Console Admin "Recettes" sub-tab).
- `src/components/FeatureRequestDetailModal.jsx` — Suggestion detail card: full description, vote, official admin reply, comment thread (Communauté "Améliorations").
- `src/components/FeatureRequestAdmin.jsx` — Admin-only feature-request triage: set status, write the official reply (Console Admin "Suggestions" sub-tab).
- `src/components/MyScansModal.jsx` — "Mes Scans": a user's own submitted prices, most recent first, with quick favorite/add-to-panier actions (profile dropdown → "Mes contributions à l'effort collectif").
- `src/utils/userStats.js` — Shared `calculateSavings()`, single source of truth used by both `PersoStats.jsx` and `UserMenu.jsx`.
- `src/components/WantedScansModal.jsx` — "Prix recherchés": community-favorited products not yet priced at the user's favorite stores (profile dropdown).
- `src/utils/scanRequests.js` — `getWantedScans()`, backing `WantedScansModal.jsx`.
- `src/components/TestDataAdmin.jsx` — Admin-only test/real product flagging (Console Admin "Données test" sub-tab); products marked test are excluded from the end-user Comparer feed but never deleted.
- `src/components/UnmatchedItemsModal.jsx` — Panier's France Hexagonale comparison: lists which cart items lack a mainland price match and their combined value.
- `src/components/RecipesHubModal.jsx` — "Idées recettes" full-screen hub (Panier tab entry point): official admin-curated recipes + community-submitted recipe ideas (submit/like/favorite).
- `src/constants/mealCategories.js` — `MEAL_CATEGORIES` (Petit-déj' / Snack du matin / Déjeuner / Goûter / Dîner), shared between `RecipesHubModal.jsx`'s submission dropdown and filter chips; must match `community_recipe_ideas_migration.sql`'s CHECK constraint.
- `src/posthogClient.js` — PostHog init (product analytics).

### Database Schema (Critical Tables)
- `products`: Includes `is_local_production`, `is_mdd` (distributor brand), `barcode`.
- `prices`: Records current price, store association, and price tag photos. RLS enabled.
- `stores`: 78 Martinique supermarket locations, aligned with BQP 2025 documentation. RLS enabled (public read).
- `user_profiles`: Extends auth with `points`, `level`, `region_code`, and `is_diaspora`.
- `bqp_categories` & `product_bqp_associations`: Driving the BQP verification engine.
- `shopping_lists` + `shopping_list_items`: Cloud shopping list (one primary list per user).
- `recipes` + `recipe_ingredients` + `recipe_cooked_log`: Admin-curated recipes feature (see Jul 27, 2026 entry above). RLS enabled; schema + 10-recipe seed applied and verified live 2026-07-27.
- `feature_requests` + `feature_votes` + `feature_request_comments`: Communauté "Améliorations" suggestions, voting, and public comments (see Jul 28, 2026 entry above). RLS enabled; comments table + admin-update RLS fix applied and verified live 2026-07-28.
- `community_recipe_ideas` + `community_recipe_idea_likes` + `community_recipe_idea_favorites`: Lightweight user-submitted recipe ideas in the "Idées recettes" hub (see Aug 7, 2026 entry above). RLS defined in `community_recipe_ideas_migration.sql`; applied and read-path verified live 2026-08-07 — write path (submit/like/favorite as a logged-in user) still needs a real click-through.

### Environment Variables
Required in `.env.local`:
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`
- `VITE_POSTHOG_KEY`
- `VITE_POSTHOG_HOST` (e.g. `https://eu.i.posthog.com`)

### Aug 27, 2026 — Sept 1 Launch Prep: Rebrand, Banner Pill, Essentiel/Optionnel Form
Three changes requested ahead of the Sept 1, 2026 official launch, all in `App10.jsx` unless noted. Confirmed `App10.jsx` is still the ACTIVE version per the file table above before starting; per this project's established convention (every post-launch change since Feb 2026 has landed directly in `App10.jsx`, never bumping to `App11.jsx`), this was edited in place rather than versioned up — confirmed with Jean-Marie before starting since the request itself assumed a version-bump convention that doesn't match the actual history.

- **Rebrand**: replaced the three remaining literal "Vie chère en Martinique" strings in `App10.jsx` (header `<h1>`, Android install-prompt copy, welcome-overlay `<h2>`) with "Prix Martinique", and the header subtitle "Quid de votre pouvoir d'achat" with "Scanner · Comprendre · Résoudre". `index.html`'s `<title>` changed from the generic `prix-martinique` to `Prix Martinique`. **`public/manifest.json` and the PWA meta tags were already correctly branded** (`name`/`short_name` already "Prix Martinique"/"Prix MTQ", no ".org" anywhere) — no change needed there. Grepped the full codebase case-insensitively for "vie chère": all other hits are either the frozen `src/_archive/App.jsx`–`App9.jsx` files (untouched, per instruction) or generic uses of the phrase "la vie chère" (cost of living) in `AboutPage.jsx`/meta descriptions unrelated to the app's proper name — left as-is, since replacing those would change meaning, not branding. **Supabase email templates** ("Confirm sign up" / "Reset password" subjects, currently "... — Vie chère en Martinique" per the Jul 27, 2026 entry above) are dashboard-only, not in this repo — flagged as a manual follow-up for Jean-Marie.
- **Community count pill**: the standalone "43 prix partagés par la communauté" card (previously its own amber card above "Où êtes-vous ?") was removed, and its exact live source (`recentPrices.length` — unchanged) now renders as a fourth pill in the gradient banner's points/level row, alongside "★ 70 points" and "Niveau 1". The points/level pills are still gated on `user && userProfile`; the community pill is not, so logged-out visitors keep seeing it (previously always visible via the standalone card). Row given `flex-wrap` for narrow widths.
- **Essentiel/Optionnel form restructure**: the manual price-entry form (`#manual-entry-section`) is now grouped into a labeled "Essentiel" block (Code-barres → Prix → Photos → Catégorie, in that order) and a visually distinct "Optionnel" block (dashed border: Produit Local ? → MDD → Produit BQP ?), submit button after both. This **intentionally reverses** the Jul 27, 2026 fix that moved Prix to right after the photos — this pass's spec explicitly calls for Prix in 2nd position, immediately after the barcode field. Two fields outside the requested Essentiel/Optionnel lists — "Nom du produit *" (required product name) and "Votre nom" (optional contributor name) — aren't mentioned in the spec; left in their existing positions (Nom du produit right before the Essentiel block, Votre nom after Optionnel, both unchanged relative to before) rather than guessing a new placement. Pure reorder — no validation/required-field changes; confirmed `products`/`prices` required-field behavior wasn't touched.
- **Verified live** (local dev server, signed in as `JMP2_972`): confirmed the rebranded header/welcome-overlay/install-prompt text and the community pill rendering correctly alongside points/level with no leftover gap where the old card was. Completed a full real test submission end-to-end — "TEST QA Essentiel Optionnel Rebrand", 3.99€ at Carrefour Express FDF-Etang Z'Abricot, Produits Laitiers, Local + MDD checked, BQP toggle on — through the new barcode→prix→photos→catégorie order. Confirmed via direct Supabase REST queries (not just "no error shown"): `products.is_local_production`/`is_mdd`/`is_declared_bqp` all `true` with the correct `category_id`; the `prices` row has the correct price/store and `origin_region_code: null` (not mislabeled Hexagone); `product_bqp_associations` links to *this* product, not a stale one; points went 70→80 (+10) in the header. Celebration overlay rendered correctly with product name and points. `npm run lint` (via `node node_modules/eslint/bin/eslint.js .`, since `npm run lint` itself fails in this sandboxed shell — a `cmd.exe`-not-found environment quirk, not a project issue) and `npm run build` both clean.
- **Test data left behind**: `qa_cleanup_2026-08-27.sql` (new, **not yet applied**) removes the one real `products`/`prices`/`product_bqp_associations` row created above. Does not reverse the +10 points awarded to `JMP2_972`.
- **Not independently re-verified**: mobile-width wrapping of the 4-pill banner row beyond what a standard desktop-viewport screenshot shows — the `flex-wrap` class should handle narrow widths but wasn't spot-checked at a specific phone breakpoint this pass.
- **Code review follow-up (same day)**: a `/code-review` pass scoped to this PR's diff (`main...dev`) surfaced two real, non-blocking items. (1) Moving the community pill into the always-rendered header means it can briefly show "0 prix partagés" on any tab before `loadRecentPrices()` resolves, whereas the old standalone card was scoped to the Scan tab only — accepted as a minor, momentary cosmetic effect of relocating it as requested. (2) `resetForm()` (inside `submitPrice`) reset `isLocal` back to `false` after a submission but not `isMdd` — a pre-existing bug (not introduced by this diff, which didn't touch `resetForm`), but the new Essentiel/Optionnel grouping visually pairs the two toggles as symmetric siblings, making the asymmetry more likely to surprise someone. **Fixed**: added the missing `isMdd: false` to `resetForm()`. `npm run lint` / `npm run build` re-verified clean after the fix.

_(The "Open Items" list has moved to the end of this file — see the bottom, just above the "Last Updated" footer.)_

### Aug 28, 2026 — Public Launch Prep: Anonymous-Submission Fix, Security Headers
First two items off the public-launch security/hardening plan (moving from the vercel.app testing URL to the custom domain, opening the app to real end users instead of known testers).

- **Anonymous price submission now gated behind sign-in, for real** — the Jul 27, 2026 entry above already found that `submitPrice` claimed "Merci pour votre contribution !" for anonymous users while RLS actually 401s that insert (products/prices require the `authenticated` role since the Feb 28, 2026 audit); it was flagged then but deliberately not fixed. Decision made explicitly this time (RPPRAC/carnival material was a distraction from this, now back to it): require sign-in, matching the Aug 7, 2026 precedent (`handleAddToShoppingListFromComparer`'s guard) — rather than opening a new anonymous-insert RLS policy right as the app goes public and the abuse surface widens. `submitPrice` now checks `!user` first and opens the sign-in modal instead of proceeding.
- **Second copy of the same bug found and fixed while in this code**: the barcode-scan "C'est le même" quick-confirm button (a second, independent price-insert path, `App10.jsx`) had the identical unguarded `supabase.from('prices').insert()` call — for an anonymous user this failed with the same RLS 401, but silently: no toast, no success message, nothing happened (worse than the manual-entry path, which at least showed a false success). Fixed with the same guard. `user_id: user?.id` → `user.id` and `if (user) await awardPoints(...)` → unconditional, since `user` is now guaranteed past the guard.
- **Left alone, deliberately**: the celebration overlay's `points > 0 ? ... : 'Merci pour votre contribution !'` fallback branch. Checked `performPriceSubmission` (`priceSubmission.js`) first — `pointsAwarded` initializes to `0` and only becomes `10` if the `award_points` RPC call itself succeeds, so this branch isn't anonymous-only dead code: it's the correct fallback for a signed-in user whose price saved but whose points-award call transiently failed. Removing it would have silently hidden that real edge case.
- **Security headers added to `vercel.json`** (previously only had COEP/COOP): `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Strict-Transport-Security` (2-year max-age + includeSubDomains, no `preload` yet — that's a much harder-to-reverse commitment, left for later once the domain cutover itself is confirmed stable), and `Permissions-Policy` explicitly scoping `camera=(self)`/`geolocation=(self)` (both genuinely used) and denying microphone/payment/usb/interest-cohort.
- **CSP shipped as `Content-Security-Policy-Report-Only`, deliberately not enforced yet.** The barcode scanner (camera + WASM via `zxing-wasm`, plus `quagga` as the iOS/Safari fallback) is explicitly flagged elsewhere in this file as a fragile, hard-won feature — an over-strict enforced CSP could silently break scanning with no visible error, which is a worse outcome than the current unhardened-but-working state. Report-Only mode surfaces violations in the browser console without blocking anything, so it can be checked against a real device (including iOS Safari specifically, since that's Quagga's target) before flipping to enforced. Policy allows `'self'` + `'wasm-unsafe-eval'` + PostHog (`eu.i.posthog.com`, `eu-assets.i.posthog.com`) for scripts, `worker-src 'self' blob:` for whatever workers Quagga/zxing spin up internally, `connect-src`/`img-src` covering Supabase (`https://*.supabase.co` and `wss://*.supabase.co` for realtime) — not yet verified against a live deployment.
- **Not done yet, next on the launch-prep list**: the actual domain cutover (Vercel domain add + DNS + Supabase Auth redirect URLs + Google OAuth console update), Supabase Pro upgrade (free tier has zero automated backups — flagged as the highest-leverage data-safety gap), storage bucket size/MIME limits, basic submission rate-limiting, and confirming Mentions légales / Politique de confidentialité exist somewhere before real EU users start creating accounts.
- `npm run lint` / `npm run build` both clean.

### Aug 28, 2026 (2) — Real iOS Test Session: Google OAuth Display-Name Bug, Lost Submission on Sign-In Redirect
First real end-to-end test on a physical iPhone (screen-share video call), following the anonymous-submission gate shipped earlier the same day. Found two genuine, previously-invisible bugs — both traced to root cause via a live query against the actual test row, not guessed.

- **Root cause: every Google OAuth sign-up has had `display_name` permanently NULL, since the feature was built.** `handle_new_user()` (`gamification_schema.sql`) only ever read `raw_user_meta_data->>'display_name'` — a key the app itself only sets during email/password `signUp()`. Google OAuth via Supabase never populates that key (it uses `full_name`/`name` instead), so `display_name` silently stayed NULL for every Google account that has ever signed up. This single NULL cascaded into two visible symptoms during the test: the "Votre nom" field never disabled/prefilled for a signed-in Google user (its condition also keys off `display_name`), and the price ended up saved as `user_name: "Anonyme"` **despite `user_id` being correctly set to the real authenticated account** — confirmed by querying the actual test row directly (`Pâtes 500g panzani`, 1.50€): `user_id` was a real UUID, `user_name` was the literal string "Anonyme". That mismatch is exactly the data-quality/traceability gap flagged when the anonymous-submission gate went in earlier the same day — an admin skimming the feed would wrongly conclude no real identity was behind that contribution.
  - **Fixed**: `display_name_google_oauth_fix_migration.sql` (new, **not yet applied**) — `handle_new_user()` now coalesces `display_name` → `full_name` → `name` → the email's local-part, covering both signup paths and guaranteeing a non-null result going forward. Includes a one-time backfill for every existing NULL `display_name` (i.e. every Google sign-up to date) pulled from the same `auth.users` metadata, plus a sanity-check query. `set search_path = public` added while touching this `security definer` function, per the Feb 28, 2026 audit's established pattern.
  - **Defense in depth, `priceSubmission.js`**: `user_name` no longer falls straight to the literal `'Anonyme'` when the free-text field is empty — it now prefers `userProfile.display_name`, then `user.email`, before ever reaching the generic label. The same fix applied to the "C'est le même" quick-confirm insert in `App10.jsx`. This means a signed-in user's row can never again show "Anonyme" purely because a text field happened to be blank, independent of whether the DB migration above has been run yet.
- **Root cause: Google sign-in is a full-page redirect, not an in-app popup — the whole app (and all its React state) reloads when it returns from accounts.google.com.** This is why the friend's first scan attempt "disappeared" after the anonymous-submission gate correctly blocked it and offered sign-in: completing Google auth reloaded the page, wiping the in-progress form (photos, price, product name, chosen store) with nothing to show for it, forcing a full redo from scratch. The same reload is the most likely cause of the reported double geolocation prompt — `StoreSelectionWizard` requests GPS in a mount effect, and the page reload after the OAuth round-trip remounts it fresh.
  - **Fixed**: `submitPrice`'s auth guard now snapshots `manualEntry` to `sessionStorage` (`PENDING_SUBMISSION_KEY`) before opening the sign-in modal, with a graceful fallback to a photo-less snapshot if the quota is exceeded. A new effect restores it once `user` resolves after the reload and flags `pendingAutoSubmit`; a second effect fires `submitPrice()` automatically once that restored state has actually landed. The user now returns from Google sign-in straight into the completed submission (celebration overlay, points awarded) instead of an empty form.
  - **Not fully solved, flagged honestly**: this does not eliminate the second geolocation prompt outright. `StoreSelectionWizard` still mounts for one render before the restore effect can run and flip `manualEntry.storeId` to a restored value (React effects run after the first commit, not before it) — so `attemptGPSDetection` likely still fires once more on that reload. It unmounts again quickly once restored rather than trapping the user in a full re-selection, which is the main practical improvement, but the permission-prompt flash itself may not be fully gone. Also plausibly compounded by an iOS Safari/WebKit quirk where installed PWAs re-prompt for geolocation on each "cold launch" regardless of a prior grant — a platform limitation outside this codebase's control if so. Worth specifically re-checking on the next real-device pass.
  - **Scope note**: the persist/resume fix covers the main manual-entry `submitPrice` flow only, not the separate "C'est le même" quick-confirm insert (which doesn't carry enough state to usefully resume) — that path still just gates on sign-in without auto-resuming.
- **Also fixed**: the "Connectez-vous" banner at the top of the Scan tab previously undersold sign-in as an optional bonus ("gagnez des points et badges!"). It now states upfront that an account is required to save a price at all, before the user invests effort in photos — while noting the flow still works and auto-completes if they scan first and sign in when prompted, thanks to the persist/resume fix above.
- **Confirmed working, no action needed**: PWA install-to-home-screen on iOS Safari.
- `npm run lint` / `npm run build` both clean.
- **Test data left behind**: the real "Pâtes 500g panzani" (1.50€) row from this test session is genuine product data, not prefixed `TEST` — left in place rather than cleaned up, unlike the usual `qa_cleanup_*.sql` convention for artificial test rows.

### Aug 28, 2026 (3) — Second Real iOS Test: Duplicate-Email Signup Silently "Succeeds", Camera Re-Prompt Investigated
Second real-device test (screen-share on a Google Meet call), after the fixes above went live.

- **Root cause, confirmed against Supabase's own documented behavior: `signUp()` does not throw an error when the email already has an account (via any provider).** By design, to prevent email-enumeration attacks, Supabase instead returns a fake, session-less user object with an empty `identities` array and sends no email at all. `AuthModal.jsx`'s signup handler only ever checked `if (error) throw error` and never looked at `data`, so it always showed "Compte créé ! Vérifiez votre email..." regardless — exactly matching what was reported: a user who already had a Google account tried email/password signup with the same address, got no error, no email, and no indication anything was wrong.
  - **Fixed**: `handleSubmit` now checks `data?.user?.identities?.length === 0` and shows a distinct message ("Aucun email n'a été envoyé : un compte existe déjà avec cette adresse...") pointing the user to sign in or reset their password instead, with a longer 6s auto-dismiss (vs 3s for genuine signups) given the longer text.
  - **Deliberate security tradeoff, flagged rather than silently made**: this message does directly confirm the email is already registered, which is the exact disclosure Supabase's API-level design avoids. Judged acceptable here because (a) it's scoped to signup only — this app's password-reset flow (`resetPasswordForEmail`) already correctly shows the same generic "a link was sent" message regardless of whether the account exists, which is the higher-stakes place to avoid leaking this, and (b) confirming email-exists-at-signup is a common, broadly-accepted pattern (Google, Facebook, etc. all do this) — the alternative (silently pretending success, as before) was actively harmful UX with no real user ever reaching this exact page to exploit enumeration having been a concern to date. Worth revisiting if that judgment call turns out wrong.
- **Camera permission re-prompting on every scan, investigated, not yet fixed.** `ZXingBarcodeScanner.jsx`'s `stopScanning()` fully calls `track.stop()` on every close (including after every successful detection and every manual close) — standard, correct practice for releasing the camera. Combined with the parent (`App10.jsx`) conditionally *unmounting* the whole scanner component between scans (`{showScanner && <ZXingBarcodeScanner/>}`) rather than just hiding it, every single scan is a fresh `getUserMedia()` call from a fully torn-down state. This is the same failure class as the geolocation double-prompt above (Aug 28 (2) entry): iOS Safari/WebKit is known to be materially less reliable than desktop/Android Chrome at persisting a camera-permission grant across discrete `getUserMedia` sessions, especially for installed standalone PWAs — plausibly re-confirming on every fresh request regardless of a prior "Allow."
  - **Not fixed this pass, deliberately** — the real fix (keeping the camera stream alive across scan-close/reopen cycles within one Scanner-tab session, rather than tearing it fully down each time) means restructuring how `ZXingBarcodeScanner` mounts relative to its parent, which touches the actual live camera pipeline. This component is explicitly flagged elsewhere in this file as fragile and hard-won ("took multiple development iterations to stabilize... do NOT risk breaking") — scoped out of this pass rather than rushed, pending a decision on whether the tradeoff (camera indicator staying active longer between scans) is acceptable.
- `npm run lint` / `npm run build` both clean for the signup fix.

### Aug 28, 2026 (4) — Meeting Transcript Review: Photos Lost on Resume-After-Signin (Root Cause of the "Nutella" Bug)
Reviewed a Gemini-generated transcript of the test session behind the two entries above before committing anything, per request. Confirmed via the transcript and live data: the anonymous-submission gate and the display_name fix are both working correctly in production (`user_name` on new submissions now shows the real name, e.g. "Maelys Philocles", not "Anonyme"; the app correctly refuses to save without signing in). The transcript's own note that geolocation "n'est plus redemandée" is a good sign for the Aug 28 (2) persist/resume fix, though still only one test session's worth of evidence.

One genuinely new finding, which the transcript explicitly flagged for follow-up ("Conservation du bug Nutella pour diagnostic" — the product was deliberately left in place, not cleaned up, so this could be root-caused): a Nutella product registered via the anonymous-scan-then-Google-signin flow ended up with **both** `product_photo_url` and `price_tag_photo_url` null. Confirmed live against the actual row.

- **Root cause: the Aug 28 (2) persist/resume fix stored the pending submission snapshot in `sessionStorage`, which has a much smaller per-origin quota than IndexedDB** — often as low as ~5MB on iOS Safari. A full-size camera photo as base64 can approach or exceed that on its own; two of them easily can. The fallback code deliberately degraded to a photo-less snapshot rather than losing the submission entirely when the quota was hit, but did so silently — exactly what most likely happened here.
  - **Fixed**: replaced the `sessionStorage`-based snapshot with a new dedicated IndexedDB store (`pending_auth_submission` in `offlineDb.js`, `savePendingAuthSubmission`/`getPendingAuthSubmission`/`clearPendingAuthSubmission`), reusing the same `idb`-backed infrastructure the offline submission queue already relies on successfully for payloads containing base64 photos. Kept as its own object store rather than folding into `pending_price_submissions`, so the offline-sync drainer (which only knows how to retry actual queued *submissions*) never mistakes a "waiting on sign-in" snapshot for a "waiting on network" one. `DB_VERSION` bumped 1 → 2 for the new store to actually get created on existing installs.
- **Also hardened, independent of the above**: `performPriceSubmission` previously swallowed a genuine photo-upload failure completely silently (caught, logged to PostHog, but never surfaced) — meaning even a normal, fully-online submission with no auth/quota involvement could end up with a missing photo and no indication why, which is plausible for the *second* Nutella entry (created ~5 minutes later, likely already authenticated) on a phone the transcript itself describes as heavily loaded ("ralentissements importants") during a live screen-share call. `performPriceSubmission` now returns `photosDropped` (which photo(s) failed, if any), and `submitPrice` shows an explicit toast ("...une photo n'a pas pu être envoyée...") when that happens, without blocking the price itself from saving.
- **Treated with appropriate skepticism, not chased further**: the transcript also describes the first signup attempt as returning "une erreur de mot de passe incorrect ou subit un plantage." This doesn't match Supabase's documented `signUp()` behavior (which returns no error at all for a duplicate email, per the Aug 28 (3) entry above) or any code path found in this codebase — most likely either a Gemini transcription imprecision (the notes themselves carry Google's own "verify for errors" disclaimer) or a conflation with the separately-reported phone slowdown. Not acted on without a reproducible case.
- **Left as-is, flagged for a decision**: the two "Nutella 220g" product rows created during this test (barcodes `80861263` and `20867263`, both real but photo-less) — unlike the usual `qa_cleanup_*.sql` convention, these weren't named with a `TEST` prefix and were explicitly kept in place for this diagnosis. Worth a decision on whether to delete them or leave them now that the root cause is understood.
- `npm run lint` / `npm run build` both clean.

Follow-up: the two Nutella rows above were deleted from production directly via the Supabase SQL Editor (not through the app) once this diagnosis was confirmed.

### Aug 28, 2026 (5) — Anton Wordmark Banner + "Actions par la transparence" Tagline
Cosmetic header pass (`69926c4`), committed between the transcript-review work above and the camera-mount fix below. Matches the "Prix Martinique — Mode d'emploi" instruction-manual title treatment.

- **`App10.jsx` header**: the plain `<h1 className="text-xl font-bold">Prix Martinique</h1>` became a two-line uppercase "stamp" (`font-stamp uppercase leading-[0.92] tracking-wide text-3xl sm:text-4xl`, rendered as `Prix<br />Martinique`), and a third line was added under the existing "Scanner · Comprendre · Résoudre" subtitle: `Actions par la transparence` (small, mono, letter-spaced). The header container switched `items-center` → `items-start` to sit the taller wordmark against the `UserMenu` cleanly.
- **`tailwind.config.js`**: `theme.extend.fontFamily.stamp = ['Anton', 'Arial Narrow', 'sans-serif']` — first real `theme.extend` entry in this project besides the plugin registration.
- **`index.html`**: added `<link>`s for Google Fonts (`preconnect` to `fonts.googleapis.com` / `fonts.gstatic.com`, then the `Anton` stylesheet). **This is the app's first external-origin asset dependency** — every other font/asset is bundled or same-origin. **CSP impact**: the `Content-Security-Policy-Report-Only` policy shipped in the Aug 28 (1) entry does *not* whitelist these hosts, so `style-src`/`font-src` must be extended to allow `https://fonts.googleapis.com` (stylesheet) and `https://fonts.gstatic.com` (font files) **before** the CSP can ever be flipped from Report-Only to enforced — otherwise the wordmark silently falls back to `Arial Narrow`/sans-serif. Logged as an Open Item below.
- No functional/logic change; `npm run lint` / `npm run build` clean.

### Aug 28, 2026 (6) — Camera Re-Prompt: Found a Bigger Bug Than the One Diagnosed
Went back to actually fix the Aug 28 (3) camera re-prompt finding. Before touching the stream-teardown behavior flagged there, re-read `App10.jsx`'s render tree end to end and found something more fundamental: **`ZXingBarcodeScanner` was mounted twice at once.** One copy rendered unconditionally near the top of `App10`'s JSX (`{showScanner && <ZXingBarcodeScanner .../>}`, right after `OfflineBanner`), and a second, separately-scoped copy rendered inside the Scan tab's own UI (`activeTab === 'scan'` block, gated additionally on a store being selected). Grepped the whole file for `setShowScanner(true)` — there is exactly one call site, inside the Scan-tab-only block — so `showScanner` can only ever be `true` while the Scan tab is already active, meaning both copies were *always* live together, never just one.

That means every scan was actually two independent `ZXingBarcodeScanner` instances each calling `getUserMedia()` and each running their own `stopScanning()`/`track.stop()` teardown on close, racing each other over the same camera hardware. This is a strictly more direct explanation for erratic camera/permission behavior than "stream torn down between scans" alone (which is otherwise unremarkable, expected `getUserMedia` hygiene) — two competing acquire/release cycles on one physical camera is exactly the kind of thing that can make WebKit re-confirm a permission it already granted.

- **Fixed**: removed the redundant unconditional render at the top of `App10.jsx`; the Scan-tab-scoped instance (the one actually wired into the real UX — store selection, `scan_session_started` analytics, its own close button) is now the only one that ever mounts.
- **Deliberately not done in this pass**: the originally-diagnosed fix (keeping the camera stream alive *across* scans instead of tearing it down after every detected barcode) still hasn't been attempted. That change means the camera stays visibly active while the user fills in the price form for the item just scanned — a real UX/privacy tradeoff, not just a bug fix — so it's being held pending a real-device re-test of this simpler fix first, per this project's "verify live before shipping" convention. If duplicate-mount was the actual root cause, that bigger change may not be needed at all.
- `npm run build` clean.

### Aug 28, 2026 (7) — Admin Dashboard: Platform, PWA-vs-Browser, and Login-Method Breakdowns
Requested: track (1) scans/usage split iOS vs Android, (2) sign-ins split email+password vs Google, (3) installed-PWA usage vs browser usage, visible in the in-app admin console (not just the external PostHog link).

No backend or read API key exists to query PostHog server-side (the exact constraint already documented at the top of `AdminDashboard.jsx` for why its "Analytics" tab links out instead of embedding), so all three are captured directly into Supabase, the same way every other admin stat in this app already works — `fetchAdminStats` fetch-all + client-side `reduce` into counts, no RPC/view needed at this data volume.

- **New tables** (`analytics_tracking_migration.sql`, not yet applied — needs to be run via the Supabase SQL Editor):
  - `app_sessions`: one row per browser-tab session (deduped via a `sessionStorage` flag, not per component mount/re-render — matters because `<StrictMode>` double-invokes effects in dev), logged on every app load regardless of auth state via `logAppSessionOnce()` in the new `utils/sessionTracking.js`, called once from `App10.jsx`'s top-level mount effect. Captures `device_platform` (`ios`/`android`/`other`, via UA sniffing in the new `utils/deviceDetection.js`) and `display_mode` (`standalone`/`browser`, via `navigator.standalone` / the `display-mode: standalone` media query). Insert open to anyone (including logged-out visitors); select restricted to admins via the existing `user_roles` check pattern (see `barcode_audit_migration.sql` for precedent).
  - `auth_events`: one row per **genuine** sign-in — deliberately not every `SIGNED_IN` event, since Supabase fires that both for an actual new login and for an ordinary page reload that merely restores an existing session from storage. Reused the same trick already in this file for `isLikelyNewOAuthSignup` (comparing timestamps) to add `isFreshSignIn()`: `last_sign_in_at` within the last 15s of "now" means a sign-in just happened; anything older means the session was just restored. `logAuthEvent()` reads the provider straight off `session.user.app_metadata.provider` (Supabase sets this correctly regardless of whether the account was created via email or Google), so it doesn't need to duplicate the signup-vs-login distinction the existing code already tracks separately.
  - Both tables' insert calls initially used `await supabase.from(...).insert(...)` without checking the returned `error` — copy-pasted oversight, but worth calling out because Supabase JS **does not throw on a DB-level error** (like "table doesn't exist"); it resolves normally with `{ error }` populated. The surrounding `try/catch` therefore silently swallowed nothing when the tables didn't exist yet during testing, since no exception was ever thrown to catch. Fixed by explicitly checking `if (error) throw error` before the catch block, matching the pattern used everywhere else in this codebase.
- **AdminDashboard.jsx**: new "Appareils & Connexion" section (Overview tab) with a reusable `BreakdownBar` component — a stacked percentage bar plus a count/percentage legend, used for all three metrics (platform, display mode, auth method) rather than three different one-off visualizations.
- **Scoping decision, confirmed with the app owner rather than assumed**: the PWA-vs-browser metric is a session count (one row per app load, whether or not that visit leads to a scan), not tied to the `prices` table the way most other admin stats are. Chosen over scan-scoped tracking because it answers "how are people accessing the app" more directly than "what fraction of *contributions* come from the installed app."
- **Not yet live**: the migration hasn't been run against production yet, so all three new breakdown cards will show "Pas encore de données" until it is. `npm run build` / lint both clean against the pre-migration state (inserts fail gracefully, logged to console, without affecting the rest of the app).

### Aug 29, 2026 — RGPD Pre-Launch: Privacy Policy, Mentions Légales, Cookie Consent Gate
Pre-launch compliance pass (target launch: Sep 1, 2026). Checked what already existed: Supabase is EU-hosted (`eu-west-1`, Ireland), PostHog is on the EU cloud, and Supabase's DPA is now built into their ToS — infrastructure was fine. The app itself had zero privacy policy, zero mentions légales, zero cookie consent, and no self-service account deletion/export (that last one is still an open gap, deferred — see below).

- **New `src/components/LegalModal.jsx`**: a single reusable modal (`type: 'privacy' | 'legal'`) rendering both documents' full text, used from two places rather than duplicating the content — the Info tab (`AboutPage.jsx`, plain-text footer links) and the signup form (`AuthModal.jsx`, a consent line under the submit button, since GDPR consent needs to be informed *at the point of collection*, not three taps away in an Info tab). `z-[510]` so it stacks above every other modal in the app (`AuthModal` is `z-[400]`, the welcome overlay is `z-[500]`).
- **Content specifics**: the app owner clarified mid-draft that Prix Martinique is a module of a larger project, Vwa Pèp La (association "en cours de constitution" — not yet legally registered), based temporarily in Zurich rather than France/Martinique pending the association's own registration address. Both documents name Jean-Marie Philocles / Association Vwa Pèp La as the responsible party, with the Zurich address explicitly marked temporary and `vwapepla.org@gmail.com` as the single contact address throughout (replacing an earlier draft that used a personal Gmail address).
- **New `src/components/CookieConsentBanner.jsx`** + `src/posthogClient.js` now sets `opt_out_capturing_by_default: true`: nothing (autocapture, `identify()`, exception tracking) is captured until the user explicitly accepts. Every existing `posthog.*` call site across the codebase (AuthContext's `identify`/`capture` calls, the various `posthog.capture()` events in App10.jsx) needed zero changes — posthog-js's own opt-out gate silently no-ops all of them until consent is given, rather than needing per-call-site guards.
  - **Real bug found and fixed while verifying this live**: the banner's first version used `!posthog.has_opted_in_capturing() && !posthog.has_opted_out_capturing()` to decide whether to show. `has_opted_out_capturing()` can't distinguish "opted out by the `opt_out_capturing_by_default` config" from "user explicitly clicked decline" — both set the same internal flag — so it read `true` from the very first page load, before anyone had made a choice, hiding the banner permanently. Fixed by tracking "has this user been asked" in its own `localStorage` key (`pm_cookie_consent_choice`), independent of PostHog's internal state. Confirmed live: banner now shows on a fresh browser, and accept/decline both dismiss it and don't reappear on reload.
- **New "Nous contacter" entry point**: a plain `mailto:` link (`src/constants/contact.js` centralizes the address) added as a card on the Info tab and inside both `LegalModal` documents' final section — opens the visitor's own default mail app pre-addressed and pre-subjected, no in-app form or backend needed to receive messages.
- **Deliberately not done in this pass**: self-service account deletion/data export (GDPR Articles 17/20) — the privacy policy points users to the contact email for these requests as a documented manual process, but nothing is automated yet. Flagged as a post-launch follow-up, not a hard blocker for Sep 1.
- `npm run build` / lint both clean.

### Aug 29, 2026 (2) — GDPR Follow-Up: Data Export + Admin-Mediated Account Deletion
Built the two items deferred in the previous entry. Export is fully self-service; deletion deliberately isn't, because it can't be — deleting the actual `auth.users` row requires `supabase.auth.admin.deleteUser()`, which needs the service-role key, and that key must never reach the browser. GDPR Article 17 only requires acting "without undue delay" (max one month, extendable), not instant automation, so an admin-mediated flow is compliant, not just a shortcut.

- **Data export** (`utils/dataExport.js`, wired into `PersoStats.jsx`'s new "Confidentialité & mon compte" section): pure self-service, no new infrastructure — every query reads only the caller's own rows under the same RLS policies that already let a signed-in user see their own favorites/prices/etc. today. Downloads a JSON file client-side (`Blob` + `URL.createObjectURL`), covering profile, price submissions, badges, favorites, shopping lists, and recipe ideas.
- **Deletion request flow**:
  - `deletion_requests_migration.sql` (not yet applied): `user_id` is `ON DELETE SET NULL`, not `CASCADE` — the audit row (who requested what, when it was completed, by which admin) must survive the account it refers to being deleted, which is exactly the row a `CASCADE` would destroy. `user_email` is snapshotted at request time for the same reason.
  - `PersoStats.jsx`: a "Supprimer mon compte" button with an inline two-step confirm (no native `window.confirm`, matches the rest of the app's style) inserts a `pending` row the user owns under RLS. If a pending request already exists, it's shown as a status banner instead of the button, so a user can't queue duplicate requests.
  - `components/DeletionRequestsAdmin.jsx` (new AdminDashboard sub-tab, "Suppressions"): lists pending and completed requests. "Traiter la suppression" calls the Edge Function below via `supabase.functions.invoke()`; a completed row gets a "Envoyer confirmation" `mailto:` link, pre-filled — same pattern as the "Nous contacter" link, deliberately not a real transactional-email integration, since this is a low-volume, already-admin-mediated flow and adding an email-sending service just to send one confirmation per deletion isn't worth the extra infrastructure right now.
  - `supabase/functions/delete-user-account/index.ts` (new — **not deployed yet**, no Supabase CLI available in this environment to do it from here): re-verifies the caller is an admin server-side (never trusts the client-side admin check alone), anonymizes the target's `prices` rows (`user_id` → null, `user_name` → "Utilisateur supprimé" — prices stay, since they're collective market data and the whole point of the app), marks the `deletion_requests` row completed, then calls `auth.admin.deleteUser()`. Everything else referencing `auth.users(id)` with `ON DELETE CASCADE` (favorites, shopping lists, badges, recipe ideas) is cleaned up automatically by that last step — only `prices` needed explicit anonymization.
- **To go live**: run `deletion_requests_migration.sql` in the SQL Editor, and deploy the Edge Function — either paste `supabase/functions/delete-user-account/index.ts` into the Supabase Dashboard's Edge Functions editor directly, or via CLI (`supabase link --project-ref euqqxictzvyszjzeejsz && supabase functions deploy delete-user-account`). `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` are injected automatically into the function's environment either way, no manual secret setup.
- `npm run build` / lint both clean.
- **Follow-up, same day**: both the migration and the Edge Function were applied/deployed live via the Supabase Dashboard's SQL Editor and Edge Functions code editor respectively (pasted via clipboard write + Ctrl+V into the Monaco editor, not typed character-by-character, to avoid the same auto-indent/bracket-closing garbling risk documented elsewhere in this file for GitHub's PR-description editor). Confirmed live: `deletion_requests` already existed (migration had been run), function deploys and shows at `https://euqqxictzvyszjzeejsz.supabase.co/functions/v1/delete-user-account`.

### Aug 29, 2026 (3) — Custom Domain Prep: New-Domain Banner for Stranded PWA Installs
Purchasing `prix-martinique.org` (via Vercel's own registrar — cheapest of Vercel/Hostinger/Hostpoint on both first-year and, more importantly, renewal price, plus zero-DNS-config auto-attach since the app is already hosted on Vercel) surfaced a real migration wrinkle: a PWA "installed to home screen" is bound to the exact origin it was installed from. Setting the custom domain as primary in Vercel makes ordinary browser visits to the old `*.vercel.app` URL redirect automatically, but an already-installed PWA's cache-first service worker can serve its cached app shell straight from cache without ever making the network request that redirect depends on — so installed users would silently keep using the old origin forever with no signal anything changed.

- **New `src/components/NewDomainBanner.jsx`**: renders only when `window.location.hostname.endsWith('.vercel.app')` — i.e., only for visitors still on the temporary origin, which after the redirect is live should mostly mean exactly the stranded-PWA-install case this exists for. Dismissible, persisted via its own `localStorage` key, same pattern as `CookieConsentBanner`.
- Mounted at the very top of `App10.jsx`'s render, above `OfflineBanner`.
- Deliberately not done: no mass-email/push-notification system exists in this app to actively notify already-registered users; the app owner will send a one-time manual email (BCC) to current testers separately, outside the codebase.
- `npm run build` / lint both clean.

### Aug 31, 2026 — CLAUDE.md Audit; Pending Migrations + Supabase Pro Applied
Doc-maintenance + verification pass, no app code changed.

- **CLAUDE.md brought current**: added the missing entry for `69926c4` (Anton wordmark banner + "Actions par la transparence" tagline — Aug 28 (5); renumbered the following two Aug 28 entries to (6)/(7)), moved the stranded mid-file "Open Items" list and "Last Updated" footer to the end, and rebuilt Open Items around the Sept 1 launch. Committed as `a77485c` on `dev` (only commit ahead of `main`).
- **Corrected a stale-state error**: an earlier draft of the Open Items claimed nine feature commits were unmerged and missing from production — that was a stale local `origin/main` ref. After `git fetch`: all Aug 28–29 work was merged via PRs #14–#19, `origin/main` is at `fb8914b`, production has everything.
- **Three pending DB migrations applied by Jean-Marie and verified live** (single SQL query against production via the dashboard SQL editor): `recipes_photo_urls_migration.sql` → 10/10 recipes now serve `/recipes-pictures/<file>`; `display_name_google_oauth_fix_migration.sql` → `count(user_profiles where display_name is null)` = 0 (backfill ran; `handle_new_user()` now coalesces `full_name`/`name`); `analytics_tracking_migration.sql` → `app_sessions` + `auth_events` tables exist (admin "Appareils & Connexion" cards will fill in as traffic accrues).
- **Supabase Pro upgrade** done — org plan badge shows PRO, so automated backups are now active (the "highest-leverage data-safety gap" flagged repeatedly above is closed).
- **Remaining before Sept 1** (as of this entry): two `qa_cleanup_*.sql` files; then the launch-week hardening list (CSP enforce + Google-Fonts allowance, email-template rebrand, storage limits, rate-limiting). See Open Items below. (The domain cutover for `prix-martinique.org` — previously the top launch blocker — was completed and verified same day; see the entry below.)

### Aug 31, 2026 (2) — Domain Cutover to `prix-martinique.org` Completed + Verified
Ran the cutover milestone (Open Item #1) end to end via the Vercel / Supabase / Google Cloud dashboards. Found most of it had **already been done** — likely by Jean-Marie or an earlier session — so this pass was mostly live verification plus one missing piece (`www`).

- **No app code change was needed.** `signInWithGoogle` and `resetPasswordForEmail` both build `redirectTo` from `window.location.origin` (`AuthContext.jsx`), share links use `window.location.origin`, `public/manifest.json` is all relative paths, and `vercel.json` has no hardcoded origin — the app is fully origin-agnostic. `NewDomainBanner.jsx` already handles stranded `*.vercel.app` PWA installs.
- **Vercel — already done**: `prix-martinique.org` added, `Valid Configuration`, connected to Production, and it's the project's primary production domain (shown as the project domain on the dashboard overview). `mvp1-prixmartinique.vercel.app` still serves Production directly (no redirect) — deliberate, so stranded PWA installs keep working and see `NewDomainBanner` ("nouvelle adresse officielle : prix-martinique.org"). Confirmed live: the app loads on `https://prix-martinique.org` with a valid cert, and the vercel.app URL still serves with the banner showing.
- **Vercel — added this pass**: `www.prix-martinique.org` → **308 Permanent Redirect** → `prix-martinique.org` (it was previously unconfigured and returned a browser error page). DNS auto-attached (Vercel registrar); shows `Valid Configuration`. Verified live: visiting `https://www.prix-martinique.org` lands on `https://prix-martinique.org/` with the app rendering normally.
- **Supabase Auth — already done**: Site URL is `https://prix-martinique.org` (saved). Redirect allow-list (6 entries) includes `https://prix-martinique.org` and `https://prix-martinique.org/*`, with the `http://localhost:5173` and `https://mvp1-prixmartinique.vercel.app` entries retained.
- **Google Cloud OAuth — no change needed, verified.** Started the Google sign-in flow from `https://prix-martinique.org` and it reached Google's "Choose an account" screen ("to continue to euqqxictzvyszjzeejsz.supabase.co") with **no `redirect_uri_mismatch`**. The OAuth `redirect_uri` is the Supabase callback (`https://euqqxictzvyszjzeejsz.supabase.co/auth/v1/callback`), which is domain-stable — the app talks to Supabase, not Google directly — so the app-domain switch never touched it. The Open Item's worry about "Google authorized redirect URIs" was a false alarm for this Supabase-brokered setup. The app-side `redirect_to=https://prix-martinique.org` param is validated by Supabase against its allow-list (already covered, above).
- **Still yours to do** (a real login can't be automated from here): one real end-to-end pass on `prix-martinique.org` — a Google sign-in, an email/password sign-in, and a password-reset email — confirming the reset link lands on `prix-martinique.org`, not the vercel.app origin.

### Sept 1, 2026 — Launch day: QA cleanup rows applied
- **`qa_cleanup_2026-08-07.sql`** and **`qa_cleanup_2026-08-27.sql`** run by Jean-Marie via the Supabase SQL Editor. Both are plain `DELETE`s scoped by `name/title ilike 'TEST QA …%'` — removed the `"TEST QA Jus de fruits de la passion"` `community_recipe_ideas` row (Aug 7 write-path test) and the `"TEST QA Essentiel Optionnel Rebrand"` `products` row + its `prices` + `product_bqp_associations` rows (Aug 27 launch-prep test). Neither reverses the small point awards to `JMP2_972` from those sessions (test account, left as-is). File headers updated with the `Status: APPLIED 2026-09-01` line to match the convention on the three older `qa_cleanup_*.sql` files.
- Remaining before the launch-week hardening list is clear: the real end-to-end auth pass on `prix-martinique.org` (in progress by Jean-Marie as of this entry).

### Sept 1, 2026 (2) — Google Sign-In Now Branded "Prix Martinique" (new Cloud project, brand verification, /privacy + /terms)
Long browser-driven session (Claude in Chrome, repeatedly fighting a flaky renderer) to fix what the Aug 31 (2) entry flagged: the Google consent screen said "to continue to **euqqxictzvyszjzeejsz.supabase.co**" instead of the app name.

- **Root cause of the blocker**: the Supabase Google provider was wired to an OAuth client (`1004754180045-13l6f5v8udkaqfc4429ukgt7qcagcivc…`) whose Cloud project **nobody could access** — not `jm.philocles@gmail.com` (zero Cloud projects, nothing pending deletion), not any signed-in account. Orphaned, not deleted. So the existing consent screen was untouchable.
- **New dedicated Cloud project** `Prix Martinique` (id `prix-martinique`, project number `952589910366`) created under **`vwapepla.org@gmail.com`** — deliberately the association's contact address (matches the privacy-policy / Mentions-légales contact), not a personal account. Jean-Marie accepted the first-time Google Cloud ToS + the API Services User Data Policy himself (Claude does not accept agreements).
- **Consent screen fully configured**: app name "Prix Martinique", user-support + developer email `vwapepla.org@gmail.com`, home `https://prix-martinique.org`, privacy `…/privacy`, terms `…/terms`, authorised domain `prix-martinique.org`. Scopes: `email`, `profile`, `openid` only — non-sensitive, so **no data-access verification is ever required** (Verification Centre confirms "not required").
- **New OAuth client** "Prix Martinique Web (Supabase)" (`952589910366-m7gb079chdq7cdm4891n8lvj70qjdbnq.apps.googleusercontent.com`), Web application. Redirect URI = the domain-stable Supabase callback `https://euqqxictzvyszjzeejsz.supabase.co/auth/v1/callback`; JS origins `https://prix-martinique.org` + `https://www.prix-martinique.org`. Jean-Marie pasted the new client ID + a freshly-generated secret into Supabase → Auth → Providers → Google (Claude does not handle secrets in plaintext). Verified live via the OAuth URL that the new `client_id` is the one in play. **Existing Google logins are unaffected** — Supabase matches users by email, not by OAuth client.
- **App published to Production** (was "Testing", which caps sign-in to allow-listed test users — a non-starter for a public launch). Basic scopes → no user cap, no "unverified app" interstitial.
- **`euqqxictzvyszjzeejsz.supabase.co` is a *required* Authorised Domain** — Google auto-adds every redirect-URI host. **Do not delete it** or Google refuses the redirect and sign-in breaks entirely. Its list position ("domain 1") is meaningless ordering.
- **`/privacy` + `/terms` shipped as static pages** (`public/privacy.html`, `public/terms.html`; `vercel.json` `"cleanUrls": true` serves them at the extensionless paths). Static, self-contained HTML — no JS, no external resources — so Google's review crawler and the service worker's offline navigation both resolve them. **Content mirrors `LegalModal.jsx` verbatim; keep the two in sync on future edits.** PRs #21 (→dev) and #20 (→main) merged; both live on `https://prix-martinique.org`.
- **`prix-martinique.org` verified in Google Search Console** under `vwapepla.org@gmail.com` via a **DNS TXT record** (`google-site-verification=6mxONI9RW_STbTkDGVfOo-9F_q7iBb8yeWdrOKvhCn8`) added in Vercel DNS (Domains → prix-martinique.org). Verified instantly. **That TXT record must stay** or the domain verification — and the brand verification below — lapses.
- **Brand verification done + published, with logo.** "Verify branding" came back **instantly** both times (basic scopes + verified domain → automated, no human review), even with the logo. Logo = `public/icon-512.png` (the "pm" gradient icon), saved on the consent screen and published. The consent screen now shows **"to continue to Prix Martinique" + the logo** for all users. Confirmed on a real device by Jean-Marie.
- **Cleanup**: after confirming the live client secret, the older unused secret on the new client was disabled → tested (login still worked) → deleted. The orphaned old client in the inaccessible account was left alone (unreachable, now unused).
- See the new `[[project-prix-martinique-google-oauth]]` auto-memory for the durable summary.

### Sept 1, 2026 (3) — Launch-Hardening Pass (#3 + #6 from the pre-announcement plan)
While waiting on RPPRAC to confirm how they'll announce the launch. Branch `chore/launch-hardening`, PRs #22 (→dev) and #23 (→main) merged.

- **Per-user submission rate limiting** — `price_submission_rate_limit_migration.sql` (**applied 2026-09-01** in the SQL Editor). `BEFORE INSERT` trigger on `prices`: caps one caller at **100 rows / rolling 10 min** and **500 / rolling 24 h**. Adds a server-stamped `submitted_by uuid` column (set to `auth.uid()` by the trigger, ignoring whatever the client sent) so the count can't be dodged by spoofing the client-set `user_id`. Admins (`user_roles.role = 'admin'`) exempt; service-role / SQL-editor / Edge-Function inserts (no `auth.uid`) unaffected. Thresholds cover a large in-store scan session and an offline-queue backlog draining on reconnect (`syncQueue.js` is FIFO, re-queues a rejected item up to `MAX_RETRIES=5`). Minor known edge: `performPriceSubmission` creates the product row *before* the price insert, so a rate-limited submission can leave an orphan product (harmless — feed is per-price-row; flag via `TestDataAdmin`).
- **CSP: Google Fonts hosts added** to `style-src` (`fonts.googleapis.com`) and `font-src` (`fonts.gstatic.com`) for the Anton wordmark. **Still `Content-Security-Policy-Report-Only`** — the enforce flip still needs one real iOS Safari test with the barcode scanner (camera + WASM + Quagga workers) actually running.
- **Storage bucket limits** — both `price-tag-photos` and `product-photos` now enforce **5 MB max** file size and **`image/jpeg,image/png,image/webp`** MIME allow-list (was unset / "Any"). The app only ever uploads `image/jpeg` (`base64ToBlob` in `priceSubmission.js` / `MainlandPriceAdmin.jsx` hardcodes it) and reads by stored public URL — never `.list()`/`.remove()` — so this is safe. Blocks SVG (an XSS vector on a public bucket), PDFs, executables.
- **Repo cleanup**: deleted the stray `nul` file (Windows `> nul` artifact); `.gitignore` now ignores `*.pdf` (award/grant material — none tracked) and `nul`.
- **Email templates were already rebranded** — "Confirm sign up" and "Reset password" subjects + bodies already say "Prix Martinique" (Open Item #2 was stale; done by Jean-Marie or an earlier session).
- **PostHog launch monitoring** — built the pinned "Prix Martinique — Launch Monitoring" dashboard (project 232864, id **928161**): traffic-by-`$host`, submissions/day, contributors & onboarding, `$exception`/day, daily visitors, + a caveats header. Companion to the "Product Analytics" dashboard (862895).
- **⚠️ Critical finding — PostHog has captured ZERO events from any deployed domain, ever** (90-day check: only `localhost` dev traffic). ~~Capture is cookie-consent-gated~~ **[Corrected Sept 3 (2): the consent gate was a contributing cause, but the primary one is that `VITE_POSTHOG_KEY` never reached the Vite build — `posthog.init()` has never run in production. See the Sept 3 (2) entry.]** Consent-independent signup/session data lives in Supabase `auth_events` / `app_sessions` (Console Admin → "Appareils & Connexion").
- **Supabase Pro headroom checked** — comfortable for a launch spike: 2 MAU / 100 K, 0.25 GB storage / 100 GB, ~0.5 GB egress / 250 GB. The one soft ceiling is **realtime concurrent connections (500 cap)** — the app opens a realtime channel per visitor for the live price feed; at ~500 simultaneous users new subs are refused and the feed stops live-updating (page still works). Not a hard blocker.
- **Storage RLS not touched this pass, flagged instead**: both buckets have a `public` INSERT policy (lets *anonymous* clients upload directly, bypassing the app's sign-in gate) and a broad SELECT policy (lets anyone enumerate every file). The app only uploads as an authenticated user and reads by known URL, so tightening INSERT to `authenticated` and removing the list policy is safe — but it's a production-RLS change that wasn't in scope for a hardening pass on launch day. Jean-Marie's call.

### Sept 3, 2026 — PostHog Switched to Cookieless / Consent-Exempt (cookie banner removed)
Follow-up to the launch-stats review. PostHog had recorded **zero** events since the Sept 1 launch (Open Item #1): the `opt_out_capturing_by_default: true` gate from the Aug 29 RGPD pass meant nothing fired — not even `$pageview` — until a visitor clicked "Accepter" on `CookieConsentBanner`, and essentially nobody did (1 `$opt_in` ever; the owner's own browser sat on `pm_cookie_consent_choice = "declined"`). Meanwhile Supabase `app_sessions` (consent-independent) logged ~670 real sessions in the same window. Decision (Jean-Marie): drop the consent model, run PostHog cookieless. Branch `feat/posthog-cookieless`.

- **`src/posthogClient.js`**: removed `opt_out_capturing_by_default`; added `cookieless_mode: 'always'` + `person_profiles: 'never'`. posthog-js now never writes a cookie or touches local/session storage — visitors are counted server-side via an irreversible rotating hash (team + daily salt + IP + UA + host; salt discarded after processing, IP never stored — project-side `anonymize_ips` was already on). No persistent identifier on the device ⇒ aggregate audience measurement that does not require a prior-consent banner under GDPR/ePrivacy (CNIL "exempted analytics" posture). Disclosed in the privacy policy instead.
- **PostHog project 232864**: "Cookieless server hash mode" set to **Stateful** (`cookieless_server_hash_mode: 2`) via MCP — keeps a short-lived server-side session map so session counts / duration / within-session funnels stay accurate; still no client identifier. Stateless (1) was rejected — it would inflate unique-visitor counts (daily hash rotation with no session bridging).
- **`src/components/CookieConsentBanner.jsx` deleted**; import + render removed from `App10.jsx`. Stale `pm_cookie_consent_choice` / `__ph_opt_in_out_` localStorage keys on returning visitors are simply never read again — left in place, harmless.
- **`src/contexts/AuthContext.jsx`**: removed all `posthog.identify(session.user.id, { email })` (3×) and the `posthog.reset()` on sign-out (1×) — a stable `distinct_id` is itself personal data and can't be persisted cookielessly anyway. `anon_to_signup_converted` and every feature `capture()` still fire, now anonymous/aggregate. Every `posthog.captureException` left untouched (aggregate error monitoring; payloads only carry a `context` string + component stack, no PII).
- **`src/components/LegalModal.jsx` + `public/privacy.html`** (kept verbatim-in-sync per the Sept 1 (2) note): section 2d + section 6 rewritten — legal basis for audience data is now *intérêt légitime* only (was "consentement / intérêt légitime"), "sans cookie ni identifiant", "aucun bandeau cookies n'est nécessaire".
- **Tradeoffs accepted**: no per-user analysis in PostHog (that lives in Supabase / Console Admin), no GeoIP enrichment, unique-visitor counts run slightly high vs a cookie'd setup. Session replay / surveys were already unused. No runtime feature-flag reads exist in the app, so the cookieless flag-eval delay is a non-issue.
- `npm run build` + `node node_modules/eslint/bin/eslint.js .` both clean.
- **Still to do after merge/deploy**: load `prix-martinique.org`, take an action, and confirm an event lands in the raw `events` table (first real production capture this project will ever have).

### Sept 3, 2026 (2) — Real Root Cause: `posthog.init()` Has Never Run in Production (env var in wrong `vercel.json` block)
PR #26 (cookieless) merged and auto-deployed — then the post-deploy capture check **still showed zero events**. Traced it in the live browser (unregistered the stale SW, hard-reloaded, inspected the production bundle):

- **`posthog-js` is bundled but `posthog.init()` never fires.** `src/posthogClient.js` guards on `if (posthogKey && ...)`, and `import.meta.env.VITE_POSTHOG_KEY` is **`undefined` in the production build** — the deployed bundle contains no `phc_…` key and no `eu.i.posthog.com` host string (confirmed by grepping `/assets/index-*.js` live). The Supabase URL + anon key **are** in the bundle (they come from Vercel **Dashboard** env vars, added Jan 11).
- **Why**: `vercel.json`'s top-level `"env"` block injects vars into **Serverless Function runtime**, not the **build**. Vite inlines `import.meta.env.VITE_*` at *build* time from `process.env`, so `VITE_POSTHOG_KEY` was never available when `vite build` ran. It's been in the wrong place since it was added (Jul 26). **This — not the consent gate — is the primary reason PostHog captured nothing from any deployed domain, ever.** (The Aug 29 consent gate would also have blocked it; removing it in PR #26 was still correct, just insufficient alone.)
- **Fix** (branch `fix/posthog-env-build`): moved the two vars from `"env"` to `"build": { "env": {...} }` in `vercel.json` (the PostHog project key is a public, write-only client token — safe to commit, and it already was). Added a `console.warn` in `posthogClient.js` when `VITE_POSTHOG_KEY` is missing in a `PROD` build, so this failure mode is never silent again.
- **If `build.env` still doesn't take** (Vercel can be finicky): add `VITE_POSTHOG_KEY` + `VITE_POSTHOG_HOST` as Vercel **Dashboard** → Environment Variables (Production + Preview), exactly like the Supabase vars, and drop the `vercel.json` block. Verify post-deploy by grepping the built bundle for `phc_`.
- **Verified live 2026-09-03** (PR #27 merged + auto-deployed): production bundle now contains `phc_…` + `eu.i.posthog.com`; `posthog.init()` runs (loads remote config + exception-autocapture); events POST to `/i/v0/e/` (HTTP 200) and land in the raw `events` table — `$pageview`, `$pageleave`, `$autocapture`, `store_wizard_step_viewed`, all `host = prix-martinique.org`. Cookieless confirmed (no cookies, no `__ph_*` localStorage). **First production capture in the project's history.** `build.env` worked — the Dashboard fallback wasn't needed.

### Sept 3, 2026 (3) — Analytics & Monitoring Overhaul (multi-milestone; plan tracked in `ANALYTICS_MONITORING_PLAN.md`)
Strategic review of the Admin Dashboard + PostHog after the launch-stats work. Findings and the 6-milestone plan live in **`ANALYTICS_MONITORING_PLAN.md`** (checkbox-tracked). Headline problem: `AdminDashboard.jsx`'s `fetchAdminStats()` reports **raw `count(*)`** with no scoping — "Total Scans" (76) mixes real user submissions + test data (`products.is_test_data`) + admin-entered France Hexagonale reference prices (`source_type='admin_reference'`) + legacy null-user rows, so it isn't "real prices from real users". Also: it counts `user_id` (client-set, spoofable, null for legacy) instead of the server-stamped `submitted_by`; 6 derived stats `.select()` full tables client-side and silently cap at Supabase's 1000-row default; the app shows users a *different* filtered number ("N prix partagés"). Guiding principle for the overhaul: **Admin Dashboard = exact business + data-health truth (Supabase, consent-independent); PostHog = behavioral depth + independent cross-check** — and because PostHog is now cookieless (`person_profiles: 'never'`), all user-level retention/cohort analysis is **Supabase-only**.

- **M1 ✅ (foundation) — done + verified live 2026-09-03** — `analytics_admin_functions_migration.sql` (+ `analytics_admin_functions_fix1_migration.sql`): admin-gated `SECURITY DEFINER` RPCs (`admin_kpi_overview`, `admin_price_timeseries`) + `v_admin_prices` view, correctly scoped (`is_test_data=false`, `martinique_scan` / `diaspora_scan` / `admin_reference` channels, `coalesce(submitted_by,user_id)`, `user_profiles.is_internal_account` toggle). `AdminDashboard.jsx` reworked. **Reconciles exactly**: internal-OFF → 61 real + 5 test + 10 réf. = 76 (the old raw "Total Scans"); 57 priced + 5 test = 62 catalog. Internal-ON → 29 real from 5 external contributors. PRs #29 + (fix1). fix1: reference/test *context* counts no longer internal-filtered.
- **M2a ✅ — verified live 2026-09-03** — `analytics_admin_export_migration.sql` (`admin_submissions_detail` RPC) + `AdminDashboard.jsx`: date-range segmented control (7j/30j/90j/Tout, persisted), dependency-free `Sparkline` on the Contributions + Contributeurs tiles, **"Exporter CSV" wired** (client-side Blob of the scoped submissions — RPPRAC/grant reporting), "Activité Récente" upgraded (product·price·store·contributor·date·TEST/FR/RÉF badges). PRs #31 + #32. **fix2 (`analytics_admin_functions_fix2_migration.sql`, PR #32)**: `prices.created_at` is `timestamp` not `timestamptz`, so `admin_price_timeseries` (never live-tested in M1) + `admin_submissions_detail` 400'd (42804); recreated both with `at time zone 'UTC'`. Live check: sparklines draw, CSV → 200/26 rows/download, Activité Récente shows 8 rich rows.
- **App-header "prix partagés" fix** (`community_price_count_migration.sql` + `App10.jsx`) — the header pill (comment: "community contribution count (everyone)") was `recentPrices.length`, i.e. the capped 50-row Comparer feed minus Hexagone + test rows → showed ~35 and stayed flat however much the community contributed. Now calls a public `community_price_count()` RPC (same scoping as `admin_kpi_overview.real_submissions` minus the internal filter — all contributors, no admin-reference, no test) so the pill matches Console Admin's "Contributions de prix" (61) and grows for real. Falls back to `recentPrices.length` if the RPC isn't applied yet.
- **M2b ✅ — verified live 2026-09-03** (PRs #35–#38; migrations `analytics_admin_m2b_migration.sql` + fix1 + fix2 applied) — drill-downs + moderation queue. `admin_submissions_browse` (paginated 25/pg, channel + "à revoir" filters, per-row `review_reason`) powers the Contributions drill, "Voir tout" **and** "Modérer Prix" (`p_review_only`); `admin_contributors` roster; `admin_submissions_detail` widened with `product_id` → Activité Récente click-through to `ProductDetailModal`. New `AdminDrillPanel.jsx`; the 4 KPI tiles are buttons. Live check: pagination ("1–25 sur 26" → "26–26 sur 26"), filters, review flags (`compte récent` / `nouveau` / TEST / RÉF / MQ) all work. fix1: `42702` OUT-param/column clash → `#variable_conflict use_column`. fix2: `42804` — `store_id` is `bigint` not `uuid` (≠ `prices.id` / `products.id`). Lesson: the drill panel's "migration pending" notice masks any RPC 400 — verify with the network tab, not just the UI. `admin_submissions_browse(...)` (paginated, channel + "à revoir" filter; per-row `review_reason`: `magasin manquant` · `prix aberrant` [>3× / <0.34× product median, ≥3 real prices] · `compte récent` [<7 j]) powers the Contributions drill, "Voir tout", **and** "Modérer Prix" (`p_review_only`). `admin_contributors(...)` → contributor roster (first/last, totals, channel mix). `admin_submissions_detail` widened with `product_id` (drop+recreate) → Activité Récente rows click-through. New `AdminDrillPanel.jsx` full-screen overlay; the 4 KPI tiles are now buttons (Contributions/Contributeurs → drill; Produits/Produits MDD → "Compléter produit"); rows → `ProductDetailModal`. Build + eslint clean.
- **M3 ✅ — verified live 2026-09-03 (PRs #40, #41)** — "Santé des données". `admin_data_health()` (freshness % + median latest-price age, categorisation %, photo %, barcode %, magasins actifs 30 j, postes BQP couverts, open `barcode_flags`), `admin_category_coverage()` (per-category priced/total/%), `admin_coverage_gaps(p_limit)` (`store_stale` / `demanded_unpriced` / `uncategorized`). Dashboard "Santé des données" section (`Meter` bars + counters + "Voir les lacunes" → `AdminDrillPanel` `health` mode). Live: 59.6% fresh, 68.4% categorised, 90.2% photo, 94.7% barcode, 5/78 magasins, 1/16 BQP; drill shows 73 stale stores + 7 uncategorized products. fix1: `admin_coverage_gaps` → `42703` (unnamed derived-table cols) → `g(kind, ref_id, label, sublabel, weight)` alias-list.
- **M4a ✅ — verified live 2026-09-03 (PR #42)** — MTQ↔Hexagone matching. `prices.source_channel` (`martinique_scan` / `diaspora_scan` / `chain_app_screenshot` / `online_capture`, backfilled) + `match_verified*` cols. `admin_mainland_match_coverage()`, `admin_mainland_gap_by_category()` (all classifiers `coalesce(source_channel, derive)`). Dashboard "Comparaison France Hexagonale" + `AdminDrillPanel` `mainland` mode. **Live: 17.5% match rate (10/57), +45.9% median MTQ premium** — the headline *vie chère* number; by category Sans catégorie +56.9% / Épicerie Salée +36.9% / Épicerie Sucrée +54.9% / Hygiène +45.9%.
- **M4b ✅ — verified live 2026-09-03 (PR #43)** — `admin_mainland_match_queue(p_limit)` + `admin_verify_mainland_match(p_price_id, p_ok)` (true = verified, false = delete). `MainlandPriceAdmin` "Trouvé en ligne / Capture appli enseigne" toggle (→ `chain_app_screenshot`, photo required). `priceSubmission.js` stamps `diaspora_scan`. `mainland` drill "File de vérification" with ✓/✗. Live: queue lists pending France entries (Cassoulet +73.1%, Huile Olive −25.2%…), ✓ removes the row.
- **M5 ✅ — verified live 2026-09-03 (PR #44)** — "Valeur livrée". `admin_value_delivered(p_since)` (median + weighted/basket gap %, MTQ dearer/cheaper, BQP-matched + BQP gap, community savings €, # under-average contributions). Public `community_mainland_gap()`. Dashboard "Valeur livrée" red card (**+36% weighted / 45.9% median** live). Community → Impact "L'écart vie chère" card (**+45.9% médiane** live, above Score de Souveraineté).
- **M6 ✅ — done 2026-09-03 (PR #45 + PostHog MCP)** — PostHog↔Admin cross-check. **`METRIC_CATALOG.md`** (repo): every metric → authoritative Supabase source + PostHog counterpart + target, covering M1–M5, the cookieless retention constraint, the events that fire today. Gap events shipped: `product_detail_viewed`, `mainland_match_verified` / `_rejected`, `mainland_screenshot_uploaded` / `mainland_online_capture_added`. **PostHog dashboards curated** (928161 / 862895): both headers rewritten (cookieless status, retention caveat, catalog link — the old "consent-gated, zero prod events" note was stale), "Price Submissions" annotated (≤ Console Admin), new tiles — "Store coverage — distinct stores selected (weekly)" (928161), "Activation funnel — store selected → first contribution" + "M4 / M5 cross-check events" (862895), "Repeat Contributors" renamed `⚠ cookieless-unreliable`. **The Analytics & Monitoring Overhaul (Open Item #10) is complete** — only the stretch `posthog_metrics_daily` export table is unbuilt.
- **M4** Martinique ↔ France Hexagonale matching pipeline — coverage tracker + **screenshot-upload capture from chains' own apps** (`source_channel='chain_app_screenshot'`) alongside diaspora real scans + admin online capture; verification queue.
- **M5** "Valeur livrée" section — the *vie chère* headline gap (MTQ↔Hexagone), aggregate savings.
- **M6** PostHog ↔ Admin cross-check layer — instrumentation gaps, `METRIC_CATALOG.md`, curated PostHog dashboards (928161 / 862895) so each admin KPI has a PostHog counterpart.

---

## Open Items

_Refreshed 2026-09-01. Ordered roughly by urgency given the Sept 1, 2026 launch._

### Done (2026-08-31 → 09-01)
- ✅ **Domain cutover to `prix-martinique.org`** completed + verified (Aug 31 (2)). `www` → 308 → apex; `*.vercel.app` still serves for stranded PWAs.
- ✅ **Google sign-in branded "Prix Martinique" + logo** — new Cloud project under `vwapepla.org@gmail.com`, brand verification done + published (Sept 1 (2)). Consent screen no longer shows the `supabase.co` URL.
- ✅ **`/privacy` + `/terms` live** on `prix-martinique.org` (static pages, `cleanUrls`). Content mirrors `LegalModal.jsx` — keep in sync.
- ✅ **`prix-martinique.org` verified in Google Search Console** (DNS TXT via Vercel DNS — the `google-site-verification=…` record **must stay**).
- ✅ **Per-user submission rate limiting** — `price_submission_rate_limit_migration.sql` applied (Sept 1 (3)). 100/10 min, 500/24 h, admin-exempt, server-stamped `submitted_by`.
- ✅ **Storage bucket limits** — `price-tag-photos` + `product-photos`: 5 MB max, `image/jpeg,image/png,image/webp` only.
- ✅ **CSP Google-Fonts hosts** added (`fonts.googleapis.com` / `fonts.gstatic.com`) — still Report-Only.
- ✅ **Supabase email templates** — "Confirm sign up" / "Reset password" already say "Prix Martinique" (subject + body).
- ✅ **Repo cleanup** — stray `nul` deleted; `.gitignore` ignores `*.pdf` + `nul`.
- ✅ **All three pending migrations applied** (`recipes_photo_urls`, `display_name_google_oauth_fix`, `analytics_tracking`) + both QA cleanup rows.
- ✅ **Supabase Pro** active (automated backups on). Headroom checked — comfortable for a launch spike.
- ✅ **`dev` → `main` current** through PR #27 (Sept 3). Aug work via PRs #14–#20; this session's via #21, #20, #22, #23, #26 (cookieless), #27 (env-var build fix).
- ✅ **PostHog captures on production — verified live 2026-09-03** (Sept 3 + Sept 3 (2) entries). Was blind for 14 months: the Aug 29 consent gate **and** `VITE_POSTHOG_KEY` never reaching the Vite build. Both fixed (PRs #26 + #27); events now land in the raw `events` table from `prix-martinique.org`. Cookieless / consent-exempt. Launch dashboard: PostHog project 232864, dashboard **928161** ("Product Analytics" companion: **862895**). Aug 31 → Sept 3 is an unrecoverable gap in PostHog (nothing was ever emitted); that window lives in Supabase `app_sessions` / `auth_events` (Console Admin → "Appareils & Connexion").

### Launch-blocking / do before the announcement
1. **One real end-to-end auth pass on `prix-martinique.org`** — Google sign-in, email/password sign-in, password-reset (confirm the reset link lands on the apex, not `*.vercel.app`).

### Hardening / follow-through (launch week, not hard blockers)
2. **CSP enforce.** Fonts allowance done; still need one real iOS Safari test with the scanner running (camera + WASM + Quagga workers), then flip off `Report-Only`.
3. **Storage RLS** (optional — not touched on launch day). Both buckets have a `public` INSERT policy (anonymous direct uploads, bypassing the app's sign-in gate) and a broad SELECT policy (anyone can enumerate every file). The app only uploads as an authenticated user and reads by known URL, so tightening INSERT → `authenticated` and dropping the list policy is safe. Jean-Marie's call.
4. **Realtime 500-connection ceiling.** The live price feed opens a realtime channel per visitor; at ~500 simultaneous users new subs are refused and the feed stops live-updating (page still works). Watch during a big RPPRAC push.
5. **`delete-user-account` Edge Function** — deployed live Aug 29 (2), not tracked/reproducible from this repo (no Supabase CLI here). Source at `supabase/functions/delete-user-account/index.ts`; keep in sync if the deletion flow changes.

### Known bugs / deferred, no fix in progress
6. **iOS camera re-prompt on every scan.** The duplicate-mount fix (Aug 28 (6)) needs a real-device re-test. The larger fix — keeping the camera stream alive across scan-close/reopen cycles — is still not attempted (fragile, hard-won scanner pipeline; also a UX/privacy tradeoff).
7. **Second geolocation prompt after the OAuth full-page redirect.** The persist/resume fix (Aug 28 (2)) mitigates the lost-submission symptom but `StoreSelectionWizard` still mounts for one render before restore. Possibly compounded by an iOS PWA cold-launch quirk. Re-check on the next real-device pass.
8. **Quagga → `@ericblade/quagga2`** — CVE mitigation / potential scanner rework. Requires physical iOS testing. Direction TBD. See "Accepted Risks & Frozen Dependencies" above.
9. **Panier `products.category_id` write RLS** — resolved empirically as "no new policy needed" (Aug 5), only tested against one account (`JMP2_972`). Confirm from a second ordinary account if it looks flaky.

### In progress (multi-milestone)
10. **Analytics & Monitoring Overhaul** — Admin Dashboard + PostHog made accurate and cross-checking. 6 milestones, checkbox-tracked in **`ANALYTICS_MONITORING_PLAN.md`**. **M1–M6 ✅ — the Analytics & Monitoring Overhaul is COMPLETE** (PRs #29–#45, + PostHog dashboards 928161 / 862895 curated per `METRIC_CATALOG.md` §7). Only the stretch `posthog_metrics_daily` export table is unbuilt. See the Sept 3 (3) changelog entry.

### Migration-header reconciliation (bookkeeping)
11. Two `.sql` file headers disagree with this changelog's body — reconcile when convenient:
    - `product_test_flag_migration.sql` header says "NOT YET APPLIED (2026-07-28)"; the Jul 28 entry documents it applied the same day. **Body is believed correct (applied).**
    - `product_favorite_counts_migration.sql` header says "APPLIED … 2026-07-28"; the Jul 28 "Prix Recherchés" entry calls it "not yet applied". **Header is believed correct (applied)** — confirm and update the entry's inline note.

---
**Last Updated**: 2026-09-03
**Current Version**: MVP v1.5 (App10)
**Status**: Launched (Sept 1, 2026). Live on `https://prix-martinique.org`; Google sign-in branded + verified; `/privacy` + `/terms` live; submission rate-limiting + storage limits in place. **PostHog now captures on production** (cookieless / consent-exempt — PRs #26 + #27, verified Sept 3). `main` current through PR #27. Before the RPPRAC announcement: one real end-to-end auth pass on the apex (Open Item #1).
